# 0802 — mcc/m++ 统一重构进度（worktree-mxx-work）

> 分支：worktree-mxx-work。目标：mcc 重构为「共享后端 + 多语言前端」，MIR 完备后启动 m++。
> 计划文档：/root/.codebuddy/plans/cosmic-forging-einstein-Cjwtv5Wd.md

## 用户已确认决策（0801-0802）
1. 统一归 mcc 管理（gcc 风格：核心后端 + 多语言前端）
2. 双二进制共享库：mcc（C 驱动）+ m++（C++ 驱动），共享 libmcc.a
3. 项目命名 m++（内部目录 src/cpp/，产物 m++）
4. 先定型 MIR 再接 C++；m++ 构建中迭代完善 MIR
5. C++ 参考多元化（GCC/Clang 思想 + aburiscript + cplusplus + 标准文档），避免复刻
6. C++ 标准广覆盖：C++98/03 → C++11 → C++14/17 → C++20/23
7. MIR 代码质量/优化程度极限化（接近 gcc -O1/-O2）

## 已完成（2026-08-01/02）
### B.1 MIR 核心（0fc8f8c）
- `include/mir.h`：MType（i8..f64/ptr/agg）、MTypeDesc 聚合（字段/位域/union/array + ext 槽）、
  MVal 值表、MConst 常量池（hash 去重）、MIns 三地址+extra 扩展位、显式 MPhi、MFn/MBlk/MRef
- `src/mir/{build,mir_util,print}.c`：构造 API、arena 分配、-dmir dump
- `test/mir/mir_test.c`：类型/聚合/常量池/构造单测全过
- `docs/mir-spec.md` + `docs/mir-type-matrix.md`
- 验证：`make check-mir-types`

### B.2 MIR 优化管线首批（220086c）
- `src/mir/passes.c`：build_uses（SSA use 链）、mfold（常量折叠）、
  msimpl（x+0/x*1/x^x、udiv 2^n→shr、and/or/xor 恒等）、mdce（死代码消除，保留副作用指令）
- `run_mir_passes(fn, optlevel)` 管线入口
- `test/mir/pass_test.c`：fold/simpl/dce 单测全过
- 验证：`make check-mir-passes`

### B.4 bridge + 架构正确化（753b00b, f16358d）
- `src/lir/bridge.c`：MFn→Fn（LIR）完整翻译（Opar 入口、Phi、调用 Oarg+Ocall、跳转/返回）
- `run_passes()` 从 src/irgen/emit.c 迁移到 src/ir/passes.c（共享后端，libmcc.a）
- `include/ir.h` 加 include guard（修双重包含）
- **端到端验证：构造 fib MFn → MIR passes → bridge → LIR 管线 → x86-64 汇编 → 链接运行，fib(5)==5 通过**
- 自举兼容：mx_strdup（替换 strdup）、可移植 ctzll（替换 __builtin_ctzll）、bridge const 强转
- mcc 可自编译自身全部源码；`--specs=meuos` 静态编译 hello 运行 exit=0
- 验证：`make check-mir`（types+passes+bridge 全绿）

## 里程碑状态
| 里程碑 | 状态 |
|:-------|:-----|
| M1 MIR 定型 | ✅ B.1+B.2 首批+bridge 完成 |
| M2 C 迁移 | ✅ B.4.2 func_to_mir 打通（MCC_USE_MIR=1，全源码 90/90 编译）|
| M3 m++ hello | ✅ C.1 骨架 + C++ class 数据成员解析 |
| M4-M6 | ⏳ |

## m++ 进度（C 线，3958de5）
- C.1.1 双二进制：mcc/m++ 共享 driver（mcc_main + mpp_main），正确区分 version/help
- C.1.2 C++ lexer：关键字表（C++98/11/20）+ 分类测试
- C.1.3 C++ 解析入口 + 语言分派：.cc 识别，走 C++ 前端；class 声明解析（访问控制 + 裸类名 + 成员访问）
- C.2.3 成员函数**定义降级**：类内 `void inc() {...}` → 独立函数 `Class_inc`，this 参数注入 + 函数体完整解析
  - `cpp_define_method(s, funct, mname, class_tag)`：构造 `Class_method(Class *this, args...)` 签名，
    复制参数 decl、注册符号到 scope（mangled 名持久化）、mkscope 注入 this+params、mkfunc/stmt/emitfunc 真正生成代码
  - `cpp_member_ident`：方法体内裸成员名降级——数据成员 → `(*this).name` lvalue，函数成员 → `Class_method` + this 传递
  - struct/union 路径（tagspec）也走成员函数降级（class_tag 从 b->type->u.structunion.tag 取）
- C.2.3 成员函数**调用降级**：`c.inc()` → `Counter_inc(&c)`，`obj.meth(args)` this 作为首参数 prepend
  - 修复：mangled 名持久化（mkdecl 只存指针）、函数标识符 decay、firstarg 逗号标志（this prepend 不强制逗号）
  - `g_cpp_member_this` 残留防护：`obj.meth` 后非 `(` token 时清除，避免泄漏到无关调用
- 已验证：counter/full/adv/methcall/stru/all/leak/ctor/ctor4 测试全过（成员调用、this 访问、方法内方法调用、struct 类、箭头访问）
- C.2.3 构造**函数**：`ClassName(...) { ... }` 定义降级为 `Class_Class`（构造名=类名，declspecs 消费类型后 `(` 检测 + 手写参数解析），
  局部对象 `Counter c;` 自动调用默认构造（cpp_emit_default_ctor，defineobj 后 hook）
- C.2.3 访问**控制**：member.access 记录 public/private/protected（class 默认 private，struct/union 默认 public），
  structbuilder 随 access 标签更新；类外 `obj.member`/`obj->member` 访问 private/protected 报错，同类方法内允许
  （cpp_same_class_context/cpp_member_accessible，tagspec struct 体也支持 access 标签）
- C.2.3 类外方法定义：`void Class::method(...) {...}` 走 `::` 限定名（declaratortypes 生成 mangled `Class_method` + 记录类名），
  decl() DECLFUNC 分支转交 cpp_define_method（注入 this + 方法上下文）；修复：qual 只被 DECLFUNC take、
  mkfunc 用 d->name 而非已 free 的 pmangled
- C.2.3 单继承：`class D : public Base {}` — cpp_class_decl 解析基类列表，基类作为**匿名成员**注册到派生类
  （offset 0，typemember 递归查找天然支持基类成员）；cpp_is_member_function/cpp_mangled_name 递归到定义类
  （`d.bump()` → `Base_bump(&d)`）；派生构造自动调用基类默认构造（cpp_emit_base_ctor）；
  protected 基类成员对派生类开放（cpp_is_derived）
- C.2.1 命名空间：`namespace NAME { ... }` — scope 加 name 字段、新 DECLNAMESPACE decl kind（decl->u.ns 指向 namespace scope），
  namespace 块解析（内部声明注册到 ns scope，嵌套支持，scope 保留不释放）；`ns::name` 表达式多级限定查找
  （primaryexpr 循环）；`ns::Type` 类型名（declspecs）；namespace scope 视为文件级上下文（linkage 修复，
  避免 funcinit(NULL) 崩溃）；类成员符号注册到类的声明 scope（type->scope），调用端从类 scope 查找
- C.2.3 析构函数：`~Class() {...}` 定义降级为 `Class_dtor`（TBNOT + 类名检测，成员注册为 `~Class` 标记名）；
  块退出时（compound 分支）遍历 scope 局部类对象调用析构；return 语句求值后、funcret 前调用（避免 dead 块）；
  dtor_done 标记防双重析构；修复空 scope 的 cap 未初始化导致的越界（检查 len==0）
- C.2.3 带参构造调用（vexing parse）：`Point p(3, 4);` — declaratortypes func 分支检测"参数是表达式非声明"（is_ctor_expr_start），
  按表达式解析参数存入 cpp_ctor_args 全局，declarator 返回 base 类类型 + 哨兵 expr；decl() 检测哨兵后重建对象，
  DECLOBJECT 分支 emit `Point_Point(&p, 3, 4)`（cpp_emit_ctor_call）
- C.2.3 重载解析：mangled 名带参数类型编码（`Class_method_ii`，cpp_mangle_type：i/u/l/L/j/J/c/C/s/S/f/d/e/p/o<tag>/v/b/n）
  — 定义端 cpp_define_method 编码显式参数；调用端 TPERIOD/cpp_member_ident 记录 pending member context
  （g_cpp_member_class/name + 深度），TLPAREN 收集实参后按类型解析重载（cpp_mangled_name_args）；
  占位 decl 处理（重载基础名不存在）、pending 深度机制（嵌套参数 postfixexpr 不误清外层）、
  `f.set;` 未调用歧义报错；修复 decay 后设 decl 破坏 TBAND、TLPAREN 多余 next()
- C.2.3 两阶段类解析：类体解析时方法体 token 缓冲（cpp_pending_method），类体结束后（布局确定）重放解析，
  方法体可引用后声明成员/方法互调；pp.c 新增 tokpush（token 数组 pushback）；修复 flush 时类体后 token
  被覆盖丢失（先压回当前 tok 再压方法体）
- C.2.3 const 成员函数：`void get() const {...}` — structdecl/decl() 方法声明后检测 TCONST，this 参数为
  `const Class*`，mangled 名加 K 后缀（`Class_methodK`）；调用端 const 对象选 K 版本（非 const 对象可回退调
  const 方法，const 对象调非 const 方法报错）；const 方法内成员访问带 const 限定（写成员报错）
- C.2.3 多继承：`class D : public A, public B` — 基类列表循环解析，各基类作为匿名成员按布局偏移注册；
  基类构造链支持子对象偏移（emit_base_ctors_for，this + offset）；继承方法调用 this 指向定义类子对象
  （TPERIOD/cpp_member_ident offset 调整）；派生类无显式构造时也调用基类构造
- C.2.3 运算符重载：`Vec operator+(Vec o)` — structdecl 检测 `operator` 关键字 + 运算符 token（cpp_op_mangle：
  pl/mi/ml/dv/rm/eq/ne/lt/le/gt/ge/pp/mm/ad/or/er/nt），手动解析参数列表，mangled `Class_operator_pl`；
  调用端 binaryexpr 检测左操作数是类 + operator 重载 → `a + b` 降级为 `a.operator+(b)`（cpp_try_operator_call）；
  修复 emittype 函数成员不占布局（overlapping）；`return *this` 形式支持类返回（分步链）
- C.2.1 using 指令：`using namespace NAME;` 把 namespace 加入 visible ns 列表（cpp_lookup_visible，
  expr_primary 的 scopegetdecl 失败后查找）；`using NAME::member;` 导入单个成员到当前 scope
- C.2.1 namespace 内类外定义：declaratortypes 多级限定名解析（`ns::Class::method`，遍历 namespace 链
  找到类），cpp_set_qual_ns 记录类所在 namespace，cpp_define_method 从该 scope 查类
- C.2.3 临时对象构造：`Vec(expr)` 作为表达式 — expr_primary 检测类名 + TLPAREN（1-token lookahead），
  cpp_temp_construct 分配匿名临时（funcinit）+ 构造调用 + 返回临时 lvalue；两阶段类解析的布局 finalize
  移到 flush 前（方法体可构造本类对象）；解锁 `return Vec(...)` 与链式 operator+ 返回类对象
- C.2.3 引用类型参数：`const Vec &o` / `Vec &o` — declaratortypes 处理 `&`（TBAND）创建 isref 指针类型；
  方法体内引用参数标识符自动解引用（`o` → `*o`）；调用端对引用参数传 `&expr`（绑定地址）；
  mangling 按被引用类型编码；修复 mktype 未初始化 isref 字段
- C.2.3 静态方法：`static void reset() {...}` — structdecl 检测 SCSTATIC + 方法 → cpp_define_method is_static
  （无 this 参数、方法体无 this 上下文，mangled 加 S 后缀在参数编码后）；`Class::method(args)` 类限定
  静态调用（expr_primary 的 `::` 分支，手动解析参数 + 构造调用）
- 已知限制：
  - 模板/多态/虚函数、虚继承、多继承同名成员歧义、bool 返回 operator 在链式临时场景（int 返回正常）、
    引用变量声明（`Vec &r = x`）、引用重载区分（值/引用参数 mangled 相同）、静态数据成员
    （类内声明 + 类外定义 + 访问）、析构逆序、全局对象构造/析构、`&obj.meth` 取重载方法地址（歧义报错）、
    嵌套类、mutable 成员、非成员运算符重载
  - 构造函数**重载**（`Point()` 和 `Point(int,int)` 都 mangle 为 `Point_Point` 冲突）→ 需参数类型编码 mangling
  - `Point p(3,4)` 带参构造**调用**（vexing parse，declarator 按参数声明解析失败）→ 需 declarator 回退机制
  - `const` 成员函数（尾部限定符）、多继承/虚继承、using 指令、namespace 内类外定义、析构逆序（对象按声明逆序析构）、全局对象构造/析构
- 待实现：模板、多态/虚函数
- 测试：make check / check-mir / check-cpp 全绿；另修 bridge_test fib export（static 导出修复后测试未同步）

## 下一步
- C.2.2/2.3：class 成员函数（this 指针）、构造/析构
- C.2.1：命名空间/using
- B.4.8/B.5：bridge 文档化 + 目录迁移

## MIR 路径验证基线（e24271d）
- `make check` / `check-mir` 全绿（direct 路径不受影响）
- c99 14 组、c11 19/19、c23 21/21 MIR 路径全部通过
- **mcc 自身 90/90 全源码 MIR 路径编译通过**（-S 和 -c 都过）
- 修复：比较/浮点宽度、store/load 宽度（ISTORES→movss）、static 符号导出

## 已知缺陷（MIR 路径 MCC_USE_MIR=1）
- **mcc_mir 自举产物运行时卡死**：MIR 前端 + gcc 后端混合链接的 mcc_mir 能 --version/--help，但 -E/编译卡死。`--help` 正常说明 driver 基本可用，卡死在预处理/编译管线（MIR 编译的 pp/scan 运行时 bug）。哈希/字符串/字符扫描等 MIR 编译的程序均正常——是 mcc 内部特定逻辑的运行时问题。**修复方向**：需专门调试 MIR 编译的 pp.c 运行时行为（对比 direct 编译的 pp）。
- 结论：**MIR 路径功能完备（普通程序 + mcc 全部源码编译正确）**，自举产物的运行时交互待后续迭代（按计划"m++ 构建中同步完善 MIR"）。

## 下一步
- C.1：m++ 骨架（C 兼容层可复用 MIR 路径）
- B.4.8/B.5：bridge 文档化 + 目录迁移（src/c/、src/lir/）


- B.2 剩余：GVN/GCM/SRA/copy 强化（需 MIR CFG 基础设施）
- B.3：后端 spill/peephole/rega 优化
- B.6：MIR 完备验收（六项）

## 环境说明
- check-c99/c11 默认 specs 需 libc-meuos.a；已 `make install` 到 projects/sysroot/usr/lib。
- MEUOS_SYSROOT=/workspace/MeuOS-Kit/sysroot（主仓库）；worktree 内用 projects/sysroot。
