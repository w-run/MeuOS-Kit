{
    "manifest_version": 1,
    "description": "任务类型 -> 验收脚本映射表。task_executor 按任务卡片 task_type 查找对应验收脚本，与 common_checks.sh 串行执行。",
    "task_types": {
        "libc": {
            "script": "libc_arch.sh",
            "description": "meuos-libc 架构移植与运行时验收"
        },
        "compiler": {
            "script": "compiler_sanity.sh",
            "description": "mcc 编译器冒烟与 C11 门禁"
        },
        "toolchain": {
            "script": "compiler_sanity.sh",
            "description": "meuos-toolchain 工具链验收（复用编译器冒烟）"
        },
        "buildtools": {
            "script": "common_checks.sh",
            "description": "meuos-buildtools 构建工具验收"
        },
        "generic": {
            "script": "common_checks.sh",
            "description": "通用任务，仅运行全局禁止检查"
        }
    },
    "default": {
        "script": "common_checks.sh",
        "description": "未匹配 task_type 时的回退验收"
    }
}
