This directory is shared with all QEMU guests at /mnt/host (9p, read-write).
Drop binaries here (e.g. mcc-compiled + libc-meuos-linked static binaries) and
run them inside a guest:  /mnt/host/<binary>
