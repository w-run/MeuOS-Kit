name: meow-env
version: 1

variables:
  MEOW_RECIPE_VALUE: enabled

default: all

targets:
  all:
    deps: [verify]
  verify:
    commands:
      - test "$MEOW_RECIPE_VALUE" = enabled
  clean:
    commands:
      - test "$MEOW_RECIPE_VALUE" = enabled
