name: meow-auto
version: 1

default: report

targets:
  report:
    deps: [/tmp/meow-auto.input]
    outputs: [/tmp/meow-auto.output]
    commands:
      - printf '$@:$<:$^' > /tmp/meow-auto.output
  clean:
    phony: true
    commands:
      - rm -f /tmp/meow-auto.input /tmp/meow-auto.output
