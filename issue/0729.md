# 0729 — mcc / meuos-libc / meuos-toolchain 待实现清单

> 验证日期：2026-07-29
> 验证方式：逐一读取源码核对，三组件共 ~108 项已验证

---

## 一、mcc 编译器 — projects/mcc

### 1. 活跃后端 FIXME

| 后端 | 文件 | 行 | 问题 |
|------|------|----|------|
| ARM | `arm_emit.c` | 43 | 32-bit float 始终用 `vadd.f64`（应为 `vadd.f32`） |
| ARM | `arm_emit.c` | 97 | 64-bit int→float 未实现（需 `__aeabi`） |
| ARM | `arm_emit.c` | 229 | `die("todo: unhandled ref for memory")` |
| ARM | `arm_emit.c` | 268 | TLS `SThr` 错用 hi12/lo12 pair |
| AArch64 | `aarch64_emit.c` | 70 | 引用 `.todo/aarch64-store-fix.md`（文件不存在） |
| AArch64 | `aarch64_emit.c` | 257 | `die("todo (arm emit): unhandled ref")` |
| AArch64 | `aarch64_emit.c` | 339 | `/* todo, handle large offsets */` |
| AArch64 | `aarch64_abi.c` | 163 | `/* todo, may read out of bounds */` |
| x86_64 | `x86_64_sysv.c` | 344 | `/* todo, may read out of bounds. */` |
| RISC-V | `riscv64_emit.c` | 371 | `die("unimplemented")` — `fixmem()` 未处理 pattern |
| LoongArch64 | `loongarch64_emit.c` | 360 | `die("unimplemented slot copy")` |

### 2. 优化器问题

| 文件 | 行 | 问题 |
|------|----|------|
| `spill.c` | 219 | `bsinit(b2, ntmp); /* todo, free those */` — bitset 不释放 |
| `spill.c` | 300 | `bsinit(u, ntmp); /* todo, free those */` — bitset 不释放 |
| `rega.c` | 561 | 块顺序启发式未验证是否优于简单 postorder |
| `rega.c` | 698,715 | 硬编码临时量阈值 `t < Tmp0` |

### 3. 前端待办（15 项，全部经源码确认）

| 文件 | 行 | 标记 | 问题 |
|------|----|------|------|
| `parse/decl.c` | 74 | XXX | 未维护 linkage 标识符映射 |
| `parse/decl.c` | 153 | XXX | 文件作用域裸 `;` 未报错（违 C11 6.7p2） |
| `parse/decl.c` | 249 | XXX | 缺少函数身份追踪，后续 `extern` 声明无法关联 |
| `parse/declarator.c` | 199 | TODO | 数组指针 qualifier 约束未强制（限函数原型内） |
| `parse/expr.c` | 99 | XXX | 复合赋值表达式复用可能 double-free |
| `parse/expr.c` | 139 | XXX | `adjust()` 应与 `decl.c:adjust` 合并 |
| `parse/expr.c` | 145 | XXX | 数组上的 qualifier 应作用于元素类型 |
| `parse/expr_primary.c` | 213 | TODO | `__builtin_expect` 未检查参数类型 `long` |
| `parse/expr_primary.c` | 220 | TODO | `INFINITY` 硬编码（待 musl math.h 就绪） |
| `parse/expr_primary.c` | 228 | TODO | `NAN` 硬编码（同上） |
| `parse/expr_unary.c` | 120 | XXX | `__alignof__` 在 32-bit 架构上行为不对 |
| `parse/specs.c` | 540 | TODO | `[[attr]]` 与数组声明符 `[` 无法区分 |
| `sema/targ.c` | 98 | TODO | 默认 target 选择硬编码（无 `-target` 时） |
| `sema/init.c` | 19 | TODO | 初始化对象栈固定 32 深度，无动态扩展 |
| `lex/pp.c` | 1173 | XXX | `#include`/`__FILE__` 中逃逸序列未处理 |

### 4. IR 生成中未实现路径（8 条 fatal）

| 文件 | 行 | 消息 |
|------|----|------|
| `irgen/expr.c` | 161 | `fatal("unimplemented declaration kind %d")` |
| `irgen/expr.c` | 202 | `fatal("not a scalar")` — ++/-- on non-scalar |
| `irgen/expr.c` | 256 | `fatal("internal error; unknown unary expression")` |
| `irgen/expr.c` | 358 | `fatal("internal error; unimplemented binary expression")` |
| `irgen/expr.c` | 443-444 | `error(..., "va_arg with non-scalar type is not yet supported")` |
| `irgen/expr.c` | 454 | `fatal("internal error: unimplemented builtin")` |
| `irgen/expr.c` | 469 | `fatal("unimplemented expression %d")` |
| `irgen/funcmem.c` | 63 | `/* TODO: implement alloc32 in IR */` — 对齐 >16 用 add-and-mask hack |

### 5. C11/C23 符合性缺陷（8 项）

来源：`test/community/chibicc/REPORT.md §5.B`

| # | 缺陷 | 现象 |
|---|------|------|
| 1 | C23 `100f` float 后缀 | `"invalid integer constant suffix 'f'"` |
| 2 | UINT64_MAX 字面量类型 | 超 `LLONG_MAX` 回退错误：`"no suitable type for constant"` |
| 3 | 宏重定义（同 token 序列） | 报 `"redefinition of macro"`，C 6.10.3p2 应允许 |
| 4 | `_Atomic int*` → `void*` | `"base types must be compatible"`，应可转换 |
| 5 | `__LINE__` 少 1 | 预处理器值比预期少 1（RUNFAIL） |
| 6 | Common/tentative-definition | `-fcommon` 行为不正确（RUNFAIL） |
| 7 | Unicode 标识符 | `$`/UCN/UTF-8 标识符不支持 |
| 8 | `va_end` builtin 类型检查 | 在宏定义点而非使用点检查类型 |

### 6. ARM 收尾项（4 项）

来源：`src/target/arm/.todo`

1. mt/ld ARM ELF32 重定位支持未完备
2. mcc driver 未通过 `MT_AS`/`MT_LD` 调用 mt 工具链
3. ARM QEMU VM rootfs 未创建
4. i386/arm 跨架构原子操作未验证

### 7. libmcc 共享后端

来源：`.todo/cpp-shared-backend.md`

- Stage A（`libmcc.a` 构建）✅ 完成
- Stage B（`projects/libmcc/` 目录创建）待启动
- Stage C（`src/{ir,opt,abi,emit,target}` 迁移）待启动
- Stage D（`m++` 二进制创建）待启动

---

## 二、meuos-libc — projects/meuos-libc

### 1. 架构受限项

| 架构 | 问题 | 来源 |
|------|------|------|
| **i386** | TLS/signal 端到端未验证（bare_tls 被 mcc i386 TLS 模型选择缺口阻塞） | `PORTING.md:159` |
| **riscv64** | 线程测试在 qemu < 8.x crash（`thread_cpu` 全局变量问题） | `src/arch/riscv64/.todo` |

### 2. 部分实现项

| 模块 | 文件 | 问题 |
|------|------|------|
| IPv6 | `src/socket/inet.c:100,113` | `inet_pton`/`inet_ntop` 拒绝 `AF_INET6` |
| hex float printf | `src/stdio/fmt_out.c:200` | `%a`/`%A` 降级为 `%g`/`%G` |
| long double | `src/stdio/fmt_out.c:134` | `%L` 按 `double` 读，精度丢失 |
| 浮点格式化 | `src/stdio/fp_fmt.c:4` | hex float 未实现 |
| cookie stream | `src/stdio/position.c:33,56` | cookie seek 返回 `ESPIPE` |
| fnmatch | `src/glob/fnmatch.c:67` | `[!]` 空取反类未实现（注释有误导性） |
| 数学库 | `src/stdlib/math.c:103` | **最小实现**（非桩）：Newton sqrt、Taylor 级数 log/exp/sin/cos。缺 asin/acos/atan/atan2/sinh/cosh/tanh/exp2/expm1/log1p/cbrt/hypot，无 IEEE-754 合规保证 |
| errno 字符串 | `src/string/error.c` | 仅 **10** 条定义（EPERM/ENOENT/EINTR/EBADF/EAGAIN/ENOMEM/EACCES/EFAULT/EINVAL/ENOSYS） |
| strftime | `src/time/time.c:237-240` | `%z`/`%Z` 静默无操作 |
| RTLD_NEXT | `src/dlfcn.c:411` | 退化为 `RTLD_DEFAULT` |
| stdio_ext.h | `include/stdio_ext.h` | 大部分函数为 no-op（`_IO_ferror` 为真实实现） |
| cpuid.h | `include/cpuid.h` | 全部为无操作桩 |
| x86intrin.h | `include/x86intrin.h` | 仅类型定义（`__m128i`/`__m128`/`__m128d`/`__v4si`），无 SIMD 内联 |
| complex.h | `include/complex.h` | 无复数数学函数（cabs/csqrt/cexp/csin/ccos/ctan/cpow/clog/carg/cproj 等全缺） |
| netdb 头文件 | `include/` | `htonl`/`ntohl`/`htons`/`ntohs` 未导出到头文件 |

### 3. 缺失的 POSIX/C 标准函数

| 函数 | 标准 | 头文件 | 状态 |
|------|------|--------|------|
| `rand` / `srand` | C89 | `stdlib.h` | ❌ 缺失 |
| `div` / `ldiv` | C89 | `stdlib.h` | ❌ 缺失 |
| `atol` / `atoll` | C89/C99 | `stdlib.h` | ❌ 缺失 |
| `mblen` / `mbtowc` / `wctomb` | C89 | `stdlib.h` | ❌ 缺失 |
| `mbstowcs` / `wcstombs` | C89 | `stdlib.h` | ❌ 缺失 |
| `setenv` / `unsetenv` / `putenv` | POSIX | `stdlib.h` | ❌ 缺失 |
| `clearenv` | POSIX | `stdlib.h` | ❌ 缺失 |
| `posix_memalign` | POSIX | `stdlib.h` | ❌ 缺失 |
| `aligned_alloc` | C11 | `stdlib.h` | ❌ 缺失 |
| `reallocarray` | BSD/glibc | `stdlib.h` | ❌ 缺失 |
| `strsignal` | POSIX | `string.h` | ❌ 缺失 |
| `strncasecmp` | POSIX | `string.h` | ❌ 缺失 |
| `memccpy` / `memmem` | POSIX/glibc | `string.h` | ❌ 缺失 |
| `strchrnul` | glibc | `string.h` | ❌ 缺失 |
| `strsep` | BSD/POSIX | `string.h` | ❌ 缺失 |
| `ffs` | POSIX | `string.h` | ❌ 缺失 |
| `grantpt` / `unlockpt` / `ptsname` | POSIX | `stdlib.h` | ❌ 缺失 |
| `getloadavg` | BSD/POSIX | `stdlib.h` | ❌ 缺失 |
| `getsubopt` | POSIX | `stdlib.h` | ❌ 缺失 |

> ✅ `strtk_r` 和 `strndup` 已在 `string.h` 中声明（原报告误列为缺失）。

### 4. 无测试覆盖的模块

math / time / glob / locale / regex / termios / syslog / wchar / dirent / dlfcn / spawn / ftruncate / mkstemp / complex

---

## 三、meuos-toolchain — projects/meuos-toolchain

### 1. 路线图级缺失（3 大项）

来源：`README.md`、`ARCHITECTURE.md`

| 特性 | 状态 | 说明 |
|------|------|------|
| **ld-shared**（动态链接） | ❌ 未实现 | `-shared`/`-pie`/ld.so/`dlopen`/`dlsym`/`dlclose` |
| **ld-tls-dynamic**（TLS 动态模型） | ❌ 未实现 | GD/LD 代码序列、`__tls_get_addr`、依赖 ld-shared |
| **ld-dwarf**（DWARF 调试信息） | ❌ 未实现 | mcc `-g` DWARF v5、ld 合并 `.debug_*`、strip `--strip-debug`、readelf 解析 |

> ⚠️ `README.md` 路线图有部分过期，仍将 target-i386/aarch64/riscv64/binutils/bootstrap/mcc-mt-integrate 列为进行中，
> 但 `ARCHITECTURE.md` 已标注 ✅ 完成。`ARCHITECTURE.md` 为准。

### 2. i386 目标受限

| 文件 | 行 | 问题 |
|------|----|------|
| `src/target/i386/reloc.c` | 10 | `R_386_GOT32` 明确标记 `(unimplemented)` |
| `src/target/i386/apply.c` | 52-54 | `R_386_GOTPC` 为桩，返回 -1 |
| `src/target/i386/apply.c` | 56-58 | `R_386_PLT32` 退化为 PC32（无真实 PLT 桩） |
| `src/target/i386/encode.c` | 7 | 不支持 REX 前缀、`%r8`-`%r15`、`%rip`-relative |
| `test/i386_e2e.sh` | — | 仅完整校验 `ret` 指令 |

### 3. ARM 目标

- `src/target/arm/` 无 README
- `test/arm_e2e.sh` 不存在
- `Makefile:226`：`--target=arm` 预期失败（但 `src/target/arm/encode.c` 已声明支持 ARMv7）

### 4. LoongArch64 目标

| 文件 | 问题 |
|------|------|
| `src/ld/link.c:2709` | QEMU 7.2 SEGV_ACCERR bug workaround：合并所有 segment 为单一 PT_LOAD |
| `test/loongarch64_e2e.sh:6,40` | QEMU 7.2.0 IL-instruction SIGILL，QEMU 运行时测试不可靠 |

### 5. strip 未实现功能

| 选项 | 文件 | 状态 |
|------|------|------|
| `-K` (keep symbol) | `src/strip/main.c:77,603` | ❌ 未实现 |
| `-N` (strip symbol) | `src/strip/main.c:78,609` | ❌ 未实现 |
| `--strip-unneeded` | `src/strip/main.c:592` | 简化为 `STRIP_DEBUG`（去调试信息） |
| `--keep-symbol`/`--strip-symbol` | `src/strip/main.c:631-633` | ❌ 未实现 |

### 6. objcopy 未实现功能

| 选项 | 文件 | 状态 |
|------|------|------|
| `--rename-section` | `src/objcopy/main.c:982-985` | ❌ 未实现 |
| `--set-section-flags` | `src/objcopy/main.c:988-992` | ❌ 未实现 |
| `--add-section` (for executable) | `src/objcopy/main.c:490-494` | ❌ 不支持可执行文件 |

### 7. assembler 限制

| 限制 | 文件 | 行 |
|------|------|----|
| 多输入文件不支持 | `src/as/main.c` | 78 |
| 嵌套 `.rept` 不支持 | `src/as/assemble.c` | 1337 |
| AVX/VEX 检测为启发式（`mnemonic starts with 'v'`） | `src/as/assemble.c` | 494-500 |
| 无 SSE3/SSSE3/SSE4.x/AVX/AVX2/AVX512 测试 | `test/` | — |

### 8. 动态链接器（rtld）

- 仅 x86_64 有架构特定启动代码（`x86_64/dlstart.S`）
- i386/aarch64/riscv64/loongarch64/arm 全缺

### 9. 测试覆盖缺口

| 测试 | 状态 |
|------|------|
| `-shared` | ❌ 无测试 |
| `-pie` | ❌ 无测试 |
| `--gc-sections` | ❌ 无测试 |
| `-Map`/`--print-map` | ❌ 无测试 |
| `--eh-frame-hdr` | ❌ 无测试 |
| `--build-id` | ❌ 无测试 |
| `--version-script` | ❌ 无测试 |
| `--link-script` (`-T`) | ❌ 无测试 |
| ARM target | ❌ 无 e2e 测试 |
| DWARF `.debug_line`/`.eh_frame` 汇编输出 | ❌ 无测试 |

---

## 四、汇总优先级

| 优先级 | 组件 | 项数 | 影响 |
|--------|------|------|------|
| **高** | mcc C11/C23 符合性 | 8 项 | 标准合规阻碍 |
| **高** | toolchain 动态链接 | 3 大项 | Phase 6 阻塞项 |
| **高** | meuos-libc 缺失 POSIX 函数 | 19+ 项 | POSIX 兼容性 |
| **中** | mcc 后端 FIXME | 11 项 | 代码健壮性与功能完备 |
| **中** | meuos-libc 测试覆盖 | 14 模块 | 质量保障 |
| **中** | mcc 前端待办 | 15 项 | 约束检查与正确性 |
| **中** | toolchain i386/ARM 限制 | 8 项 | 多架构完备性 |
| **低** | libmcc 共享后端 | 3 阶段 | m++ 前置条件 |
| **低** | ARM 收尾 | 4 项 | 端到端验证 |

## 五、本次验证记录

- **mcc**：65 项 → 63 ✅ 确认，1 ⚠️ 部分，0 ❌ 不符
- **meuos-libc**：14 项 → 10 ✅ 确认，3 ⚠️ 部分，1 ⚠️ 数量修正（9→10）
- **meuos-toolchain**：24 项 → 20 ✅ 确认，2 ⚠️ 部分，2 🔍 文件未找到
- **总计**：~103 项，96% 准确率，4 项有数量/描述偏差，均已修正

---

## 六、深度代码审查 — 实际缺陷（2026-07-29）

> 审查方式：逐行读取源码，重点查找**内存安全、逻辑错误、未定义行为、资源泄漏、安全漏洞**。
> 共发现 **102 项缺陷**（33 CRITICAL / 37 MAJOR / 32 MINOR）。

### 6.1 mcc 编译器（25 项）

#### CRITICAL（6 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 1 | `sema/type.c:190` | `typerank()` | 运算符优先级：`t->size << 3 + 4 | 0x1` 被解析为 `(t->size << (3+4)) | 1`，表达式意图不清晰 | ✅ R7 已修复 |
| 2 | `sema/type.c:479` | `typehasint()` | 未定义行为：`-1ull << (t->size << 3) - 1` 中 `t->size==8` 时移 64 位（超过类型宽度）。应加括号为 `-1ull << ((t->size << 3) - 1)` |
| 3 | `irgen/funcmem.c:58-66` | `funcalloc()` | 对齐 > 16 时重复填充：default 分支加 `align-16`，后文又加一次 `align-16`，总填充为 `2*(align-16)` |
| 4 | `irgen/emit.c:188-218` | TLS 模型 | 非 PIC + extern TLS：代码发 SExt|SThr（IE），但文档要求 SGenThr（GD），映射颠倒 |
| 5 | `opt/simpl.c:55` | `ulog2()` | 非 2 的幂次输入导致数组越界读。函数无断言守卫 |
| 6 | `opt/rega.c:748-749` | `ps` 数组 | 复合字面量数组越界迭代：`(Blk**[3]){&b->s1, &b->s2, (Blk*[1]){0}}` 中第3个元素是 1 元素数组，`ps++` 后读越界 |

#### MAJOR（9 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 7 | `sema/init.c:46-63` | `initadd()` | 链表插入后 `init` 指针未更新，可能产生悬垂 `p->last` 指针 |
| 8 | `opt/gvn.c:93` | `adduse()` | 变参函数调用 `adduse(t2, UJmp, b)` 缺少变参参数，UB |
| 9 | `ir_util.c:466` | `newtmp()` | 静态 `n` 不会跨函数重置，临时名称无限增长 + 符号表内存泄漏 |
| 10 | `opt/fold.c:171,190` | 浮点零除 | 常量折叠遇到除零产生 NaN，后续 `foldint()` 中 NaN 比较非确定 |
| 11 | `irgen/expr.c:40` | `atomicrmw` | `static struct decl callee[6]` 中 `type` 字段未初始化 |
| 12 | `driver/main.c:257` | i386 march | i386 march 值被赋给 `arm_march` 变量，易混淆且脆弱 |
| 13 | `irgen/emit.c:729` | `escape_string()` | 堆内存泄漏：`array` 缓冲区创建后从未释放 |
| 14 | `ir_util.c:450` | `phiargn()` | 返回 `-1` 作为 `unsigned int`（=UINT_MAX），调用者须用 `-1u` 判断 |
| 15 | `sema/eval.c:22` | 宽度掩码 | `1ull << t->u.arith.width - 1` 在 `width=0` 时为 UB |

#### MINOR（10 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 16 | `sema/type.c:480` | `typehasint()` | 右移表达式运算符优先级混乱，可读性极差 |
| 17 | `irgen/expr.c:150` | `calcvla()` | 对每个表达式都调用 `calcvla()`，含 void/标量类型（函数调用开销） |
| 18 | `irgen/irgen.h:119-123` | `ptrclass()` | 硬编码 `typelongs` 与指针尺寸一致，概念上脆弱 |
| 19 | `sema/init.c:231-233` | 断言 | 标量花括号嵌套触发 `assert`，若 `error()` 改为非致命则崩溃 |
| 20 | `irgen/emit.c:735-748` | `emitintegerdata()` | size > 8 时 `die()` 退出而非优雅报错 |
| 21 | `opt/simpl.c:24` | 目标比较 | 所有 `Oblit1` 指令上都 `strcmp(T.name, "i386")`，可用布尔标志替代 |
| 22 | `opt/rega.c:5-6` | `assert` 宏 | TEST_PMOV 模式下 `assert(x)` 双重求值 |
| 23 | `opt/live.c:76-77` | 带副作用断言 | `bscount()` 的计数被 assert 消去，NDEBUG 下丢失 |
| 24 | `sema/type.c:478` | 符号/无符号比较 | `i >= -1ull << 63` 行为正确但编码风格危险 |
| 25 | `irgen/expr.c:40` | 全局 decl 数组 | `atomicrmw` 6 个静态 decl 副本可被后续函数引用 |

### 6.2 meuos-libc（22 项）

#### CRITICAL（6 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 1 | `stdio/fmt_out.c:274` | `sprintf()` | 用 `(size_t)-1` 为缓冲区大小，`pos` 达 `SIZE_MAX` 时写入任意地址 |
| 2 | `netdb/getaddrinfo.c:216-219` | 缺少花括号 | `else if (!node)` 后缺少 `{}`，`else` 悬空绑定，`inet_pton(AF_INET6, NULL, ...)` 崩溃 |
| 3 | `stdio/char_io.c:157` | `fflush(NULL)` | 空操作实现，`exit()` 前缓冲输出丢失 |
| 4 | `stdio/exit.c:31` | exit 不刷新 | 同上 |
| 5 | `stdlib/malloc.c:128-136` | `realloc()` | TOCTOU 竞态：释放锁后其他线程可 `free(pointer)`，双重释放 |
| 6 | `stdlib/convert.c:163-165` | `abs(INT_MIN)` | 有符号溢出（UB） |

#### MAJOR（9 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 7 | `dlfcn.c:242-253` | mprotect 禁用 | `#if 0` 块禁用 LOAD 段内存保护设置，.text 段保持读写权限 |
| 8 | `glob/fnmatch.c:15-17` | FNM_PERIOD | 逻辑错误：`*` 通配符模式不检查点文件（`.hidden`） |
| 9 | `string/token.c:3` | strtok | 全局 `token_state` 非线程安全 |
| 10 | `netdb/getaddrinfo.c:85-91` | 返回悬垂指针 | `__resolve_host()` 返回指向栈变量 `addr4` 的指针 |
| 11 | `stdio/position.c:63-64` | ftell 下溢 | `ungetc` + `ftell` 在文件开头导致 `pos=-1`，返回负值且未设 errno |
| 12 | `stdio/fmt_in.c:60-73` | scanf %d | `-(int)value` 在 `value > INT_MAX` 时 UB |
| 13 | `stdio/fp_fmt.c:58-63` | sign_bit | 假设小端 + 8 字节 double，不可移植；违反严格别名规则 |
| 14 | `stdio/file_misc.c:44-47` | tmpfile | `fdopen` 失败时 fd 泄漏（文件已 unlink 无法清理） |
| 15 | `stdlib/process.c:10` | system() | `environ` 若为 NULL 则 `execve()` 崩溃 |

#### MINOR（7 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 16 | `string/error.c:3-14` | errno 表 | 仅 10/130 个 POSIX errno 有字符串 |
| 17 | `dirent/dirent.c:64-65` | strncpy | `strncpy` 计数满时不加 NUL（已用显式终止修复） |
| 18 | `stdlib/malloc.c:45-47` | aligned_size | SIZE_MAX-14 ~ -1 范围内返回 0（可更优雅） |
| 19 | `stdio/memory_stream.c:99` | funopen | 模式推断不支持读写流 |
| 20 | `locale/locale.c:19` | setlocale | 仅检查 `LANG`，不检查 `LC_ALL`/`LC_CTYPE` 等 |
| 21 | `signal/signal.c:209` | sigfillset | 假设 `sigset_t` 正好等于 `unsigned long` |
| 22 | `ctype/ctype.c:8,16` | iscntrl/ispunct | 负 `char` 值行为不安全 |

### 6.3 meow 构建系统（17 项）

#### CRITICAL（6 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 1 | `parse.c:240-264` | Shell 引号转义 | 值中的 `'` 转义错误，`a'b` 生成 `'a\'b'` 而非 `'a'\''b'` |
| 2 | `exec.c:101,106` | realloc NULL | 输出捕获循环中 `realloc()` 不检查 NULL，OOM 时 segfault |
| 3 | `graph.c:391-393` | setvbuf 目标 | 复制粘贴错误：`setvbuf(stdout,...)` 而非 `setvbuf(lf,...)` |
| 4 | `main.c:214-258` | 内存泄漏 | 首个 `malloc(RECIPE_MAX)` 被覆盖，从未释放 |
| 5 | `parse.c:138` | expand_files() | 固定 4KB 缓冲区，超长 glob 结果静默截断 |
| 6 | `parse_meow.c:236-271` | YAML probe | 无冒号的 YAML list 项被静默忽略 |

#### MAJOR（5 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 7 | `probe.c:56-60` | 命令注入 | `system()` 使用 recipe 控制的 `cc`/`cflags` |
| 8 | `probe.c:60,89` | system() | 使用 `system()` 而非 `fork/exec`，环境未隔离 |
| 9 | `graph.c:24-32` | expand_path() | 单静态缓冲区，重入 bug |
| 10 | `parse.c:239` | 缓冲区守卫 | `'` 扩展到 4x 而非 2x，低估了缓冲区需求 |
| 11 | `graph.c:498-565` | Y2038 | 32-bit 平台使用 `long` 时间戳 |

#### MINOR（6 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 12 | `parse.c:342-357` | 缺少花括号 | `depends:`/`uses:`/`default:` 缩进层级解析错误 |
| 13 | `exec.c:101` | out_cap 溢出 | `out_cap *= 2` 无溢出守卫 |
| 14 | `main.c:99` | --arch 消耗子命令 | `--arch` 标志吞掉子命令参数 |
| 15 | `probe.c:166` | strcpy | 使用 `strcpy` 而非 `snprintf` |
| 16 | `lint.c:53-79` | 全局状态 | `cflags_global`/`ldflags_global` 在 lint 迭代间不重置 |
| 17 | `graph.c:393` | _IONBF | 在日志文件上全局设 `_IONBF` 性能影响 |

### 6.4 meuos-toolchain（21 项）

#### CRITICAL（7 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 1 | `src/libelf/elf32.c:73` | 段表校验 | 误读 `e_flags`（36）而非 `e_shoff`（32），所有 ELF32 解析出错 | ✅ R1 已修复 |
| 2 | `src/ld/link.c:2682-2707` | ELF32 Header | 始终写 64 字节 ELF64 格式 header，i386 ELF32 输出完全损坏 | ✅ R2 已修复 |
| 3 | `src/libelf/writer32.c:122-128` | OOM segfault | `calloc` 返回 NULL 在先，错误检查在后，确定性 segfault | ✅ R3 已修复 |
| 4 | `src/ld/link.c:1190-1191` | ELF32 共享库 | 始终用 `mt_elf64_get_symbol` 读 ELF32 符号，越界读取 | ✅ R6 已修复 |
| 5 | `src/libelf/elf.c:199` | 符号检测 | 用 `==` 而非 `>=` 判断 ELF32 符号大小，padding 时损坏数据 | ✅ R4 已修复 |
| 6 | `include/mt/elf.h:112-113` | 重复宏 | `MT_PT_GNU_RELRO` 定义两次 |
| 7 | `src/ld/link.c:2560-2583` | NULL 组名 | `strings_add()` 中 `strlen(NULL)` segfault |

#### MAJOR（8 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 8 | `link.c:497,524` | 零除 | `s32.size / s32.entry_size` 未检查 `entry_size > 0` |
| 9 | `link.c:2099-2114` | ELF32 重定位 | i386 路径使用 x86_64 relocation 类型号 |
| 10 | `strip/main.c:413` | 仅支持 ELF64 | `strip` 用 ELF64 结构处理 ELF32，越界读取 | ✅ R5 已修复 |
| 11 | `link.c:2602-2615/2822-2837` | build-id 重复计算 | 相同 FNV-1a 哈希计算两次，间隔 200 行 |
| 12 | `link.c:1768-1773` | TBSS file offset | NOBITS 段不应推进文件偏移，产生文件空洞 |
| 13 | `elf.h:261-263 / elf32.h:85-87` | 宏重复定义 | `MT_ELF64_ST_BIND`/`ST_TYPE` 定义在两处 |
| 14 | `link.c:1920-1925` | i386 TLSLD/TLSGD | 缺少架构专属重定位分发 |
| 15 | `link.c:171` | 错误信息缓冲区 | `strncpy` 截断时不加 NUL（已手动加）|

#### MINOR（6 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 16 | `writer32.c:163-165` | 重复写入 | `e_flags`/`e_ehsize` 写两次 |
| 17 | `link.c:1598` | 硬编码条目大小 | `n * 12` 而非 `n * section.entry_size` |
| 18 | `link.c:2831-2836` | O(n²) hash | 大段逐字节哈希极慢 |
| 19 | `link.c:1667` | atoi | `atoi()` 在溢出时为 UB |
| 20 | `as/assemble.c:573` | \\0 转义 | 技术上正确但文档混淆 |
| 21 | `as/assemble.c:52-63` | mt_malloc(0) | 模式不一致：`size==0?1:size` 分散使用 |

### 6.5 meuos-sysroot（17 项）

#### CRITICAL（8 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 1 | `libmsys/msys.c:569` | SHA-256 验证 | 压缩文件：哈希校验用压缩数据计算，但索引存储的是未压缩哈希——总是失败 |
| 2 | `libmsys/msys.c:468` | msys_readlink | `bufsize=0` 时 `bufsize-1` 回绕到 SIZE_MAX，无限长度 `memcpy` |
| 3 | `libmsys/overlay.c:246-254` | realloc 错误 | 三重 realloc 错误处理导致 double-free：部分成功时释放错误指针 |
| 4 | `msysctl/main.c:308-314` | 命令注入 | `system("diff -u ...")` 参数来自用户输入，未经清理 |
| 5 | `libmsys/msys.c:1014` | 空文件加载 | 压缩档案中空文件触发解压器返回 NULL，`msys_load`/`msys_fopen` 全部失败 |
| 6 | `libmsys/msys.c:587` | v1 名字截断 | `name_len` 用 `uint8_t` 截断，>255 字节名字的 v1 条目静默跳过 |
| 7 | `libmsys/msys.c:682` | 签名校验下溢 | `avail` 在 `nlen>0` 时回绕，后续越界读 mmap |
| 8 | `libmsys/stream.c:88-89` | 链表泄漏 | `free(m->chunks)` 仅释放头节点，所有解压缓冲泄漏 |

#### MAJOR（6 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 9 | `libmsys/msys.c:236-292` | union 非活跃成员 | `m->hdr` 和 `m->hdr_v2` 通过非活跃 union 成员读取（UB）|
| 10 | `msysctl/main.c:206,216` | 双重 time() | `.msg` 文件和时间戳文件可能因时钟跳动不匹配 |
| 11 | `mkmsys/main.c:619-631` | 路径遍历 | `extract_msys` 不清理 entry 名字中的 `../`，任意文件写 |
| 12 | `mkmsys/main.c:298-301/717-719` | v1/v2 不一致 | v2 writer 压缩降级直接 exit(1)，v1 优雅回退 |
| 13 | `libmsys/ed25519.c:63-66` | 密钥全零 | `libsodium` 不可用时静默返回全零密钥，签名不可区分 |
| 14 | `libmsys/ed25519.c:57` | sodium_init | 丢弃 `sodium_init()` 返回值，初始化失败无告警 |

#### MINOR（3 项）

| # | 文件 | 行 | 描述 |
|---|------|----|------|
| 15 | `msysctl/main.c:644` | 未检查 write() | `write(fd, data, dsize)` 返回值未检查 |
| 16 | `msysctl/main.c:206,216` | time() 双重求值 | 低概率时钟跳动导致不一致 |
| 17 | `mkmsys/main.c:408-410` | zstd 空操作 | 错误处理块中只有注释，无实际代码 |

### 6.6 本次代码审查汇总

| 组件 | CRITICAL | MAJOR | MINOR | 总计 |
|------|----------|-------|-------|------|
| mcc | 6 | 9 | 10 | 25 |
| meuos-libc | 6 | 9 | 7 | 22 |
| meow | 6 | 5 | 6 | 17 |
| meuos-toolchain | 7 | 8 | 6 | 21 |
| meuos-sysroot | 8 | 6 | 3 | 17 |
| **合计** | **33** | **37** | **32** | **102** |

#### 最优先修复项（跨组件 Top 12）

| 优先级 | 组件 | 缺陷 | 影响 |
|--------|------|------|------|
| P0 | toolchain | ~~elf32.c:73 — 段偏移读错 e_flags~~ | ✅ R1 已修复 |
| P0 | toolchain | ~~link.c:2682 — ELF32 Header 64B 写~~ | ✅ R2 已修复 |
| P0 | mcc | type.c:190 — 类型排名运算符优先级 | long 类型提升逻辑完全错误 |
| P0 | libc | fmt_out.c:274 — sprintf SIZE_MAX 溢出 | 任意地址写入（潜在 RCE） |
| P0 | libc | getaddrinfo.c:216 — NULL 指针解引用 | 无条件 crash |
| P0 | sysroot | msys.c:569 — SHA-256 哈希压缩 vs 未压缩 | 压缩档案验证总是失败 |
| P0 | sysroot | overlay.c:246 — realloc double-free | ENOMEM 时堆损坏 |
| P1 | mcc | type.c:479 — 64-bit 移位 UB | `typehasint()` 对 long 类型行为错误 |
| P1 | libc | malloc.c:128 — realloc TOCTOU | 多线程双重释放 |
| P1 | meow | parse.c:240 — Shell 引号转义 | recipe 构建可能失败 |
| P1 | sysroot | msys.c:468 — readlink bufsize=0 溢出 | 确定性缓冲区溢出 |
| P1 | sysroot | msysctl — diff 命令注入 | 任意命令执行（CVSS 9.8） |
