/* msh/var.h — 变量管理接口（骨架 stub） */
#ifndef MEUOS_MSH_VAR_H
#define MEUOS_MSH_VAR_H

#include "msh/msh.h"

/* 骨架阶段：仅占位，留作 P6 阶段实现：
 *   - getenv/setenv 简单转发
 *   - 局部变量
 *   - ${VAR} 展开
 *   - 命令替换 $(...)
 *   - 算术展开 $((...))
 */

#endif /* MEUOS_MSH_VAR_H */
