/* x86_64 target descriptor -- provides the canonical x86_64 target. */
#include "mt/target.h"

/* The x86_64 target instance lives in src/target/target.c (the registry).
 * This file is a placeholder for per-target init or validation logic
 * that may be added when the sub-target is split out.  For now the
 * singleton in the registry is sufficient. */
