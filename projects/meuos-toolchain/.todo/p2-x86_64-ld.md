# P2：x86_64 静态链接器（核心完成）

## 输入

- mt/as 生成的 ET_REL；
- mt/ar 归档及其 symbol index；
- meuos-libc 的 `crt1.o`、`libc-meuos.a`、`libatomic-meuos.a`。

## 交付

- 符号解析和归档按需抽取；
- 节区布局、PT_LOAD、入口点；
- x86_64 首期 relocation；
- `--sysroot/-L/-l/--static/-o`。

## 已完成门禁

- `make -C projects/meuos-toolchain check-ld-x86_64`；
- mt as + mt ar + mt ld 的 syscall smoke 在 x86_64 宿主运行；
- `.text/.rodata/.got/.data/.bss`、归档成员、GOT/PLT、PC-relative 和绝对 relocation；
- 未定义符号诊断；
- 不调用宿主 `ld`。

## 后续扩展

- MeuOS libc/crt1/sysroot 静态链接；
- 更完整的 weak/common/归档按需抽取；
- overflow 诊断、符号表输出、动态链接和多架构。
