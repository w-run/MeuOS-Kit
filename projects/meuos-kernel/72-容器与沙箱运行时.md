# 72 - 容器与沙箱运行时（Container & Sandbox Runtime）

> 子领域：容器与沙箱运行时 / Container & Sandbox Runtime
> 团队：`kernel-plan`（第八轮拓展维度调研员，lite / hy3 产出）。
> 关联文档：`00-总览与路线图`、`04-进程与调度`（Job 账本 §4.4、结构化取消 §4.6、监督 §4.5）、`06-文件系统`（统一命名空间 / C3 每进程 namespace、`.msys` 原子更新）、`07-安全模型`（capability/rights、受约束 mint §4.5、派生收窄）、`27-电源与热管理`、`40-电源管理与能耗`、`46-进程管理-方案A`、`47-进程管理-方案B`、`53-用户系统-方案B`（§8.3/§8.4 合成 uid 投影）、`61-内核自举与早期启动深化`（C61-1 `.mimg`/` .msys` 唯一载体）、`63-网络栈实现深化`（`.net` 子树 reveal）。
>
> **百思纪律（铁律）**：本文只写设计/规划文档，不写实现代码；结论可修订式追加。本文建立在以下已决地基之上——capability Day 1 唯一权限模型（`07`）、Job 资源账本（`04` §4.4）、每进程 namespace = capability 组合树（`06` C3）、`.mimg`/`.msys` 为五架构唯一载体（C61-1，取代裸 ELF/multiboot）、多 realm 边界默认 deny（`00` 第八轮自主采纳 / `56` §7.3 冲突8）；**不引入新内核原语**，**不新增 right bit**（rights 位图 bit0–47 冻结、`07` §4.1，bit48+ 预留区首冻结 bit49–55，`00` 第八轮）；**五架构（x86_64/i386/aarch64/riscv64/loongarch64）容器/sandbox 逻辑同源**，仅启动载体（`.mimg` 五架构入口段）与 NIC 驱动层差异（承接 `63` §7）。
>
> 方法论铁律贯穿：解构型思维、多系统参照（OCI runc / systemd-nspawn / gVisor / Firecracker / FreeBSD jail / Linux userns）、非缝合怪、修订式追加不删。
>
> 本文为规划草案，后续以修订式追加更新，不覆盖、不删改 `00`–`71`、README、`.issues/`、git。跨域项以 `00` 总览 `A1`–`A12` / `C1`–`C18` 为准；与 `04`/`06`/`07`/`46`/`47`/`53`/`61` 严格一致。

---

## 0. 核心结论（一句话）

> **容器 = 一个 Job 子树 + 组合 namespace 视图（由 `06` C3 每进程 namespace 与 `04` Job 账本组合而成）；sandbox 默认 deny，capability 委托范围显式 grant，与多 realm 边界（`00` 第八轮 / `56` §7.3 冲突8，下文统称「A7 多 realm」二级标签，与 `00 §2` 地基 A7=VMO/VMAR 零拷贝**无关**）同构；OCI-ish 镜像/manifest 投影为 `.mimg`/`.msys` 载体（C61-1 唯一载体）；并与 `06` C3、`04`、`07`、`46`/`47`、`53` 用户系统衔接——全程复用既有内核原语（Job / namespace / capability / VMO-pager），不引入新原语、不新增 right bit。**

---

## 1. 容器模型（Job 子树 + namespace 组合视图）

### 1.1 先推演「容器应该是什么」

从内核哲学出发——MeuOS 已有 Job（资源与生命周期单元，`04` §4.1/§4.4）与每进程 namespace（capability 组合树，`06` C3）。那么「容器」在 MeuOS 里不应是一个新原语，而应是**既有两件事的组合闭包**：

- **资源与生命周期边界** = 一棵 Job 子树（`04` §4.1）。容器 init 进程挂在某个 Job 下；其派生的所有进程/子 Job 自然归入该子树；父 Job 终止 → 传播终止整棵容器树（lifetime control，对齐 Zircon）。
- **视图边界** = 一组 namespace 绑定（每个进程在 spawn 时 COW 继承 Job 的 namespace 模板，`06` C3 采纳结论）。容器即「一组进程共享同一份 namespace 模板 + 各自可在模板内 `unveil` 裁剪」——没有绑定的路径对容器内进程「根本不存在」（默认全不可见、显式 reveal，对齐 `06` §4.1）。

二者合一：**容器 = 一个 Job 子树 + 附着于该 Job 的 namespace 模板**。

### 1.2 MeuOS 容器模型（自主收敛）

```
┌─────────────────────────────────────────────┐
│  Job（容器根 Job，即容器身份）                 │
│  - 资源配额 (cpu/mem/handles/io/pid)  §4.4    │
│  - namespace 模板（容器共享视图，C3 COW）      │
│  - capability 委托范围（受约束 mint §4.5）     │
│  - 监督策略（容器 init 崩溃→重启/终止传播）    │
└───────────────┬─────────────────────────────┘
                │ 容器 init 作为首个进程（持有本 Job handle）
┌───────────────▼─────────────────────────────┐
│  Process: container-init (pid 1 角色)         │
│  - 命名空间视图 = Job 模板 COW 继承            │
│  - 后续 spawn 在「本 Job 子树」内，视图同源    │
└───────────────┬─────────────────────────────┘
                │
         ┌──────▼──────┐   ┌──────▼──────┐
         │ 子 Process   │   │ 子 Job      │  ... 容器内部层级
         └─────────────┘   └─────────────┘
```

- **与 `04 §4.1` 衔接**：容器不是「特殊进程」，而是「被 `meu-supervisor` 在指定 Job 下 spawn 的普通进程树」，其 Job handle 决定配额与能力边界（除非显式收窄）。这与 `04` 原生 spawn 模型完全一致——**容器只是 spawn 的一个结构化用法**，而非新一等公民。
- **与 `04 §4.4` 衔接**：容器配额 = Job 账本配额（CPU 权重+可选硬上限 / 内存上限 / 句柄数 / IO 带宽 / pid 数）。限额是 Job 的固有属性，进程诞生即受限，无法「先越权再被关进笼子」（超越 cgroups 事后挂接）。
- **与 `06` C3 衔接**：容器的「文件系统隔离」= 该 Job 的 namespace 模板中 `{ "/": root, "/dev": dev_svc, "/proc": proc_svc, "/net": net_svc, "/msys": sysroot }` 绑定集；容器内部进程 spawn 时 COW 继承并可在自己的命名空间内 `unveil` 裁剪，互不影响（对齐 `06` §4.1 + C3 采纳结论「spawn COW 继承父/Job 模板」）。
- **与 `04 §4.5` 衔接（监督）**：容器 init 由 `meu-supervisor` 拉起并持有其 Job handle；容器 init 崩溃 → 经 exception channel（`00` A9）上行监督者，按策略重启/终止传播。容器自带「监督内监督」层级（`04` §4.5 监督树与 Job 树重合），无需外部 `systemd`/`containerd`。

> 与参照系统的差异：比 Linux「cgroup + namespace + pivot_root 三件套」更统一——MeuOS 把三者收敛为「Job 子树 + namespace 模板」两个本已存在的原语；比 FreeBSD jail 多了 capability 受约束 mint 的细粒度委托；比 gVisor/Firecracker 更轻——sandbox 边界是 capability 而非独立内核/VM（见 §2）。

---

## 2. sandbox 默认 deny 与 capability 委托（显式 grant）

### 2.1 默认 deny 的落点

MeuOS 的 sandbox 默认 deny 不是「加一层 seccomp 过滤器」，**而是 capability 模型的天然推论**（`00` A1/A2）：进程只持有 spawn 时显式传入/派生的句柄与 rights，没有隐含权限。因此「容器 sandbox 默认 deny」= 容器 init 的 Job 模板只 reveal 最小命名空间绑定、容器子进程只继承收窄后的 capability 集（对齐 `04` §4.2 G4「默认不继承任何句柄」+ `08` 兼容层默认不绕过 pledge）。

- **无「root 逃逸」面**：真实权限源是 capability（`53` 内核模型：无 root、无 uid 权限附着），`setuid` 二进制 = 受限 spawn + 显式 capability（`04` C4 采纳），**永不提权**。容器内「看起来是 root（uid 0）」只是 `53` 合成 uid 视图（`00` C4 / `53` §7.1.4），零权限附着，不突破任何内核不变量。
- **sandbox 收缩 = 派生收窄**：容器子进程 spawn 时，其 capability 集 = 父 Job 能力边界 ∩ 显式收窄声明（`07` §4.5 派生收窄），只能更窄、不可越类放大（与 `06` C2 受约束 mint 同源，破 confused deputy）。

### 2.2 capability 委托范围：显式 grant，与多 realm 边界同构

容器对外（跨 Job、跨 realm、跨命名空间）的任何能力授予，必须**显式 grant**，且遵循 `07` §4.5 受约束 mint（委托人 rights 上限 ∩ 客户端请求下限取交集）。这恰与**多 realm 边界**（`00` 第八轮自主采纳 / `56` §7.3 冲突8）同构：

| 多 realm 边界（已采纳） | 容器 sandbox 委托（本文同构复用） |
|--------------------------|------------------------------------|
| 跨 realm 取消/能力/命名传播**默认 deny、不穿透** | 跨容器能力/命名/job 传播默认 deny、不穿透 |
| realm = namespace 之上的隔离域，capability 边界即 realm 边界 | 容器 = 一个 realm 实例，Job 边界即容器边界 |
| 仅**显式 grant** 跨域，不引入新统一跨 realm 层 | 仅显式 grant 跨容器共享（如 volume/共享 socket/跨 realm 服务），不引入新跨容器层 |
| 不引入新内核原语 | 不引入新内核原语（复用 Job/namespace/capability） |

> **编号注意**：本 § 与 `53 §7.5`、`46 §7.7`、`47 §7.8` 引用的「A7 多 realm」是 `56`/`62` 的二级标签，与 `00 §2` 地基 **A7=VMO/VMAR+pager 零拷贝** 无关——切勿覆盖 A7 地基。容器即该「多 realm 边界」的一个标准实例，而非新概念。

### 2.3 跨容器共享的投影语义

- **共享卷（volume）** = 两个 Job 的 namespace 模板各自 reveal 同一个 `.msys` 子树上的目录能力（`06` mount-capability 委托 / C3 同构），经 cap-transfer 显式 grant，受 `READ`/`WRITE` rights 约束。
- **共享 socket / 服务** = 经 `05` Channel 端点或 socket 句柄的 cap-transfer（`63` §3.1），跨 Job 授予需发送方持 `GRANT` right（`07` §4.5）。
- **网络命名空间视图** = 容器 `/net` 子树 reveal（`06` C3 / `63` N7），由 netstack 服务的每接口/每子网视图提供，容器只看见被授予的子树。
- **以上全部 fail-closed**：未显式 grant 即不可达（对齐 `53 §7.5` realm 默认 deny），映射错误不突破内核不变量。

---

## 3. OCI-ish 兼容投影（镜像/manifest → `.mimg`/`.msys`）

### 3.1 唯一载体纪律（C61-1）

`00` 第八轮采纳 **C61-1**：`.mimg` 为五架构唯一内核启动载体，`.msys` 为 sysroot/rootfs 唯一载体（取代裸 ELF/multiboot/`06` C-G5）。**容器镜像不得另创容器专属格式**——OCI-ish 镜像/manifest 必须投影为既有 `.mimg`/`.msys` 载体，与系统自举/回滚共用同一条信任链（`32`/`61` §3、§5）。

### 3.2 OCI 构件 → MeuOS 载体映射

| OCI 构件 | MeuOS 投影 | 载体 / 落点 | 说明 |
|----------|------------|-------------|------|
| OCI Manifest（JSON，索引 layers/config） | 容器 manifest = **`.msys` 内容寻址 head Ref + 签名**（`06` §4.3 / C-G5 A 子集） | `.msys` 子树 Ref + ed25519 签名（`61` §3.2 双锚同构） | 镜像指纹 = `.msys` 内容哈希，天然防篡改（`06` C-G5 / `54`） |
| OCI Image Config（args/env/workingDir/entrypoint） | **Job manifest / spawn 输入**（`04` §4.2） | supervisor 声明式服务描述（`04` §4.5） | 进程参数/环境 = spawn argv/env；capability 集 = 容器 Job 能力边界 |
| OCI Layer（tar diff，有序叠加） | **`.msys` 可写子卷的 CoW 层**（`06` §4.3 暂存克隆 + B 主线 extent CoW） | `.msys` 命名空间子树 | 层叠加 = CoW 克隆链，原子 repoint 切层（`06` §4.3 非破坏式） |
| OCI Layer 内容（文件） | 统一命名空间节点 + 句柄 rights（`06` §4.1/§4.2） | MeuOS-FS 服务 | 校验和/压缩/加密在 extent 层、密钥=capability（`06` C2） |
| OCI runtime 启动（runc create/start） | `meu-supervisor` 在容器 Job 下 spawn（`04` §4.5） | supervisor + Job | 不引入 OCI runtime 内核 ABI，仅兼容层翻译 |
| rootfs / pivot_root | `/msys` + `/` 命名空间 repoint（`06` §4.3） | namespace 模板绑定 | 非破坏式，旧持有者续持旧能力（`06` C-G5） |

- **信任链对齐**：容器镜像验签复用 `.msys` 的 ed25519 包签名 + extent Merkle 根（`61` §3.3 / `06` §4.2），与 `.mimg` 双锚对称——容器与系统共用 `32` 信任链，无独立镜像信任根。
- **五架构同源**：`.mimg`/` .msys` 内部承载五架构内核/库；容器镜像**一次打包、五架构同载**（`61` §3.2 Item 0 内含五架构 `arch/<arch>/boot` 入口段），容器运行时无需 per-arch 镜像。

### 3.3 uid 映射投影边界（与 `53 §8.3`/`§8.4` 协同）

- **容器内部 uid 视图** = `53` 合成 uid 兼容投影（`00` C4 / `53` §7.1.4）：容器内「uid 1000」只是合成视图标签，**真实隔离始终由 capability 兜底**（对齐 `53 §8.3`）。
- **「容器内 uid ↔ 宿主」映射** 在 MeuOS 不是内核 userns，而是「容器内部合成 uid 视图 ↔ 真实 capability 作用域」的兼容投影；映射错误不应突破内核不变量（`53 §8.3` 待原型验证项，`C4`/`D6` 协同）。
- **`access()` 模拟返回值**（`53 §8.4`）：兼容层据进程 capability 集合模拟返回值，何时真/何时 `EPERM` 由 `53 §8.4` 定稿，不引入内核新语义。
- **边界纪律**：任何「uid 映射即越权」的反模式被 capability 兜底否决（对齐 `53 §8.3` 实质成本之五）；合成 uid 不携带任何权限（与 `00` C4 / `53` 内核模型严格一致）。

---

## 4. 资源隔离粒度（CPU/内存/IO/pid 以 Job 为单元）

### 4.1 隔离单元 = Job（与 `04 §4.4` 同构）

容器资源隔离**完全复用** Job 账本（`04` §4.4），无独立 cgroup 式机制：

| 资源 | 计量方式（Job 账本） | 容器语义 | 超限行为 |
|------|----------------------|----------|----------|
| CPU 时间 | 公平权重 + 可选硬上限（带宽） | 容器 CPU 配额 = Job 权重 | 降权重/限流，不杀进程 |
| 内存 | Job 级内存上限（VMAR 映射 + 内核对象） | 容器内存上限 | Job 内选择性 OOM/通知监督者（`04` §5 议题5） |
| 句柄数 | 句柄表上限 | 容器句柄上限 | 拒绝新分配 |
| IO 带宽 | 按设备/层配额（原型期可后置 `05`/`10`） | 容器 IO 配额 | 限流 |
| pid/线程数 | pids/threads 上限 | 容器进程数上限 | 拒绝 spawn |

- **层级天然是树**：容器 Job → 子 Job → Process，与监督树重合（`04` §4.4），「谁该为超限负责」归因与监督/重启策略一体化。
- **与 `06` C-G5 回收归属一致**：容器镜像的自动 CoW 版本链/Blob GC 归 FS 服务自管，**不计入**容器 Job 账本；容器显式快照（含生效 `.msys` sysroot）归 supervisor 经 Job 账本管辖（`06` C-G5 第七轮裁决）。

### 4.2 能耗账本（与 `27`/`40` 弱耦合）

- **能耗原型期不作为 Job 账本硬配额项**（`04` §4.4 第四轮裁决回写 / `27`/`40`）：容器能耗不写入账本、不强制上限。
- **能耗仅作 task packing 输入**：用户态 `powerd`（`27` §3.3）据容器 Job 的 CPU 配额做能耗感知调度（同 Job 内任务聚核、空闲核深 idle）；调度器（EEVDF+RT）的 EAS 子策略（`04` §4.3 / `27` §5.1）在弱耦合下仅做 packing。
- **强耦合待 EAS 议题裁决**：把能耗升级为硬配额（超限限流/降权重）待 `04` §5 议题6（异构核 big.LITTLE EAS）裁决后定；届时扩展 `§4.1` 账本对象与检查点。**不变量**：`27`/`40` 的 thermal 硬限始终是内核 TCB 无条件边界，容器能耗约束不得突破。

---

## 5. 与用户系统（`53`）及监督者（`04`）协同

### 5.1 与监督者（`04`/`46`/`47`）协同

- **容器生命周期由 `meu-supervisor` 驱动**（`04` §4.5）：supervisor 读取容器 manifest（§3.2），在指定 Job 下 spawn 容器 init，持有其 Job handle；消费进程退出/异常事件（`00` A9 exception channel），按策略重启/升级/降级。
- **监督者自身受约束**：supervisor 持「创建 Job/监督」专属 capability（`04` §4.5），普通容器进程无法冒充；容器 init 崩溃不污染 supervisor（结构化取消，`04` §4.6 / `00` A9）。
- **与 `46`/`47` 衔接**：监督/Job 的详细实现（Job 命名可查询、scoped 标识、生命周期传播语义、异常仲裁）见 `46-进程管理-方案A` / `47-进程管理-方案B`（`47 §7.8` 含 realm 边界监督语义）；本文只承接「容器 = Job 子树 + namespace 模板」的监督落点。

### 5.2 与用户系统（`53`）协同

- **身份 = capability，合成 uid 仅展示层**（`53` §7.1.4）：容器内身份视图由 `53` 合成 uid 翻译层投影（`00` C4 / `53` 第七轮 C-G4 采纳 B），零权限附着；容器「root」只是合成标签。
- **uid 映射投影**（`53 §8.3`/`§8.4`）：容器内部 uid ↔ 真实 capability 作用域的投影边界由 `53` 定稿，本文不重判（见 §3.3）。
- **跨 realm 身份边界**（`53 §7.5`）：容器即 realm 实例，跨容器身份/会话隔离由 capability 派生边界承载（与 `53 §7.5` 多 realm 边界一致），合成 uid 不穿透 realm。
- **认证 broker 协同**（可选）：容器镜像签名校验可复用 `53 §8.5` 的 `AUTH_ISSUE` 受约束 issuer（bit55，`00` 第八轮冻结，Day 1 不触发）——仅当需容器镜像签发身份令牌时启用，本文仅标注衔接点。

---

## 6. 五架构同源；与既有地基同构（不引入新内核原语）

### 6.1 五架构同源

容器/sandbox 的全部语义（Job 子树 + namespace 模板 + capability 受约束 mint + `.mimg`/`.msys` 载体）**无任何架构相关代码**：

- 容器运行时逻辑 = 纯 capability + Job + namespace 的组合，**与页表格式/中断控制器/ABI 形态无关**（对齐 `01` 同源码异架构、`63` §7 netstack 同源范式）。
- 唯一架构差异点 = **启动载体 `.mimg`**（`61` §3.2 Item 0 内含五架构 `arch/<arch>/boot` 入口段，容器镜像一次打包五架构同载）与 NIC 驱动层（承接 `63` §7）——二者均不侵入容器/sandbox 抽象。
- i386 纯 C 约束（`02` C2）**不影响**容器运行时（它只落在内核早期启动与 NIC 驱动 C 后端），容器逻辑全在 Rust 用户态服务（supervisor / FS / netstack）与内核 capability 监视器（Rust TCB，`00` A3）。

### 6.2 与既有地基同构（无新原语、无新 right）

| 本文所用机制 | 既有地基（不新增） | 同构依据 |
|--------------|--------------------|----------|
| 容器 = Job 子树 | `04` §4.1/§4.4 Job 账本 | 原生 spawn + Job 树，容器只是结构化用法 |
| 容器视图 = namespace 模板 | `06` C3 每进程 namespace | COW 继承父/Job 模板，默认全不可见 |
| sandbox 默认 deny | `07` capability 模型 / `00` A1/A2 | 无隐含权限，句柄即 capability |
| 显式 grant 委托 | `07` §4.5 受约束 mint / 派生收窄 | 委托人上限 ∩ 客户端下限，只能收窄 |
| 跨容器共享 | `05` cap-transfer / `06` mount-capability | 现有 IPC/挂载能力委托，无新层 |
| 镜像 = `.msys`/` .mimg` | C61-1 唯一载体 / `06` §4.3 | 复用系统自举信任链 |
| 资源隔离 | `04` §4.4 账本 / `27`/`40` 能耗 | 已有 Job 配额，能耗弱耦合 |
| 跨容器边界 | 多 realm 边界（`00` 第八轮） | A7 多 realm 二级标签，同构复用 |

> **铁律重申**：本文**零新增内核原语**，**零新增 right bit**（rights 位图 bit0–47 冻结、`07` §4.1；bit48+ 预留区首冻结 bit49–55，`00` 第八轮）。所有结论均为既有地基的组合投影，未突破 `00` A1–A12 / C1–C18 任何一条；跨域冲突以 `00` 总览为准。

---

## 7. 待决项表（标注是否需大喵拍板）

| 编号 | 待决项 | 是否需大喵拍板 | 关联 |
|------|--------|----------------|------|
| **C1** | **容器 uid 映射投影边界**（「容器内 uid ↔ 真实 capability 作用域」如何不越权地投影为合成视图，防止「映射即越权」） | **已裁决·reasoning 决策已出（2026-08-03）：映射即展示、零权限附着** | `53 §8.3`/`§8.4`、`00` C4/`D6` |
| **C2** | **OCI 兼容边界**（哪些 OCI manifest/config/layer 字段 Day 1 投影、哪些 ENOSYS；是否提供 runc 式 OCI runtime 接口） | 否（协同 `08` 兼容层定稿） | `08`、`06` C-G5、`61` C61-1 |
| **C3** | **资源隔离粒度 vs 能耗硬配额**（能耗是否升为 Job 账本第六类硬配额，超限限流/降权重） | **已裁决·reasoning 决策已出（2026-08-03）：能耗弱耦合、不硬配额** | `04 §4.4` 第四轮、`27`、`40` |
| **C4** | **跨容器 capability 委托范围**（哪些 cap 可跨容器显式 grant、grant 上限是否需 supervisor 级 mint 约束） | 否（与多 realm 边界 / `07 §4.5` 同构，协同定稿） | `00` 多 realm、`07 §4.5`、`53 §7.5` |
| **C5** | **与多 realm 边界关系**（容器是否即一个 realm 实例、跨容器是否一律按 realm 默认 deny 处理） | **已裁决·reasoning 决策已出（2026-08-03）：容器≠realm、默认 deny** | `00` 第八轮、`56 §7.3` 冲突8、`46 §7.7`/`47 §7.8`/`53 §7.5` |
| **C6** | **容器 init 作为 Job root 还是 supervisor 子 Job**（容器树在 supervisor Job 下的挂靠位置与生命周期传播语义） | 否（协同 `04`/`46`/`47` 定稿） | `04 §4.5`、`46`、`47` |
| **C7** | **网络命名空间视图在容器内投影**（`/net` 子树 reveal 语义、容器间 netns 隔离粒度） | 否（协同 `06` C3 / `63` N7 定稿） | `06` C3、`63 §3.3`/`N7` |
| **C8** | **容器镜像签名验签的 broker 角色**（是否复用 `AUTH_ISSUE` bit55 受约束 issuer 做镜像身份令牌，Day 1 是否触发） | 否（`00` 第八轮已冻结 bit55，Day 1 不触发；仅标注衔接点） | `53 §8.5`、`00` 第八轮、`32`/`61` |

> 标注：本文为规划草案，后续以修订式追加更新，不覆盖、不删改 `00`–`71`、README、`.issues/`、git。跨域冲突以 `00` 总览 `A1`–`A12` / `C1`–`C18` 为准；本文零新增内核原语、零新增 right bit，全部结论为 `04`/`06`/`07`/`46`/`47`/`53`/`61` 既有地基的组合投影。待决项 C1/C3/C5 为架构级、需大喵拍板；C2/C4/C6/C7/C8 为协同执行层定稿项。

---

## 8. 第十轮自主采纳·闭环 C1 / C3 / C5

### 8.1 C1（容器 uid 映射投影边界）→ 采纳「映射即展示、零权限附着」
- 容器内合成 uid 仅作 capability 作用域**展示投影**（复用 `53` C-G4 已采纳 B：合成 uid 零权限附着）。
- 映射规则由 `53 §8.3`/`§8.4` 翻译层统一承载，容器不新增映射原语。
- 「映射即越权」经翻译层不可越权 + 真实裁决只走 capability 规避。与 C4 / `07 §4.6` 同源。

### 8.2 C3（能耗硬配额）→ 采纳「per-Job 能耗弱耦合、不做硬配额」
- 容器资源隔离以 CPU/内存/IO/pid 经 Job 账本（`04`）为准。
- 能耗维持第七轮「能耗-Job」弱耦合口径，不升格硬配额、不新增 right。与 `27`/`40` 能耗-Job 一致。

### 8.3 C5（与多 realm 边界关系）→ 采纳「容器≠realm；跨容器默认按 realm 默认 deny」
- 容器是 Job 子树 + namespace 组合视图（同一 realm 内隔离单元），不引入新 realm 实例。
- 跨容器取消/能力/命名传播默认 deny 不穿透，与第八轮「多 realm 边界」裁决（capability 边界即 realm 边界、默认 deny）同构。
- 容器不引入新内核原语/新 right bit，与 `00` A1–A12 / C1–C18 严格一致。

> 待大喵复核。

---

## 9. reasoning 决策（2026-08-03）：C1 / C3 / C5 大喵复核细化

> 本节为 reasoning 决策 agent（2026-08-03）在指挥官第十轮「映射即展示零权限附着 / 能耗弱耦合 / 容器≠realm 默认 deny」占位基础上做的细化复核——不删不改 §8 原文与 §7 待决项的占位采纳，仅追加复核结论与「被否决选项的取舍」理由。复核重点在 capability Day 1 唯一裁决点（`07` §4.1 rights 位图冻结纪律）、五架构同源可行性、与既有地基（多 realm 边界 / 容器 = Job 子树 + namespace 组合视图 / `.mimg`/`.msys` 唯一载体）的协同。

### 9.1 C1 · 容器 uid 映射边界 → 同意「映射即展示、零权限附着」

**reasoning 决策（2026-08-03）**：同意并固化「容器内合成 uid 仅作 capability 作用域展示投影，不携带任何 capability；映射错误不可越权」——同时明示「`53 §8.3`/`§8.4` 是承载而非绕开 capability 模型的翻译层，容器 fs 不新增映射原语」。

**理由（含被否决选项的取舍）**：

- **capability 唯一裁决点自洽**：与 `07 §4.1` rights 位图冻结纪律（bit 0–47 已分、bit 48+ 预留）+ `00 A1/A2` capability Day 1 唯一权限模型严格同源。容器内「uid=1000」的合成视图仅是 inspect/翻译接口的呈现标签，所有真实权限判定走 capability 句柄 rights，不存在「映射即越权」的 confused deputy 路径。
- **与既有地基协同**：与 `53 §7.1.4` 合成 uid 仅展示层、`53 §7.5` 多 realm 身份边界、`00 C4` uid/gid 仅作合成视图、`00 C7` 复用 `READ`/`WRITE` 不新增 bit、`00 §5-6` 五架构薄后端、`61 C61-1` `.mimg`/`.msys` 唯一载体同源——形成「capability 唯一裁决 + 合成视图展示 + 多 realm 边界拒绝穿透」三层同源闭环。
- **五架构同源可行性**：uid 合成视图是纯翻译规则（uid 域间映射 = 「容器内部呈现 ↔ Job capability 作用域」），uid 翻译在用户态 `53` 服务内完成（capability 受约束 mint，不入核、不入 TCB），五架构零差异。
- **长期可维护性**：uid 翻译规则可由 `53` owner 修改、回滚、灰度，不触发内核重建；与 `04 §4.5` 监督者框架兼容（容器 init 崩溃 → 监督者按策略重启 uid 翻译容器，与 `53` 翻译层重置）。

**被否决选项**：

- **「真实 uid 映射（Linux userns 式）」**——否决。引入内核 uid 概念与 userns 管理原语，扩张内核 TCB、违背 A6「重服务出核缩 TCB」、与 `00 A2` capability Day 1 唯一模型直接冲突。`53 §8.3` 已记录「userns 在 MeuOS 不入核」的实质成本，映射留在翻译层是正确的妥协。
- **「容器内 uid 即真实 capability 主体」（uid-as-principal）**——否决。在 capability 模型下句柄即 capability、uid 不可作为权限主体，否则立刻破坏 `07 §2.3`「不存在『超级用户』可以绕过 capability 集合」的 Day 1 基线。
- **「uid 翻译 + capability 委派都进内核（强耦合）」**——否决。翻译规则频繁迭代，进内核意味着每次修改触发全量镜像重建，违反 `.msys` 升级纪律（`23 §5`）。

**对本文的细化落点（不删前文）**：§3.3 已写「uid 映射投影边界」由 `53 §8.3`/`§8.4` 翻译层承载，§9.1 进一步明示「翻译层 = 用户态服务、不入核、不入 TCB」以避免后续被误读为「uid 翻译有内核桥接需求」；`§7` C1 状态列同步升格。

### 9.2 C3 · 容器能耗硬配额 → 同意「per-Job 能耗弱耦合、不做硬配额」

**reasoning 决策（2026-08-03）**：同意并固化「容器能耗维持 per-Job 弱耦合口径、不作为 Job 账本第六类硬配额」——同时把约束边界收口到「thermal 硬限始终为内核 TCB 无条件边界（`27`/`40` 不可破）、能耗基准仅作 task packing 信号（`74 §4` 不升级为配额）」。

**理由（含被否决选项的取舍）**：

- **TCB 体量与跨架构差异**：能耗模型跨架构差异巨大（x86_64 RAPL + 完整 PMU / aarch64 AMU + 异构核 / riscv64 SBI PMU 平台碎片化 / loongarch64 自有 PMU / i386 无现代 PMU，仅周期估算）；Day 1 硬配额 = 「在一份不完整的数据上跑计费」，不可承受、与 `74 §1.4`「`unsupported` 架构以降级标记暴露而非伪造零值」fail-closed 原则冲突。
- **thermal 与能耗的边界正确性**：thermal 硬限是「触发即关」的物理边界（与 `27 §4` 一致），是 threat 到硬件的不可破环、是内核 TCB 必守边界；能耗是「信号 → 调度」，是 P 类（policy）决策而非 M 类（mechanism）决策。把能耗提升为硬配额 = 把策略决策固化到机制层，违背 `07` §4.1 rights 设计原则（rights 应只描述对象权限而非策略选择）。
- **与既有地基协同**：与 `27 §3.3` 第四轮「能耗-Job 弱耦合」已采纳（`00 §3` 冲突登记表）、`40 §1.3`/`§4` 能耗不进 Job 账本、`04 §4.4` Job 账本 Day 1 冻结（CPU / 内存 / 句柄数 / IO 带宽 / pid 数五类，不含能耗）、`74 §4` perf counters 与能耗协同基准同源——形成「thermal 硬限不可破 + 能耗信号弱耦合 + 五类硬配额不动」三层同源闭环。
- **未来升级路径明确**：能耗硬配额的真正解锁条件不是「意愿」而是「硬件数据稳态 + 模型成熟度」，即：(a) 跨架构 `ENERGY_READ` 真实可用（非 `unsupported`）；(b) per-Job 能耗聚合模型经 TLA+ 模型检验（`20 §4.2` Layer B）；(c) EAS 议题 6 给出 per-cpu-class 计费语义（`04 §5` 议题 6）——三件齐备才升级，且升级路径本身由执行层发起、不在原型期预设。

**被否决选项**：

- **「能耗硬配额（第六类 Job 账本）」**——否决。能耗模型跨架构不稳 + proto 硬配额 + thermal 已守物理边界，三层叠加 = 原型期净损。
- **「per-package 能耗硬上限，不分 Job」**——否决。绕过 capability 边界且无法与 Job 监督者框架集成（OOM 归因对不上 Job），与 `04 §4.4` Job 账本是容器资源隔离的唯一单元这一底座冲突。
- **「能耗硬配额 + thermal 豁免」**——否决。豁免 = 「某种配置 root」漏洞入口，直接违背 `07 §4.2` 配置 root 不可存在 + `30 C16②` 编译期固定安全参数不可运行期改写。

**对本文的细化落点（不删前文）**：§4.2 已写「能耗原型期不作为 Job 账本硬配额」，§9.2 进一步把「升级路径的三件先决条件」显式列出（拱卫未来升级不被误解为「随意添加配额维度」），与 `74 §4` / `04 §4.4` 同源；`§7` C3 状态列同步升格。

### 9.3 C5 · 容器与多 realm 关系 → 同意「容器≠realm、跨容器默认按 realm 默认 deny」

**reasoning 决策（2026-08-03）**：同意并固化「容器 = Job 子树 + namespace 组合视图（同一 realm 内隔离单元），不引入新 realm 实例；跨容器默认按多 realm 边界默认 deny 处理；显式 grant = 多 realm 边界显式 grant 的同构复用」——同时把「realm」与「容器」的抽象层区分明示，避免后续被误读为「容器即权限隔离边界」或「realm 即沙箱」。

**理由（含被否决选项的取舍）**：

- **抽象层语义清晰**：realm = capability 边界之上的隔离域（capability 边界即 realm 边界，默认 deny 是 `56 §7.3` 冲突 8 与 `46 §7.7`/`47 §7.8`/`53 §7.5` 同源结论）；容器 = Job 树的生命周期 + namespace 视图的隔离闭包（Job 子树 = 资源与生命周期单元，namespace 模板 = 视图边界）。两者抽象层正交：Job 节点可属不同 realm；同 realm 内可有多个容器。强行绑定（容器 = realm 或 realm = 容器）会破坏同源复用基础。
- **与既有地基同构复用**：跨容器传播默认 deny 不穿透 = 「跨 realm 取消/能力/命名传播默认 deny、不穿透」的同构复用（`00 §3` 第八轮多 realm 边界采纳）；仅显式 grant 跨域 = 多 realm 边界显式 grant 的同构复用；不引入新跨容器层 = 多 realm 边界无新统一跨 realm 层的同构复用。一套边界逻辑、三处场景（跨 realm / 跨容器 / 跨服务），零概念碎片。
- **capability 模型自洽**：与 `00 A5` Job 资源账本 Day 1 + `00 A2` capability Day 1 唯一裁决点 + `07 §4.5` 受约束 mint 一致；跨容器 capability 委派走「委托人 rights 上限 ∩ 客户端请求下限」取交集的受约束 mint（破 confused deputy）——容器仅是「以 Job 为单位走 mint」的天然分组，不引入新授权维度。
- **五架构同源 + 长期可维护性**：容器 runtime = 纯 capability + Job + namespace 组合，零架构分支（与 `63 §7` netstack 仅 NIC 驱动层差异 + `68 §7.1` 调度五架构同源同构）；监督/Job 生命周期传播语义由 `46 §4`/`47 §4` 定稿、容器只在其上做结构化用法。

**被否决选项**：

- **「容器即 realm 实例」**——否决。把 Job 子树与 capability 边界耦合，破坏同构复用与「realm 即 capability 边界」的语义唯一性；后续跨容器 + 跨 realm 判定分裂为两条独立规则，引入认知与实现负担。
- **「跨容器默认允许，按 manifest 收敛」**——否决。违背 `07 §2.3`「Day 1 即强制、无回退」 + `00 多 realm`「默认 deny、不穿透」 + `22 §4.2`「fail-closed」三重基线；与「容器 = sandbox」的概念相等价于「容器 = 默认不越狱的封闭环境」。
- **「容器内自带专属 broker 解决跨容器认证」**——否决。与 `81 §8.5` `AUTH_ISSUE`(bit 55) 受约束 broker 已构成完整机制叠加 broker = 双 issuer 混乱；多 realm 边界已提供跨域认证框架，容器无需单立。
- **「容器跨节点自动 follow（自动按 realm 网络拓扑转发）」**——否决。引入隐式能力传播，与「显式 grant」纪律冲突；跨节点能力委派遵循 `68 §4.1` cross-node cap 委派的现有规则，容器不另设隐式网络。

**对本文的细化落点（不删前文）**：§2.2 已写「容器 sandbox 委托与多 realm 边界同构」的类比表，§9.3 进一步明示 realm 与容器的正交关系（Job 子树节点可属不同 realm；同 realm 内可有多个容器），避免误读为「容器 = realm」；`§7` C5 状态列同步升格。

---

### 容器运行时（直接对标）
- OCI Image Specification（manifest/config/layers 结构）：https://github.com/opencontainers/image-spec/blob/main/spec.md
- OCI Runtime Specification（runc create/start、state）：https://github.com/opencontainers/runtime-spec/blob/main/spec.md
- runc（参考 OCI runtime 实现）：https://github.com/opencontainers/runc
- systemd-nspawn（轻量容器/namespace 工具，无独立内核）：https://www.freedesktop.org/software/systemd/man/systemd-nspawn.html
- FreeBSD jail（OS 级容器前身，capability 式隔离）：https://docs.freebsd.org/en/books/handbook/jails/

### 用户态沙箱隔离（sandbox 默认 deny 思想）
- gVisor（用户态 sentry 沙箱，syscall 拦截 + 独立内核态表面，非 VM）：https://gvisor.dev/docs/architecture_guide/
- Firecracker（microVM 隔离模型，KVM 轻量 VM）：https://firecracker-microvm.github.io/
- Linux user_namespaces（uid 映射、容器隔离基石）：https://man7.org/linux/man-pages/man7/user_namespaces.7.html
- Capsicum（默认 deny 能力模式，可见性即权限）：https://www.cl.cam.ac.uk/research/security/capsicum/

### 镜像/信任链（载体映射基线）
- `.mimg`/`.msys` 唯一载体（C61-1）：本文 `61-内核自举与早期启动深化.md` §3 / `00` 第八轮自主采纳 C61-1
- Android Verified Boot（vbmeta = 分离签名 + Merkle 根，`.mimg` Item 4 外置 vbmeta 思想来源）：https://source.android.com/docs/security/features/verifiedboot
- Fuchsia blobfs（内容寻址、hash=完整性，`.msys` A 子集对标）：https://fuchsia.dev/fuchsia-src/concepts/filesystems/blobfs

### 项目内（已读，跨域对齐）
- `00-总览与路线图.md`（`A1`–`A12` 句柄即 capability、`A4` 统一命名空间、`A5` Job 账本、`A7` VMO/VMAR 零拷贝、C18/C60-1/C61-1/多 realm 边界第八轮采纳）
- `04-进程与调度.md`（Job 树 §4.1、spawn §4.2、EEVDF+RT §4.3、Job 账本 §4.4、监督 §4.5、结构化取消 §4.6、第四轮能耗弱耦合回写）
- `06-文件系统.md`（统一命名空间 §4.1、C3 每进程 namespace、`.msys` 原子更新 §4.3、C2 密钥=cap、C-G5 A/B 并存）
- `07-安全模型.md`（capability/rights 冻结位图 §4.1、受约束 mint/派生收窄 §4.5）
- `27-电源与热管理.md` / `40-电源管理与能耗.md`（能耗弱耦合、task packing、thermal 硬限）
- `46-进程管理-方案A.md` / `47-进程管理-方案B.md`（监督/Job 详细语义、`47 §7.8` realm 监督）
- `53-用户系统-方案B.md`（合成 uid 视图 §7.1.4、`§8.3`/`§8.4` 容器 uid 映射、`§7.5` 多 realm、`§8.5` AUTH_ISSUE）
- `61-内核自举与早期启动深化.md`（C61-1 `.mimg` 唯一载体、`.msys` 双锚、`§3` 统一映像格式）
- `63-网络栈实现深化.md`（`.net` 子树 reveal、N7 vlan 子接口、socket 句柄 capability）

---

> 本文为「设计/规划」草案，结论（容器 = Job 子树 + namespace 组合视图；sandbox 默认 deny、capability 显式 grant 委托，与多 realm 边界同构；OCI-ish 镜像/manifest 投影为 `.mimg`/`.msys` 载体；资源隔离以 Job 为单元、能耗弱耦合；五架构同源、零新增内核原语/right bit）供执行层与 `04`/`06`/`07`/`46`/`47`/`53` 引用；待决项 C1/C3/C5 需大喵架构级拍板，C2/C4/C6/C7/C8 为协同执行层定稿项。跨域冲突以 `00` 总览 `A1`–`A12` / `C1`–`C18` 为准。本文不修改 `00`–`71`、README、`.issues/`、git。
