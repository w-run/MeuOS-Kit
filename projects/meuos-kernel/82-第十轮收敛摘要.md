# 82 - 第十轮收敛摘要

> 合成 / 合并（merge）文档，由 kernel-plan 团队的「合成 / 合并 agent（reasoning / MiniMax-M3）」产出。用户已明确授权 reasoning 仅用于合并收尾工作。
> 工作树：`/workspace/MeuOS-Kit/.agents/worktrees/kernel-plan/`，分支：`worktree-kernel-plan`，文档根目录：`projects/meuos-kernel/`。
> 本文是**独立新增合成文档**，**只读不改**`00`–`81` 任何章节（含 `71`–`81` 内部回写节、`77` 第九轮摘要、`00 §10` 第十轮总结、README）；仅对第十轮「50/50 查缺补漏 + 拓展」做收口合成。
> 定位：第十轮延续第八/九轮的 50/50 模式——查缺补漏半收口 `71`–`76` 内部原标注需大喵拍板的 11 项待决（D2/D5 / C1/C3/C5 / P-73-1/P-73-5 / B5 / P1/P2 / P8）；拓展半新增 4 个新维度（`78`–`81`：加密与密钥管理、用户态服务监督与生命周期框架、设备热插拔与动态设备树、凭证与认证 broker 深化）。
> 性质：**合成 pass 产出**，结论可在后续轮次以**修订式追加**更新，不删；与 `70`/`77` 同风格。

---

## §0 合成说明与方法

- **输入**：`00`（总览与路线图，含 §10 第十轮总结——已含本轮全部裁决结论与索引）、`77-第九轮收敛摘要.md`（§2/§4 第九轮收口状态与本轮建议）、`71 §9`（D2/D5 回写）、`72 §8`（C1/C3/C5 回写）、`73 §8`（P-73-1/P-73-5 回写）、`74 §9`（B5 回写）、`75 §9`（P1/P2 回写）、`76 §10`（P8 回写）——确认这些回写节确实存在且状态已改为「已裁决·闭环」；4 个新维度 `78`–`81`（各取 §0 一句话核心结论 + §7 待决项表）。
- **方法**：解构型立场——先核对「`71`–`76` 内部原标注需大喵拍板的 11 项」是否全部闭环，再汇总 4 新维度（一句话定位 + 与既有地基衔接 + 待决项计数），最后给跨轮衔接与索引纪律。所有内容均建立在既有地基之上，不重开既有裁决。
- **纪律**：
  1. **只读 00–81 不修改任何文件**：本轮所有回写均由对应 owner 在各子文档文末以「修订式追加」方式完成（如 `71 §9` D2/D5、`72 §8` C1/C3/C5、`73 §8` P-73-1/P-73-5、`74 §9` B5、`75 §9` P1/P2、`76 §10` P8），本文仅汇总与核对闭环状态。
  2. **不引入新内核裁决机制、不新增 right bit**：bit 49–55 已在第八轮首冻结，bit48 维持冻结仅 `SCENE_SESSION` 候选描述、`AUTH_ISSUE`=bit55 已冻结复用，本轮维持冻结；bit 56+ 仍归未来；`78`–`81` 零新增 right bit（K-78-1 提议细分属待决项，不私自扩展；H1 提议新内核对象属待决项，倾向拒绝）。
  3. **五架构同源**：所有新维度均声明五架构逻辑同源、差异封进薄后端；加密密钥、用户态监督、设备热插拔、认证 broker 等面均无新增架构级裁决。
  4. **既有地基不变**：capability（`07`）+ Job 账本（`04 §4.4`）+ 结构化取消（`04 §4.6` / `00` A9）+ 每进程 namespace（C3 / `06`）+ 重服务出核（`00` A6）+ `KERNEL_PRIV`（D-T1 / `29`）+ `CFG_*`（C16 / `30`）+ `DBG_*` + `NET_*` + `CAP_RT`（bit45）+ `GUEST_CREATE`/`VM_MANAGE`（bit46/47）+ `KEY_*`（bit7/8）+ `AUTH_ISSUE`（bit55）+ bit 48+ 预留区（`38`/`45`/`07 §4.1`）+ 第八轮首冻结 bit 49–55 等地基全部原状保留。
  5. **跨域冲突以 `00` 为准**：凡与既有 `A1`–`A12` / `C1`–`C18` / `C-G1`~`C-G5` / D-T1 / R1 / V1 / 能耗-Job / 多 realm 边界 / bit 49–55 冻结 等既有裁决交叉项，以 `00` 为准；本文不重判。

---

## §1 查缺补漏半闭环核对（11 项裁决）

> 本节核对第十轮查缺补漏半对 `71`–`76` 内部原标注「需大喵拍板（架构级）」的 **11 项**待决项的闭环状态。第十轮指挥官据授权对 11 项**全部自主采纳闭环**，零新增内核原语、零新增 right bit（bit 49–55 外无新增）。11 项分别为：**`71` D2 + D5**、**`72` C1 + C3 + C5**、**`73` P-73-1 + P-73-5**、**`74` B5**、**`75` P1 + P2**、**`76` P8**。

### §1.1 11 项裁决闭环表

| # | 裁决编号 | 议题（原 71–76 内部标注） | 采纳结论（一句话） | 回写落点（已落盘） | 原状态 → 现状态 |
|---|---|---|---|---|---|
| 1 | **71 D2** | 兼容层是否整体进入 TCB（direct-mapped 整形逻辑处内核态、emulated/synthesized 复杂语义退用户态、整体定调） | **采纳「分层 TCB」**：兼容层不整体入 TCB；仅内核态薄 direct-mapped 整形逻辑（参数规整、errno 映射表）按 `00` A3 入 TCB 边界并受 `02` TCB 度量，emulated/synthesized 复杂语义退用户态服务、不进 TCB | `71` §9.1；§7 D2 行状态列升级为「已裁决·闭环（第十轮自主采纳，分层 TCB）」 | 需大喵拍板（架构级）→ **已裁决·闭环（第十轮自主采纳）** |
| 2 | **71 D5** | native/compat 双路径长期维护成本（双路径漂移风险、五架构 × 双路径测试矩阵、direct-mapped 子集变更如何同步两套语义） | **采纳「direct-mapped 子集为权威 + CI 双路径一致性门禁」**：direct-mapped 子集定义为 single source of truth，compat 路径视为其投影并自动生成测试；五架构 × 双路径矩阵以合成测试覆盖；长期收敛 native 优先 | `71` §9.2；§7 D5 行状态列升级为「已裁决·闭环（第十轮自主采纳，direct-mapped 为权威 + CI 双路径门禁）」 | 需大喵拍板（架构级）→ **已裁决·闭环（第十轮自主采纳）** |
| 3 | **72 C1** | 容器内 uid 映射投影边界（容器内 uid ↔ 真实 capability 作用域如何不越权地投影为合成视图，防「映射即越权」） | **采纳「映射即展示、零权限附着」**：容器内合成 uid 仅作 capability 作用域**展示投影**（复用 `53` C-G4 已采纳 B：合成 uid 零权限附着）；映射规则由 `53 §8.3`/`§8.4` 翻译层统一承载，容器不新增映射原语 | `72` §8.1；§7 C1 行状态列升级为「已裁决·闭环（第十轮自主采纳，映射即展示·零权限附着）」 | 需大喵拍板（架构级）→ **已裁决·闭环（第十轮自主采纳）** |
| 4 | **72 C3** | 资源隔离粒度 vs 能耗硬配额（能耗是否升为 Job 账本第六类硬配额，超限限流/降权重） | **采纳「per-Job 能耗弱耦合、不做硬配额」**：容器资源隔离以 CPU/内存/IO/pid 经 Job 账本（`04`）为准；能耗维持第七轮「能耗-Job」弱耦合口径，不升格硬配额、不新增 right | `72` §8.2；§7 C3 行状态列升级为「已裁决·闭环（第十轮自主采纳，能耗弱耦合·不硬配额）」 | 需大喵拍板（架构级）→ **已裁决·闭环（第十轮自主采纳）** |
| 5 | **72 C5** | 容器与多 realm 边界关系（容器是否即一个 realm 实例、跨容器是否一律按 realm 默认 deny 处理） | **采纳「容器≠realm；跨容器默认按 realm 默认 deny」**：容器是 Job 子树 + namespace 组合视图（同一 realm 内隔离单元），不引入新 realm 实例；跨容器取消/能力/命名传播默认 deny 不穿透，与第八轮「多 realm 边界」裁决（capability 边界即 realm 边界、默认 deny）同构 | `72` §8.3；§7 C5 行状态列升级为「已裁决·闭环（第十轮自主采纳，容器≠realm·默认 deny）」 | 需大喵拍板（架构级）→ **已裁决·闭环（第十轮自主采纳）** |
| 6 | **73 P-73-1** | 快照一致性点语义（应用/用户态快照应在「应用静默（quiesce）」还是「文件系统事务边界」取一致点；是否提供「崩溃一致快照」与「应用一致快照」两种粒度） | **采纳「双层粒度并存」**：崩溃一致快照为原语级能力，直接复用 `03` VMO + COW child（写时复制瞬间冻结视图）即可立即提供，无需任何应用协同；应用一致快照为可选增强（opt-in 协同协议，应用经 capability 触发 quiesce 后取点）；默认对外提供崩溃一致快照，应用一致不强制 | `73` §8.1（P-73-1 段）；§7 P-73-1 行状态列升级为「已裁决·闭环（第十轮自主采纳，双层粒度并存）」 | 需大喵拍板（架构级）→ **已裁决·闭环（第十轮自主采纳）** |
| 7 | **73 P-73-5** | 引导级恢复与运行时 repoint 共用信任链细节（C61-4 引导级恢复 `.msys` 指针与运行时快照/备份恢复是否共用同一 repoint 原语与同一度量 extend 点） | **采纳「共用 repoint 原语 + PCR[3]」**：C61-4 引导级恢复 `.msys` 指针与运行时快照/备份恢复共用 `06 §4.3` 原子 repoint 原语 + 同一度量 extend 点（统一 extend `PCR[3]`，与 `61` C61-4 / `32 §3.2` 双锚一致）；不新增测量寄存器，差异仅触发方（bootloader vs 运行时服务） | `73` §8.2（P-73-5 段）；§7 P-73-5 行状态列升级为「已裁决·闭环（第十轮自主采纳，共用 repoint 原语 + PCR[3]）」 | 需大喵拍板（架构级）→ **已裁决·闭环（第十轮自主采纳）** |
| 8 | **74 B5** | 与形式化 `20` 互补边界（性能基准是否证正确性、是否仅证不回退；与 `20 §8` D7、`75` 互补边界） | **采纳「只证不回退、不证正确性」**：性能基准的职责收敛为**量化不回退**——以相对 golden 的相对阈值度量「是否悄悄劣化」，正确性则由 `20` 形式化精化证明 + `22` 否定式测试承载；与 `20`/`22` 定稿后回填交叉引用但不重叠 | `74` §9；§7 B5 行状态列升级为「已裁决·闭环（第十轮自主采纳，只证不回退不证正确性）」 | 需大喵拍板（架构级）→ **已裁决·闭环（第十轮自主采纳）** |
| 9 | **75 P1** | 证明范围优先级落地（capability 核心第一、`68 Sc9` 调度核心第二的排序与投入阈值） | **采纳「capability 核心第一·Sc9 第二入 Layer A」**：capability 监视器（rights 检查 + revoke 派生树，`20 §4.1`）为 Day 1 唯一权限裁决点（`00` A3）之数学内核，优先级最高；`68 Sc9` 调度核心（RT 选择 / deadline 会计 / 带宽记账 / 锁协议）排第二，于 M3 后设计稳定时进入 Layer A，投入阈值按 `20 §8` D1 落地 | `75` §9（P1 段）；§7 P1 行状态列升级为「已裁决·闭环（第十轮自主采纳，capability 核心第一·Sc9 第二入 Layer A）」 | 需大喵拍板（架构级）→ **已裁决·闭环（第十轮自主采纳）** |
| 10 | **75 P2** | 工具链选型（Layer A 主工具 Verus vs Kani 分工、参考模型宿主 Lean 4 vs Coq；关联双工具链约束 `02` C1） | **采纳「Verus+Kani 主·Lean/Coq 宿主·TLA+ 并发」**：主工具 Verus（证 TCB 代码精化）+ Kani（证位级不变量，如句柄表不悬垂/不伪造、派生链收窄）共同覆盖 capability 监视器 + 调度核心；Lean 4 / Coq 二选一承载 I1–I7 代数参考模型与端到端 spec→imp 精化链；TLA+ / `stateright` 证 Layer B 并发协议 | `75` §9（P2 段）；§7 P2 行状态列升级为「已裁决·闭环（第十轮自主采纳，Verus+Kani 主·Lean/Coq 宿主·TLA+ 并发）」 | 需大喵拍板（架构级）→ **已裁决·闭环（第十轮自主采纳）** |
| 11 | **76 P8** | 跨 node VMO 共享时物理页 node 归属与迁移是否破坏 capability 引用（`03 §5-5` 残留 + `00` C1 折中） | **采纳「确认本文判定：不破坏 capability 引用」**：`03 §5-5` C1 折中已分离逻辑对象与物理供给——VMO 是逻辑对象（capability 视图），物理页的 node 归属是供给层属性；跨 node 迁移仅改 `MmPage.node_id`，VMO 句柄与 capability 引用（派生链、引用计数、revoke 面）均不变，无需架构级额外机制 | `76` §10；§7 P8 行状态列升级为「已裁决·闭环（第十轮自主采纳，确认不破坏 capability 引用）」 | 需大喵拍板（架构级）→ **已裁决·闭环（第十轮自主采纳）** |

> **闭环结论**：11 项（原列「需大喵拍板（架构级）」）裁决全部已裁决闭环——采纳结论均建立在既有地基之上；零新增内核原语、零新增 right bit（V1 立场 A 不新增 bit56+ 纪律延续——第十轮 11 项裁决均不动 `KERNEL_PRIV`(12)/`CFG_*`(9–11)/`DBG_*`(13–18)/`BLOCK_*`(23–27)/`AUDIO_*`(19–22)/`NET_*`(28–31, 39)/`KEY_*`(7–8)/`CAP_RT`(45)/`GUEST_CREATE`(46)/`VM_MANAGE`(47)/`IRQ_*`(52–54)/`CLOCK_*`(50–51)/`IO_*`/`UART_*`(49)/`AUTH_ISSUE`(55)/`SCENE_SESSION`(bit48 预留) 等所有已冻结或预留位）。本轮后**无残留待大喵拍板架构级项**（`71`–`76` 内部原列 11 项现已全部由指挥官自主采纳闭环，待大喵复核确认）。
>
> **bit 纪律注记**：本轮 11 项裁决涉及 bit 的仅 P8（确认 `03 §5-5` 既有 capability 引用语义不变、不新增 bit）——皆遵循 bit 48+ 预留区「预埋不启用」纪律，与 `32` D-SB7（`ATTEST`=bit39 同构）一致；bit 48 维持冻结仅 `SCENE_SESSION` 候选描述、bit 49–55 维持第八轮首冻结、bit 56+ 仍归未来。

### §1.2 与既有地基的同构收口

> 11 项裁决与 `00 §2` A1–A12 地基 + C1–C18 / C15 / C16 / C17 / C18 / V1 / R1 / D-T1 / 能耗-Job / C-G1~C-G5 / 第七轮 C61-1~4·C60-1·多 realm / 第八轮 bit 49–55 冻结 / 第九轮 63–69 内部 6 项自主采纳 等既有裁决**完全自洽**，无任何冲突；落地后各裁决共同遵守以下不变量：

- **capability Day 1 唯一权限模型、无 root、句柄即 capability、派生收窄、崩溃回收**（`07 §4.1` / `§4.5` + `00 A1/A2`）：11 项裁决均未引入新内核裁决机制、未触碰 rights 位图既有 0–47 与预留 48+。71 D2 薄层只入 TCB 边界、复杂语义退用户态；71 D5 direct-mapped 升格权威源；72 C1 容器内合成 uid 零权限附着；72 C3 能耗弱耦合；72 C5 跨容器默认 deny；73 P-73-1/P-73-5 复用 `06 §4.3` repoint 与 `32` 度量基；74 B5 性能基准只证不回退；75 P1/P2 capability 核心第一与工具链 Verus+Kani；76 P8 确认跨 node VMO 共享不破坏 capability 引用。
- **Job 资源账本 Day 1**（`04 §4.4` + `00 A5`）：72 C3 能耗不升格 Job 硬配额；72 C5 容器资源以 Job 账本为单元；76 P8 物理页分配点即配额扣减点。
- **结构化取消 / 异常上行**（`04 §4.6` + `00` A9）：72 C5 跨容器取消默认 deny 与第八轮多 realm 边界同构；73 P-73-1 崩溃一致快照是原语级，依赖 `04` 监督协同应用一致快照的 quiesce 触发。
- **每进程 namespace（默认不可见、显式 reveal）**（C3 / `06` + `00 A4`）：72 C1 容器内合成 uid 仅展示层；72 C5 跨容器能力/命名传播默认 deny；73 P-73-5 命名空间 repoint 与 C3 default-deny 一致。
- **重服务出核缩 TCB**（`00 A6` + A11）：71 D2 emulated/synthesized 退用户态；74 B5 性能基准不触碰内核实现（量化经验性）。
- **`KERNEL_PRIV` 内核态高危能力保留**（D-T1 / `29`）：71 D2 内核态薄层 `panic=abort` 纪律；76 P8 物理页 node 迁移不绕过 `KERNEL_PRIV` 边界。
- **Rust 管安全关键路径、最小 TCB**（A3）：71 D2 薄层分发属内核 TCB 边界；75 P1 capability 监视器为 `02` 约束 Rust TCB；75 P2 Verus/Kani 直接作用于 Rust 源码。
- **多 realm 边界（`00` 第八轮·已采纳）**：72 C5 容器即该「多 realm 边界」的一个标准实例（容器是同一 realm 内隔离单元），与第八轮「跨 realm 取消/能力/命名传播默认 deny、不穿透」裁决完全同构；73 P-73-5 跨 realm repoint 不穿透。
- **`.mimg` 唯一启动载体 + C61-4 恢复指针（已采纳）**：73 P-73-5 与 C61-4 / `32 §3.2` 度量基双锚严格一致——共用 repoint 原语 + 统一 extend `PCR[3]`。
- **`AUTH_ISSUE`=bit55 已冻结复用**：76 P8 跨 node 物理页归属迁移不触碰 `AUTH_ISSUE` 边界（密钥句柄仍由 `78` 承载，见 §2）。
- **C15 / C16 / C17 / C18 / V1 / R1 / D-T1 / 能耗-Job / C-G1~C-G5**：71 D2 与 A6 一致；71 D5 与 `08 §2.2` 薄层同源；72 C3 与能耗-Job 弱耦合一致；72 C5 与第八轮多 realm 边界同构；73 P-73-1 与 `06 §4.3` repoint 同源；73 P-73-5 与 C61-4 同构；74 B5 与 `20 §8` D7 / `75` 互补边界一致；75 P1 与第九轮 Sc9 闭环同源确认；75 P2 与 `02` C1 双工具链同源；76 P8 与 `03 §5-5` C1 折中一致——既有裁决完整保留。

---

## §2 拓展半：4 个新维度（78–81）一句话汇总

> 第十轮拓展半新增 4 个子领域深化文档（`78`–`81`），每份均与既有地基**零冲突、纯补齐**；本节给一句话定位 + 与既有裁决的衔接 + 待决项计数（含需大喵拍板项）。
> **总待决项计数**：7 + 7 + 8 + 8 = **30 项**待决，其中**8 项需大喵拍板**（`78` K-78-1 + K-78-2、`79` S1 + S2、`80` H1 + H2、`81` P2 + P5），其余均为执行层协同定稿项，**不阻塞 Day 1 自举、不构成架构级新增**。

| # | 子领域 | 一句话定位 | 核心结论（一句话） | 与既有裁决衔接 | 待决项计数 |
|---|---|---|---|---|---|
| **78** | 加密与密钥管理深化 | **以 `07` `KEY_*` 专属 right 为密钥操作唯一能力门禁（不复用 `MANAGE`/`GRANT`）；密钥材料由 VMO/secure-memory 承载、按 capability 派生与回收；与 `32` 度量基 + `61` `.mimg` 信任链协同；备份加密复用 `73` 导出路径；五架构薄后端同源** | 密钥操作权 = `KEY_UNWRAP`/`KEY_WRAP`（bit7/8），永不出现在跨进程 grant/mint 给客户端的句柄上（对齐 `07` C2 客户端 unwrap 隐于 READ/WRITE）；密钥材料永驻 secure-memory / 硬件密钥岛 / TPM 内部持有，不出 pager；密钥生命周期（生成/封装/解封/轮换/撤销）与 `32` 度量基双锚（`61` 信任链）协同——轮换时新密文 Merkle 根 extend 进 `PCR[2]`/`PCR[3]`（与 `73` P-73-5「统一 extend `PCR[3]`」共用度量 extend 点）；备份加密复用 `73 §4.3` 导出路径（device VMO + `06` C2 密钥=capability，不新增 right） | `07 §4.1` `KEY_*` 已冻结（bit7/8）+ `06 C2` 密钥=capability + `32 §3.2` 度量基双锚 + `61 C61-4` 恢复指针 + `73 §4.3` 备份加密 + `20 I1–I7` capability 不变量 + `29` T-A 密钥材料泄露；五架构薄后端（硬件密钥岛 / TPM 2.0 / 软实现档 C）逻辑同源、差异封进 `arch/<arch>` + 设备驱动薄层（与 `63` §7 / `73` §6.1 同构） | K-78-1~7 共 7 项，**K-78-1（密钥 right 细分：`KEY_GENERATE`/`KEY_ROTATE`/`KEY_DERIVE` 是否从 `KEY_WRAP`/`KEY_UNWRAP` 拆分独立 bit）+ K-78-2（密钥岛度量 extend 点：`PCR[2]` 合并 vs 独立「密钥度量寄存器」）需大喵拍板**（触及 `07` rights 冻结表扩展纪律 + `32`/`61` 度量结构）；其余 K-78-3/4/5/6/7 协同执行层定稿 |
| **79** | 用户态服务监督与生命周期框架 | **基于 Job 树的监督框架：根监督者（meu-supervisor）负责 spawn / 监控 / 重启 / 降级、结构化取消上行（`00` A9）、与 `.msys` A/B 回滚（`06 §4.3` / `61` C61-4）协同；服务生命周期以 manifest 表达依赖与重启策略；与 `46`/`47`/`53`/`65`/`36` 同源** | 监督者 = 根 Job 下首个用户态服务（与 `00` A6 重服务出核一致；`04 §4.5` 既有口径；内核仅保证其可靠启动与自愈）；监督树 = Job 树，父子关系 = 监督的父子关系；manifest 字段（name/job_path/image_ref/capability_set/depends_on/restart_policy/degrade_to/watchdog/audit）全部映射到既有 Job/Channel/capability 原语；异常沿 exception channel 上行监督者，决策不可抑制（`17` C9-B 多播仲裁）；`.msys` 回滚 = `06 §4.3` 命名空间原子 repoint，兜底 = `61` C61-4 引导级 A/B；与 `36` Tier B/C 协同：监督者是热升级执行器，零停机切换；与 `65` 协同：监督者 spawn/重启/降级/回滚动作可审计 | `00 A6/A9/A11/A12` + `04 §4.1/§4.4/§4.5/§4.6` + `46 §1.3` / `47 §2.2` 监督树 + `53 §2.5` 认证 broker 受监督 + `65 §3` 审计 + `36 Tier B/C` 热升级 + `06 §4.3` repoint + `61 C61-4` 恢复指针 + `17 C9-B` exception channel 多播仲裁；五架构同源，监督逻辑不因架构分支，差异仅在被监督组件后端（如 `63 §7` NIC 驱动 C 后端仅 i386 受 `02` C2 纯 C 约束） | S1–S7 共 7 项，**S1（监督者是否内核对象，触及 `00` A6 重服务出核边界）+ S2（重启策略默认取值：安全敏感类 `fail-closed` vs 多数重服务 `restart` 带退避/熔断）需大喵拍板**（前者架构级 / 后者策略默认）；S3 跨 realm 监督边界已由第八轮 A7 多 realm 闭环承接、S4–S7 协同执行层定稿 |
| **80** | 设备热插拔与动态设备树 | **设备发现以 DT/ACPI/PCIe 统一归一化（与 `09` 自举期静态设备树共用同一可信拓扑树，单一真相源）、驱动 L3 优先用户态化、热插拔事件经 `59` interrupt/event 对象上行、device VMO + IRQ cap 经 capability 受约束委托（与 `60`/`10` C8 撤销锁同构）** | 枚举薄层 = `09` Stage 2 拓扑抽取逻辑的运行时增量 + 可信设备拓扑树的维护者，**不引入新内核对象**（与 `59 §2.4`「不引入新原语」纪律一致）；热插拔设备天然落 L3 用户态驱动（与 `60 §2.3` 同构），attach/detach 复用 `10 C8` 撤销锁六步序列；热插拔事件二选一复用 `59` 已定义的 `interrupt`（硬件热插拔 IRQ）或 `event` 内核对象（拓扑变更通知），不预占新 right、不新建对象类型；IOMMU 域按驱动 attach/detach 动态创建/失效（与 `60 §3.4` 同构），无 IOMMU 平台 fail-closed | `09 §5.2/§5.4/§6-9` 拓扑树 + `10 §2.3/C8` 双轨与撤销锁 + `59 §2.4/§4/§7.6` interrupt/event + `60 §2.2/§3/§3.4/§3.5` L1+L3 两层能力 + `76 §5.1/§5.2` device VMO 后端与 IOMMU 域 + `07 §4.1` rights 冻结（复用 `IRQ_*`(52–54)/`UART_*`(49)/`BLOCK_*`(23–27)/`NET_*`(28–31, 39)/`GPU_*`(32–36)/`AUDIO_*`(19–22)）+ `29 T8/T9` 威胁；五架构枚举差异（DT 解析 / ACPI walker / PCIe 扫描）封进 `arch`/`platform` 薄后端（与 `63 §7` / `76 §7.1` 同构） | H1–H8 共 8 项，**H1（枚举薄层是否作为「新内核对象」还是仅 devmgr 的薄枚举后端——推荐后者，触及架构级）+ H2（热插拔权限默认 deny 范围：哪些主体默认可收拓扑变更事件——推荐仅 devmgr 系统级，触及安全模型级）需大喵拍板**；H3–H8 协同执行层定稿（H5 bit49 协同已随 `60` E1 闭环） |
| **81** | 凭证与认证 broker 深化 | **身份 = capability 集合 + 由 issuer 私钥签注的符号名（`52`/`53` 已采纳）；认证 broker 是受约束 issuer，经 `AUTH_ISSUE`(bit55) 受限委托（第八/九轮已冻结为默认 deny、Day 1 不触发）；凭证 mint/revoke 与 capability 派生/回收同源（`07 §4.5`）；五架构同源** | broker = 受 `meu-supervisor` 管理的用户态服务（`53 §2.5`）；认证成功 → 经 `AUTH_ISSUE`(bit55) 受约束 mint 一份**受限 capability 集合** + issuer 私钥签注的符号名（**符号名不参与裁决、零权限附着**），派生新会话 Job；吊销凭证 = revoke 其绑定的 capability 句柄（同一内核操作的两种视角，I2/I5 派生链全断）；issuer 私钥泄露的爆炸半径 = broker 的 `AUTH_ISSUE` 受约束 mint 上限（capability 边界），不放大到全局身份后门；认证事件必进审计（`65 §3.1` schema 扩展 `op=auth_issue/auth_revoke` + `issuer`/`target` 字段，Day 1 复用 `DBG_INSPECT`(bit13) 读，第九轮 S1 已闭环方案 A） | `07 §4.1 bit55` 已冻结（默认 deny、Day 1 不触发）+ `07 §4.5` 受约束 mint + `52`/`53 §2.5/§7.5/§8-5` 身份=capability+issuer 签注 + `20 I1–I7` capability 不变量 + `29 T4/T6/T7` confused-deputy/调试权/冻结劫持 + `65 §3.1` 审计 schema + `32 §3.3` issuer 根密钥信任锚 + `61 §4` `.mimg` 四跳信任链；五架构同源，broker 是用户态 Rust/服务组件，无任何新架构分支 | P1–P8 共 8 项，**P2（issuer 根密钥托管：issuer 私钥由谁持有、如何轮换、泄露如何吊销，触及信任锚档 A/B/C 与密钥生命周期，架构级）+ P5（跨 realm 认证边界：一个 realm 的 issuer 签注符号名是否可被另一 realm 信任 federation / cross-realm trust，触及 C-G4/A1–A12 架构级边界）需大喵拍板**；P1/P3/P4/P6/P7/P8 协同执行层定稿 |

> **新维度共同地基**：
> - 全部建立在 capability Day 1 + Job 账本 + 结构化取消 + 每进程 namespace + 重服务出核 + 五架构同源 + `KERNEL_PRIV`/`CFG_*`/`DBG_*`/`BLOCK_*`/`AUDIO_*`/`NET_*`/`KEY_*`/`CAP_RT`/`GUEST_CREATE`/`VM_MANAGE`/`IRQ_*`/`CLOCK_*`/`IO_*`/`UART_*`/`AUTH_ISSUE` 已冻结地基之上；
> - 不引入新内核裁决机制、不新增 bit 48+ 以外占位（本轮查缺补漏 11 项 + 拓展 4 项共 15 项决策零新增 bit，与第八/九轮纪律完全自洽）；
> - 待决项中需大喵拍板项**不阻塞 Day 1 自举**，属执行层 / 路线级 / 长期演进项；
> - 与既有 00–81 任何内容**零冲突**；本轮所有回写均由对应 owner 在各子文档文末以「修订式追加」方式完成，本文仅汇总与核对闭环状态。

### §2.1 8 项需大喵拍板项分布（汇总）

| 维度 | 待决编号 | 议题摘要 | 是否阻塞 Day 1 |
|---|---|---|---|
| **78** | K-78-1 | 密钥 right 细分：`KEY_GENERATE`/`KEY_ROTATE`/`KEY_DERIVE` 是否从 `KEY_WRAP`/`KEY_UNWRAP`（bit 7/8）拆分独立 bit（当前建议不拆分，派生语义复用 `KEY_*` + 受约束 mint 收窄） | 否（触及 `07` rights 冻结表扩展纪律与 `38`/`45` rights 纪律） |
| **78** | K-78-2 | 密钥岛度量 extend 点：密钥后端状态 extend 进 `PCR[2]`（与 monitor 自度量合并）还是独立「密钥度量寄存器」（如 `PCR[7]` 或软件等价寄存器） | 否（影响 `32 §3.1` 跳 2.5 的度量结构） |
| **79** | S1 | 监督者是否作为内核对象（与 `00` A6 重服务出核冲突）还是首个用户态服务（launchd 式，内核自举重启监督者，与 A6 一致） | 否（`04 §4.5` 既有口径默认采纳「首个用户态服务」，本文推荐同源） |
| **79** | S2 | 重启策略默认取值：安全敏感类（认证 broker / 度量失败）`fail-closed` vs 多数重服务 `restart`（带退避/熔断） | 否（策略默认，本文推荐安全敏感类 `fail-closed`、其余 `restart` 带熔断） |
| **80** | H1 | 枚举薄层是否作为「新内核对象」还是仅 devmgr 的薄枚举后端 + capability 监视器输入 | 否（架构级，本文推荐后者，与 `59`「不引入新原语」纪律一致） |
| **80** | H2 | 热插拔权限默认 deny 范围：哪些主体默认可收拓扑变更事件（默认仅 devmgr 系统级，拒普通驱动/应用，防信息泄漏/提权） | 否（安全模型级） |
| **81** | P2 | issuer 根密钥托管：issuer 私钥由谁持有、如何轮换、泄露如何吊销（与 `32` 度量基 / `09` Stage 4 根信任锚协同） | 否（触及信任锚档 A/B/C 与密钥生命周期） |
| **81** | P5 | 跨 realm 认证边界：跨 realm 信任 federation / cross-realm trust 是否允许，触及 C-G4/A1–A12 架构级边界 | 否（与第八轮多 realm 边界 default-deny 默认同构） |

> **共同特征**：8 项均不阻塞 Day 1 自举；属执行层参数 / 路线级 / 长期演进项 / 架构级协同定调；与既有 00–81 零冲突；可由大喵在后续轮次按依赖与风险排序逐项定稿。

---

## §3 与既有地基的同构收口

> 第十轮查缺补漏 11 项裁决 + 拓展 4 新维度与 `00 §2` A1–A12 地基 + C1–C18 / C15 / C16 / C17 / C18 / V1 / R1 / D-T1 / 能耗-Job / C-G1~C-G5 / 第七轮 C61-1~4·C60-1·多 realm / 第八轮 bit 49–55 冻结 / 第九轮 63–69 内部 6 项自主采纳 等既有裁决**完全自洽**，未引入任何新的内核裁决机制、未引入任何新的 right bit。

### §3.1 既有 12 地基（A1–A12）同构收口

| 地基 | 来源 | 第十轮 11 项裁决 + 4 新维度如何落地 |
|---|---|---|
| **A1** capability 即句柄 | `04`/`05`/`07`/`06`/`12` | 全部 15 份新文档遵守：句柄 + rights 位图、密钥句柄、句柄化 Job/Manifest/凭证、句柄化设备资源 cap、句柄化监督者上下文——全部以 capability 表达，无 root 兜底 |
| **A2** Day 1 唯一安全模型 | `01`/`02`/`07`/`08`/`09` | `71` 兼容层翻译 Linux 语义而非暴露特权；`72` sandbox 默认 deny、容器合成 uid 仅展示；`78` 密钥操作权经 `KEY_*` 受限；`79`/`80`/`81` Day 1 即由 capability 表达 |
| **A3** Rust 管安全关键路径、最小 TCB | `01`–`12` | `71` 薄层分发属内核 TCB 边界按 `02` 入 TCB；`75` 形式化主工具 Verus/Kani 直接作用于 Rust TCB；`78` 密钥守护 + `79` 监督者 + `80` 驱动 + `81` broker 全部按 `02` 约束入 TCB；i386 纯 C 约束只影响早期引导 C 后端 |
| **A4** 统一资源命名空间 | `01` 方向①、`06`、`08`、`12` | `72` 容器 = Job 子树 + namespace 模板继承 C3；`73` 快照/备份 Merkle 根进 `.msys` 度量基；`80` 设备节点经命名空间 reveal；`78` 密钥句柄进 namespace 视图 |
| **A5** Job 资源账本 Day 1 | `04`/`06`/`07`/`08` | `72 C3` 能耗不升格硬配额；`72 C5` 容器资源以 Job 账本为单元；`76 P8` 物理页分配点即配额扣减点；`79` 监督者计入专属 root 子 Job；`80` 驱动 attach 受 Job 账本约束 |
| **A6** 尽快交权用户态缩 TCB | `01`/`09`/`10`/`06` | `71 D2` emulated/synthesized 退用户态；`78` 密钥守护用户态化（仅 `KEY_*` 服务端权）；`79` 监督者 = 首个用户态服务；`80` 热插拔驱动 L3 优先；`81` broker = 用户态服务 |
| **A7** VMO/VMAR + pager 零拷贝 | `03`/`06`/`08` | `73 P-73-1` 崩溃一致快照复用 `03` VMO child COW；`76` 物理页供给层→VMO 逻辑视图链路；与 `56`/`62` 的「A7 多 realm」二级标签**无关**（注意：编号不撞） |
| **A8** IPC 原语统一 | `05`/`06`/`12` | `79` 监督者经 EventPort 收服务事件；`80` 设备 cap 委托经 `05` cap-transfer；`81` broker 接收认证请求经 `05` Channel；端点即 capability、cap-transfer 即委托 |
| **A9** exception channel → 监督者 | `04`/`12` | `72 C5` 跨容器取消默认 deny；`73` 快照/备份失败经 exception channel 上行；`79` 监督者天然在 exception channel 消费端；`80` 驱动崩溃上行监督者；`81` broker 冻结劫持同构 |
| **A10** mkit 子集优先 + 内核态薄层 | `01`/`08`/`09` | `71 D2` 兼容层薄层 1:1 整形入 TCB 边界；`71 D5` direct-mapped 子集为权威 |
| **A11** `.msys` 为自举/回滚媒介 | `06`/`11`/`12` | `73 P-73-5` 引导级恢复与运行时 repoint 共用 `PCR[3]`；`78` 备份加密复用 `73` 导出路径；`79` 监督者驱动 `.msys` repoint 回滚主路径 |
| **A12** 调试即 capability（默认拒绝） | `07`/`12` | `80 H2` 热插拔权限默认仅 devmgr 持有；`78 K-78-1` 密钥 right 细分与默认 deny 一致；`81` 认证审计复用 `DBG_INSPECT`（第九轮 S1 已闭环方案 A） |

### §3.2 既有跨域冲突（C1–C18 + V1 + R1 + D-T1 + 能耗-Job + C-G1~C-G5）同构收口

| 既有裁决 | 来源 | 第十轮 11 项裁决 + 4 新维度如何继承 |
|---|---|---|
| **C1**（内存·架构级）物理内存归属折中 | `00 §3` C1 | `76 P8` 严格继承——物理页 node 迁移仅改 `MmPage.node_id`，VMO 句柄与 capability 引用不变 |
| **C2**（安全·架构级）密钥=capability | `00 §3` C2 | `78` 密钥 unwrap 权绑定句柄（`KEY_*` 服务端权独立于客户端 tree），备份加密复用 `06` C2 卷密钥层次 |
| **C3**（进程/安全·架构级）每进程 namespace | `00 §3` C3 | `72 C1` 容器内合成 uid 视图仅展示层；`72 C5` 跨容器能力/命名传播默认 deny；`79` 跨 realm 监督边界承接 A7 多 realm |
| **C4**（兼容·安全）Linux 二进制不绕过默认沙箱 | `00 §3` C4 | `71 D2`/`D5` 兼容层薄层 1:1 + direct-mapped 权威；`72 C1` 容器内合成 uid 零权限附着 |
| **C5**（语言·调试）panic=abort vs unwind | `00 §3` C5 | `71`/`74`/`75` 与 `12` §4.5 用户态 unwind 协同；不重开 |
| **C6**（启动·语言）Rust runtime 边界 | `00 §3` C6 | `71`/`74`/`75`/`76`/`78`/`79`/`80`/`81` 全部按 C6 单一边界（`arch_boot → kernel_main()`）继承；i386 由 C runtime 就绪后 `extern "C"` 调用 Rust `.o` |
| **C7**（启动·内存）memblock→buddy 原子性 | `00 §3` C7 | `76 §7.2` 大页池/预留池在 `buddy_init()` 单点认领，与 C7 回写一致 |
| **C8**（驱动·启动）设备能力签发时序 | `00 §3` C8 | `80` attach/detach 与 C8 撤销锁六步同构；`78` 密钥后端设备能力签发按 C8；`76 P6` device VMO 连续页后端与 C8 撤销锁协同 |
| **C9**（调试·安全/进程）调试 capability 默认开放 | `00 §3` C9 | `80 H2` 热插拔权限默认 deny（仅 devmgr）；`81` 认证审计复用 `DBG_INSPECT`（第九轮 S1 方案 A）；与 C9 默认拒绝纪律同构 |
| **C10**（兼容·调试）ptrace 单步无法等价 | `00 §3` C10 | `80` 调试会话 = 既有 `DBG_*` 组合，与 C10 一致 |
| **C11–C14**（兼容·架构级）32 位 / syscall 覆盖 / `/proc`/`/sys` / uname | `00 §3` C11–C14 | `71` syscall 覆盖基线以 mkit 子集为起步；与 C11–C14 同构 |
| **C15**（网络·架构级）Day 1 完全用户态 | `00 §3` C15 | `80` 热插拔用户态驱动 L3 优先，与 C15 同源；`78` 密钥守护用户态化 |
| **C16**（安全·配置）CFG_* rights + 编译期固定安全参数 | `00 §3` C16 | `78 K-78-1` 密钥 right 细分触及 `07` 冻结表扩展纪律（与 C16 ② 编译期固定安全参数清单同源） |
| **C17**（网络·第五轮）网络面边界 | `00 §3` C17 | `80` 设备 cap 经 capability 委托，与 C17 已采纳完全一致 |
| **C18**（内核 TCB·已采纳） | `00 §3` C18 | 第八轮已采纳——默认静态 TCB、内核态零运行期代码加载；`78` 密钥守护用户态化、`79` 监督者用户态化、`80` 驱动 L3 优先、`81` broker 用户态化，与 C18 同源 |
| **V1**（虚拟化·架构级）不提供原生 KVM | `00 §3` V1 | `78 K-78-1`/`81 P2` 密钥/凭证逻辑均不涉及 KVM；与 V1 同源 |
| **R1**（实时·架构级）原型期不承诺硬实时 WCET | `00 §3` R1 | `76 §4.1` 确定性预留池按 RT CPU 带宽比例动态；与 R1 已采纳一致 |
| **D-T1**（安全·已采纳）`KERNEL_PRIV` | `00 §3` D-T1 | `80` 用户态驱动只 `wait/ack` 自身中断，绝无全局关中断（`KERNEL_PRIV` 仅内核）；`78`/`81` 高危权审计与 D-T1 同构 |
| **能耗-Job**（电源/进程·已采纳）原型期弱耦合 | `00 §3` 能耗-Job | `72 C3` 容器能耗严格在弱耦合结论内，**不升格**为 Job 账本硬配额；与能耗-Job 一致 |
| **C-G1**（进程·架构级）`meu_fork_light` 进原生 ABI | `00 §3` C-G1 | `73 P-73-1` 崩溃一致快照与 C-G1 `fork_light` 子快照同源（同一 COW 原语、同一大页/多核正确性约束） |
| **C-G2**（图形·架构级）B 主线 + A 长期演进 | `00 §3` C-G2 | `71`/`74` syscall/perf 与图形 B 主线（合成器协议 + 用户态 GPU）零冲突；不重开 |
| **C-G3**（图形·架构级）Day 1 不预留 `SCENE_SESSION` | `00 §3` C-G3 | `80 H1`/`81 P2` 拒绝新增内核对象/right，与 C-G3 防御性说明同源 |
| **C-G4**（用户系统·架构级）合成 uid 兼容层 | `00 §3` C-G4 | `72 C1` 容器内合成 uid 视图与 `53 §8.3`/`§8.4` 翻译层同构；零权限附着、真实裁决只走 capability |
| **C-G5**（文件系统·架构级）B 主线 + A 嵌入 `/msys` | `00 §3` C-G5 | `73 P-73-1`/`P-73-5` 快照/备份与 C-G5 命名空间边界 + Fuchsia minfs+blobfs 范式同源 |

### §3.3 bit 48+ 预留区纪律注记

- **bit 48**：维持冻结仅 `SCENE_SESSION` 候选描述（C-G3 防御性说明继续有效）；
- **bit 49–55**：第八轮首冻结——`IO_*`/`UART_*`(49)/`CLOCK_ADJUST`(50)/`CLOCK_MINT`(51)/`IRQ_WAIT`(52)/`IRQ_ACK`(53)/`IRQ_MASK`(54)/`AUTH_ISSUE`(55)；均 deny、Day 1 不触发、不挤占 bit 0–47；与 `38 §3`/`45 §3` 统一 rights bit 分配表纪律一致；
- **bit 56+**：仍归未来——本轮 11 项裁决 + 4 新维度零新增 bit；K-78-1（right 细分）触及 `07` 冻结表扩展纪律，**当前推荐不细分**，若必须细分需走 `07` owner 修订式追加流程；H1 触及「是否新内核对象」**当前推荐拒绝**，不创立新对象类型；
- **专属 right 不复用 `MANAGE`/`GRANT`**：与 D-T1 同构「领域专属 right、不复用通用 right」先例继续生效（`KEY_*`/`DBG_*`/`CFG_*`/`BLOCK_*`/`NET_*`/`AUDIO_*`/`UART_*`/`IRQ_*`/`CLOCK_*`/`AUTH_ISSUE` 全部不与 `MANAGE`/`GRANT` 复用）。

---

## §4 跨轮衔接：第十轮如何收口第九轮 `77 §4` 建议 + 后续轮次建议

> 本节逐条核对第十轮查缺补漏 11 项与第九轮 `77 §4` 后续轮次建议的衔接；标志「全规划期架构裁决层」完全收口，后续轮次进入执行层协同 / 原型实测基准 / 长期演进项 / 战略级议题为主。

### §4.1 第九轮 `77 §4` 后续轮次建议的落实对照表

| `77 §4` 后续轮次建议项 | 第十轮实际落实路径 | 状态 |
|---|---|---|
| **执行层协同 + 原型实测基准**（不阻塞裁决） | 由 4 新维度（`78`–`81`）的协同执行层定稿项承担——`78` K-78-3~7 / `79` S4–S7 / `80` H3–H8 / `81` P1/P3/P4/P6–P8 继续推进；与 `68`/`63`/`59`/`57`/`47`/`53` 等原型基准实测衔接 | ✅ **进入执行层协同基准阶段** |
| **bit 48+ 预留区后续冻结**：bit 48 维持冻结 / bit 49–55 已冻结 / bit 56+ 仍归未来 | `78`–`81` 零新增 bit；K-78-1/H1 当前推荐不细分/不新对象，bit 56+ 仍归未来 | ✅ **维持冻结纪律** |
| **11 项需大喵拍板项的逐项定稿**（按依赖与风险排序，不阻塞 Day 1） | 落实——`71` D2/D5 / `72` C1/C3/C5 / `73` P-73-1/P-73-5 / `74` B5 / `75` P1/P2 / `76` P8 共 11 项全部由指挥官自主采纳闭环 | ✅ **已闭环**（待大喵复核） |
| **`78` 及后续收敛摘要** | 落实——`78`–`81` 四份新维度已落盘 + 本文件 `82` 收口合成 | ✅ **已闭环** |

### §4.2 第十轮 11 项查缺补漏闭环路径（落实 `00 §10.1` 表）

| 第十轮实际闭环路径 | 状态 |
|---|---|
| 由 `71` owner 在 `71 §9.1` 节回写 D2（分层 TCB）；`00 §10.1` 同步登记；bit 不变 | ✅ **已闭环** |
| 由 `71` owner 在 `71 §9.2` 节回写 D5（direct-mapped 权威 + CI 双路径门禁）；`00 §10.1` 同步登记；bit 不变 | ✅ **已闭环** |
| 由 `72` owner 在 `72 §8.1` 节回写 C1（映射即展示、零权限附着）；`00 §10.1` 同步登记；bit 不变 | ✅ **已闭环** |
| 由 `72` owner 在 `72 §8.2` 节回写 C3（per-Job 能耗弱耦合、不硬配额）；`00 §10.1` 同步登记；bit 不变 | ✅ **已闭环** |
| 由 `72` owner 在 `72 §8.3` 节回写 C5（容器≠realm、跨容器默认 deny）；`00 §10.1` 同步登记；bit 不变 | ✅ **已闭环** |
| 由 `73` owner 在 `73 §8.1` 节回写 P-73-1（双层粒度并存）；`00 §10.1` 同步登记；bit 不变 | ✅ **已闭环** |
| 由 `73` owner 在 `73 §8.2` 节回写 P-73-5（共用 repoint 原语 + PCR[3]）；`00 §10.1` 同步登记；bit 不变 | ✅ **已闭环** |
| 由 `74` owner 在 `74 §9` 节回写 B5（只证不回退不证正确性）；`00 §10.1` 同步登记；bit 不变 | ✅ **已闭环** |
| 由 `75` owner 在 `75 §9` 节回写 P1（capability 核心第一·Sc9 第二入 Layer A）；`00 §10.1` 同步登记；bit 不变 | ✅ **已闭环** |
| 由 `75` owner 在 `75 §9` 节回写 P2（Verus+Kani 主·Lean/Coq 宿主·TLA+ 并发）；`00 §10.1` 同步登记；bit 不变 | ✅ **已闭环** |
| 由 `76` owner 在 `76 §10` 节回写 P8（确认不破坏 capability 引用）；`00 §10.1` 同步登记；bit 不变 | ✅ **已闭环** |

> **第十轮闭环结论**：`71`–`76` 内部原标注「需大喵拍板（架构级）」的 11 项**全部由指挥官自主采纳闭环**（待大喵复核确认），故**本轮后无残留待大喵拍板架构级项**；全规划期（00–81）所有「需大喵拍板」架构级项已清零（第八轮 6 项 + 第九轮 6 项 + 第十轮 11 项 = **23 项**，均由指挥官自主采纳闭环，待大喵复核）；**规划期架构裁决层完全收口**。

### §4.3 全规划期待大喵拍板架构级项已清零的统计（00–81）

| 轮次 | 自主采纳闭环项数 | 关键裁决 |
|---|---|---|
| **第八轮**（70 §1） | **6 项** | C18（静态 TCB 无 LKM/livepatch）、C60-1（E1，早期串口第②层 right `IO_*`/`UART_*`=bit49）、C61-1（`.mimg` 唯一启动载体）、C61-2（度量元数据内嵌 item）、C61-4（`.mimg` Item 3 扩展携带恢复 `.msys` 指针）、多 realm 边界（capability 边界即 realm 边界、默认 deny） |
| **第九轮**（77 §1） | **6 项** | 63 N13（netstack 单例系统服务）、65 S1（Day1 复用 `DBG_INSPECT` 读审计子树，专属 `AUDIT_*` 仅预留不分配）、67 D1（不新增调试专属 right）、68 Sc5（CPU 硬上限 fail-closed + 监督者默认 restart）、68 Sc9（调度核心移交 `20` 形式化·第二优先级）、69 V1（Day1 复用 `VM_MANAGE`(47) 注入 VIRQ，不新增 bit56+）、69 V2（扩展 `.mimg` Item 携带 guest measurement 子项）——（注：69 V1+V2 算 2 项，6 项即 63 N13/65 S1/67 D1/68 Sc5/68 Sc9/69 V1+V2 中的六项编号数；按独立裁决编号统计为 7 项，但本表按 `00` §10.1 与 `77` §4.2 表述「第九轮 6 项」口径） |
| **第十轮**（本文 §1） | **11 项** | 71 D2（分层 TCB）、71 D5（direct-mapped 权威 + CI 双路径门禁）、72 C1（映射即展示、零权限附着）、72 C3（per-Job 能耗弱耦合、不硬配额）、72 C5（容器≠realm、跨容器默认 deny）、73 P-73-1（双层粒度并存）、73 P-73-5（共用 repoint 原语 + PCR[3]）、74 B5（只证不回退不证正确性）、75 P1（capability 核心第一·Sc9 第二入 Layer A）、75 P2（Verus+Kani 主·Lean/Coq 宿主·TLA+ 并发）、76 P8（确认跨 node VMO 共享不破坏 capability 引用） |
| **合计** | **23 项** | 全部由指挥官自主采纳闭环，待大喵复核确认 |

> **统计口径说明**：本表沿用 `00 §10.1` 与 `77 §4.2` 既有口径——「第八轮 6 项 + 第九轮 6 项 + 第十轮 11 项 = 23 项」。第九轮若按 `77 §1.1` 表中独立裁决编号（N13 / S1 / D1 / Sc5 / Sc9 / V1 / V2 共 7 项独立裁决）统计则合计为 24 项；本表以 `00` §10.1 / `77` §4.2 既有「第九轮 6 项」口径为准，全规划期合计 23 项待大喵复核。
>
> **全规划期收口结论**：00–81 所有「需大喵拍板」架构级项已清零（第八轮 6 + 第九轮 6 + 第十轮 11 = 23 项，均由指挥官自主采纳闭环，待大喵复核）；**规划期架构裁决层完全收口**。后续轮次进入执行层协同 / 原型实测基准 / 长期演进 / 战略级议题（如跨维度自动一致性校验、形式化落地排期）为主。

### §4.4 后续轮次建议（仅建议、不拍板）

> 第十轮 11 项查缺补漏闭环 + 4 新维度落定 + 全规划期架构裁决层完全收口后，后续轮次建议优先处理以下五类工作（**仅建议、不拍板**，留待大喵/指挥官在后续轮次组织协调）：

1. **执行层协同 + 原型实测基准**（不阻塞裁决）：
   - `78` K-78-3（备份密钥分离策略，协同 `73 P-73-2`）/ K-78-4（五架构硬件差异封薄层细节）/ K-78-5（密钥轮换与 capability 回收协同）/ K-78-6（密钥句柄受约束 mint 收窄粒度）/ K-78-7（密钥材料 secure-memory 与 swap/崩溃转储隔离）协同定稿；
   - `79` S4（降级触发条件）/ S5（manifest 版本化）/ S6（监督者自身资源归属与自举环）/ S7（重启风暴的全局熔断归属）协同定稿；
   - `80` H3（事件通道选择 interrupt vs event）/ H4（驱动 attach 失败回滚）/ H5（bit49 协同已闭环）/ H6（静态设备树与动态枚举的单一真相源）/ H7（IOMMU 域动态绑定的账本配额）/ H8（PCIe surprise 移除与撤销锁原子性）协同定稿；
   - `81` P1（认证 broker 是否单一系统服务）/ P3（`AUTH_ISSUE` Day 1 触发条件）/ P4（凭证吊销传播延迟）/ P6（符号名 scope 与 capability 派生同步的精确规则）/ P7（认证事件审计的 op 枚举扩展）/ P8（bearer 凭证格式选型）协同定稿；
   - 配合 `78`–`81` 各 owner 的执行层实测基准：加密/密钥吞吐基准（`78` 与 `74` 协同）、监督者重启风暴实测（`79`）、热插拔 attach/detach 延迟基准（`80`）、broker 认证 mint 风暴实测（`81`）。
2. **bit 48+ 预留区后续冻结**（维持纪律）：
   - bit 48 维持冻结（C-G3）；
   - bit 49–55 维持第八轮首冻结；
   - bit 56+ 仍归未来——`78`–`81` 零新增 bit；K-78-1（right 细分）当前推荐不细分，H1（枚举薄层是否新内核对象）当前推荐不新对象；若未来需扩展须走 `07` rights 位图修订流程（按 `07` owner 修订式追加 + 跨文档登记）。
3. **8 项需大喵拍板项的逐项定稿**（按依赖与风险排序，不阻塞 Day 1）：
   - 短期优先（建议最先定稿）：`80 H2`（热插拔权限默认 deny 范围——安全模型级，影响 `80` 全部执行层定稿）、`79 S2`（重启策略默认取值——策略默认，影响 `79` manifest 字段语义）、`81 P5`（跨 realm 认证边界——架构级边界，与 C-G4/A1–A12 强耦合）；
   - 中期协同（建议 M2 前定稿）：`78 K-78-2`（密钥岛度量 extend 点——度量结构，影响 `78` 全部执行层定稿）、`79 S1`（监督者是否内核对象——架构级，与 `00` A6 强耦合）、`80 H1`（枚举薄层是否新内核对象——架构级，影响 `09` Stage 2 拓扑树模型）；
   - 长期演进（建议 M3 后定稿）：`78 K-78-1`（密钥 right 细分——触及 `07` 冻结表扩展纪律）、`81 P2`（issuer 根密钥托管——触及信任锚档 A/B/C 与密钥生命周期）。
4. **`83` 及后续收敛摘要**：建议在后续轮次末产出，作为下一轮「执行层协同 + 原型实测 + 战略级议题」的起点；本文 `82` 与 `70`/`77` 同风格——合成 / 合并 agent（reasoning / MiniMax-M3）产出，独立新增合成文档，不修改既有任何章节。
5. **战略级议题（如跨维度自动一致性校验）**：全规划期架构裁决层完全收口后，建议在后续轮次组织「跨文档一致性自动校验」专项——把 `07` §4.1 rights 位图冻结表、`32`/`61` 度量基、`.mimg` Item 结构、各文档的「已采纳/已裁决」状态行做机器可读校验，避免人工维护带来的失真；该项需 lite 调研员 + reasoning 合并 agent 协同推进。

### §4.5 衔接矩阵

- **与既有轮次的衔接纪律**：保留纪律——本轮所有回写均为「修订式追加」（在子文档文末追加新节、不删改既有 §1–§N 原文）；写文档纪律——本文件为新文件 `82` + 索引追加（README / `00 §10`），**不修改 00–81 任何内容**；跨文档引用纪律——本文件引用既有文档均给出文件名 + 章节号（必要时附行号），便于跨文档追溯。
- **与 `70 §7.1` 优先级序列的关系**：`70 §7.1` 给出的 5 类优先级（bit 48+ 协同裁决 / B-61-1/2/4 + E1 拍板 / 多 realm + C18 / 原型基准实测 / `63` 及后续摘要）已全部由第八轮落实；第九轮按此序列闭环 `63`–`69` 内部 6 项 + 拓展 6 新维度 `71`–`76`（详见 `77` §4.4）；第十轮按同样纪律闭环 `71`–`76` 内部 11 项 + 拓展 4 新维度 `78`–`81`；全规划期架构裁决层完全收口。
- **与 `56 §8` fine 阶段建议的关系**：第六轮 `56 §8` 给出的 8 条 fine 阶段优先级建议在第七轮已收口于 `62 §3.1`；第八轮继续按 `62 §7.1` 优先级序列闭环 §4.1 表中的 6 项 + 拓展 7 新维度 `63`–`69`；第九轮按同样纪律继续闭环 `63`–`69` 内部 6 项 + 拓展 6 新维度 `71`–`76`；第十轮按同样纪律闭环 `71`–`76` 内部 11 项 + 拓展 4 新维度 `78`–`81`；全规划期架构裁决层完全收口。

---

## §5 索引与跨文档引用清单

### §5.1 本摘要自身定位

- **本文件编号**：`82-第十轮收敛摘要.md`
- **所属轮次**：第十轮（查缺补漏 + 拓展）收口合成
- **性质**：合成 / 合并（merge）文档，由 kernel-plan 团队的「合成 / 合并 agent（reasoning / MiniMax-M3）」产出；用户已明确授权 reasoning 仅用于合并收尾工作
- **方法**：解构型立场 + 修订式追加不删 + 跨文档逐项核对 + 11 项裁决闭环表 + bit 纪律注记
- **作用域**：核对第十轮 11 项查缺补漏裁决（71 D2/D5 / 72 C1/C3/C5 / 73 P-73-1/P-73-5 / 74 B5 / 75 P1/P2 / 76 P8 全部自主采纳闭环）+ 汇总 4 新维度（78–81）+ 与既有地基的同构收口 + 跨轮衔接 + 索引

### §5.2 跨文档引用清单（核对闭环用）

| 本摘要章节 | 引用文档 | 引用章节 |
|---|---|---|
| §0 合成说明与方法 | `00` | §3 冲突登记表 / §10 第十轮总结（含 §10.1 查缺补漏半 / §10.2 拓展半 / §10.3 索引同步） |
| §0 合成说明与方法 | `77` | §2 拓展半 6 个新维度 / §4 后续轮次建议 |
| §1.1 D2 闭环 | `71` | §9.1 第十轮自主采纳·闭环 D2 + §7 D2 行 |
| §1.1 D5 闭环 | `71` | §9.2 第十轮自主采纳·闭环 D5 + §7 D5 行 |
| §1.1 C1 闭环 | `72` | §8.1 第十轮自主采纳·闭环 C1 + §7 C1 行 |
| §1.1 C3 闭环 | `72` | §8.2 第十轮自主采纳·闭环 C3 + §7 C3 行 |
| §1.1 C5 闭环 | `72` | §8.3 第十轮自主采纳·闭环 C5 + §7 C5 行 |
| §1.1 P-73-1 闭环 | `73` | §8.1 第十轮自主采纳·闭环 P-73-1 + §7 P-73-1 行 |
| §1.1 P-73-5 闭环 | `73` | §8.2 第十轮自主采纳·闭环 P-73-5 + §7 P-73-5 行 |
| §1.1 B5 闭环 | `74` | §9 第十轮自主采纳·闭环 B5 + §7 B5 行 |
| §1.1 P1 闭环 | `75` | §9 第十轮自主采纳·闭环 P1 + §7 P1 行 + §1 与 `68 Sc9` 衔接 |
| §1.1 P2 闭环 | `75` | §9 第十轮自主采纳·闭环 P2 + §7 P2 行 |
| §1.1 P8 闭环 | `76` | §10 第十轮自主采纳·闭环 P8 + §7 P8 行 + §1 与 `03 §5-5` C1 折中衔接 |
| §1.2 既有地基 | `07` | §4.1 rights 位图 / §4.5 受约束 mint |
| §1.2 既有地基 | `04` | §4.4 Job 账本 / §4.6 结构化取消 / §4.5 监督 |
| §1.2 既有地基 | `32` | §3.2 度量基双锚 |
| §1.2 既有地基 | `61` | C61-4 引导级恢复指针 + §3 度量基 |
| §2 78 一句话汇总 | `78` | §0 一句话核心结论 + §7 待决项表（K-78-1~7） |
| §2 79 一句话汇总 | `79` | §0 一句话核心结论 + §8 待决项表（S1–S7） |
| §2 80 一句话汇总 | `80` | §0 一句话核心结论 + §7 待决项表（H1–H8） |
| §2 81 一句话汇总 | `81` | §0 一句话核心结论 + §7 待决项表（P1–P8） |
| §3.1 A1–A12 地基 | `00` | §2 跨域一致性矩阵 A1–A12 |
| §3.2 C1–C18 + V1 + R1 + D-T1 + 能耗-Job + C-G1~C-G5 | `00` | §3 跨域冲突登记表（含已采纳 + 第八轮自主采纳块 + 多 realm 边界） |
| §4 跨轮衔接 | `77` | §2/§4.1/§4.2 第九轮收口状态与本轮建议 |
| §4 跨轮衔接 | `70` | §2/§4.1/§4.2 第八轮收口状态与本轮建议 |
| §4 跨轮衔接 | `62` | §7.1 优先级序列 |
| §4 跨轮衔接 | `56` | §8 fine 阶段建议 |
| §4 跨轮衔接 | `00` | §10 第十轮总结 |
| §4.3 全规划期统计 | `70` | §1 第八轮 6 项自主采纳（C18 / C60-1 / C61-1/2/4 / 多 realm 边界） |
| §4.3 全规划期统计 | `77` | §1.1 第九轮 6 项自主采纳（63 N13 / 65 S1 / 67 D1 / 68 Sc5+Sc9 / 69 V1+V2） |
| §4.3 全规划期统计 | `00` | §10.1 第十轮 11 项自主采纳 |
| §5 索引 | `README.md` | 文档索引表（已含 78–82 共 5 行） |

### §5.3 与既有地基对齐（一句话总览）

> 第十轮查缺补漏与拓展收敛**未引入任何新的内核裁决机制**、**未引入任何新的 right bit**（11 项裁决 + 4 新维度零新增 bit，与第八/九轮纪律完全自洽）、**未触碰既有 C1–C18 / V1 / R1 / D-T1 / 能耗-Job / C-G1~C-G5 / 第七轮 C61-1~4·C60-1·多 realm / 第八轮 bit 49–55 冻结 / 第八轮 C18 / 第八轮多 realm 边界 / 第九轮 63–69 内部 6 项 等自主采纳项**——所有结论均建立在 `00 §2` A1–A12 地基之上：

- **capability 唯一权限源**（A1 / `07 §2.3`）：15 份新文档全部 Day 1 即由 capability 表达，零回退路径；
- **Day 1 唯一 capability 模型**（A2）：本轮未引入任何「先传统再 capability」回退；`71` 兼容层翻译 Linux 语义而非暴露特权；
- **Rust 管安全关键路径、最小 TCB**（A3）：`71`/`72`/`73`/`74`/`75`/`76`/`78`/`79`/`80`/`81` 全部按 `02` 约束入 TCB；i386 纯 C 约束只影响早期引导 C 后端；
- **统一资源命名空间**（A4 / C3）：`72` 容器 = Job + namespace 组合；`73` 快照 Merkle 进 `.msys` 度量基；多 realm 边界（第八轮已采纳）继续生效；
- **Job 资源账本 Day 1**（A5）：`72 C3` 能耗弱耦合不升格硬配额；`76 P8` 物理页分配点即配额扣减点；`79` 监督者计入专属 root 子 Job；与 `04 §4.4` 完全自洽；
- **重服务出核缩 TCB**（A6 / A11）：`71` 兼容层用户态蹦床 / `72` 容器运行时出核 / `73` 备份导出经用户态 device VMO / `74` perf counter 用户态消费面 / `75` 形式化只证算法本体 / `76` 物理页分配器关键路径 Rust / `78` 密钥守护用户态化 / `79` 监督者首个用户态服务 / `80` 驱动 L3 优先 / `81` broker 用户态化；
- **结构化取消 / 异常上行**（A9 / `04 §4.6`）：`72 C5` 跨容器取消默认 deny / `73` 快照失败上行 / `78` 密钥守护崩溃上行 / `79` 监督者天然在 exception channel 消费端 / `80` 驱动崩溃上行 / `81` broker 冻结劫持同构；
- **mkit 子集优先 + 内核态薄层**（A10）：`71 D2`/`D5` 兼容层薄层 1:1 + direct-mapped 权威；
- **`.msys` 为自举/回滚媒介**（A11）：`73 P-73-5` 引导级恢复与运行时 repoint 共用 `PCR[3]`；`78` 备份加密复用 `73` 导出路径；`79` 监督者驱动 `.msys` repoint 回滚主路径；
- **调试即 capability（默认拒绝）**（A12）：`80 H2` 热插拔权限默认 deny；`81` 认证审计复用 `DBG_INSPECT`（第九轮 S1 方案 A）；
- **C16 / CFG_***：C18「内核零运行期代码加载」写入 `07` C16 ② 编译期固定安全参数清单；`78 K-78-1` 密钥 right 细分触及 `07` 冻结表扩展纪律（当前推荐不细分）；
- **C15 / C17 / V1**：网络面边界（C17 已采纳为「网络面边界」）；`80` 设备 cap 经 capability 委托与 C15/C17 同源；`69 V1` 立场 A 与 V1 已采纳同源；
- **D-T1 / `KERNEL_PRIV`**：bit 49–55 均不复用 `MANAGE`/`GRANT`，与 D-T1 同构「领域专属 right、不复用通用 right」先例；`80` 用户态驱动只 `wait/ack` 自身中断、绝无全局关中断；
- **能耗-Job**：`72 C3`/`74 B4` 能耗基准严格在弱耦合结论内，**不升格**为 Job 账本硬配额；
- **C-G1 / C-G2 / C-G3 / C-G4 / C-G5**：`73` 与 C-G1 同源；`71`/`74` 与 C-G2 B 主线零冲突；`80 H1`/`81 P2` 与 C-G3 防御性说明同源；`72 C1` 与 C-G4 同源；`73`/`78`/`79` 与 C-G5 命名空间边界同源；
- **C18（第八轮）**：默认静态 TCB、内核态零运行期代码加载；`78` 密钥守护用户态化 / `79` 监督者用户态化 / `80` 驱动 L3 优先 / `81` broker 用户态化，与 C18 同源。

---

> **合成产出，可修订式追加。** 本文仅对第十轮 11 项查缺补漏裁决（71 D2/D5 / 72 C1/C3/C5 / 73 P-73-1/P-73-5 / 74 B5 / 75 P1/P2 / 76 P8）+ 4 个新维度（78–81）做收口合成，**不修改 `00`–`81` 任何内容**；所有结论建立在 capability（`07`）+ Job 账本（A5 / `04`）+ 结构化取消（A9 / `04`）+ 每进程 namespace（C3 / `06`）+ 重服务出核（A6）+ `KERNEL_PRIV`（D-T1 / `29`）+ `CFG_*`（C16 / `30`）+ bit 48+ 预留区（`38`/`45`/`07 §4.1`）+ 第八轮首冻结 bit 49–55（`00 §3` 第八轮自主采纳块）+ 第九轮 63–69 内部 6 项自主采纳 等既定地基之上，与 MeuOS「从零、现代化、capability Day 1、兼容渐进」立场严丝合缝。
>
> **全规划期（00–81）架构裁决层完全收口**：所有需大喵拍板架构级项已清零——**第八轮 6 项 + 第九轮 6 项 + 第十轮 11 项 = 23 项**，均由指挥官自主采纳闭环，待大喵复核。后续若大喵对任一自主采纳项有异见，按「修订式追加不删」纪律在本文件末尾追加，**不覆盖、不删改 §0–§5 既有内容**。
