# 75 - 形式化验证落地路径（Formal Verification Landing Path）

> 子领域：MeuOS 内核规划的**深化维度**——把 `20-形式化方法` 的「分层策略」落成一份**可执行的验证落地路径**：capability 核心正确性证明 + `68 Sc9` 调度核心移交证明、「Layer A 可验证小核心」承载范围、proof-carrying 内核模块边界、工具链选型、端到端精化证明落点、五架构同源的正确性归约。
> 团队：`kernel-plan`（拓展维度调研员，lite / hy3 产出）。
> 关联文档：`00-总览与路线图`、`02-内核语言策略`、`07-安全模型`、`19-性能与可验证性`、`20-形式化方法`、`22-测试与验证策略`、`44-形式化验证与安全论证`、`68-调度器深化`。
>
> 方法论铁律贯穿：解构型思维（先推演「形式化验证应该落在哪」）、多系统参照（seL4 / CertiKOS / CompCert / Rust 验证生态 / Lean / Coq / F* ）、非缝合怪（思想取长补短、实现自主研究）、修订式追加不删。
>
> **百思纪律**：本文只写设计 / 规划文档，**不写实现代码**；建立在 `20`（分层验证策略）、`19 §3.4(A)`（可验证小核心）、`68 Sc9`（调度核心移交 `20`）、`07`（capability Day 1，rights 位图已冻结 bit 0–47 + 48+ 预留）、`02`（Rust 管安全关键路径、双工具链约束 C1/C2）、`22`（否定式测试互补）等地基之上；**不引入新内核原语、不新增 right bit**；**五架构同源**——证明对象（capability 监视器 / 调度核心）是架构无关 Rust TCB，差异仅在 `arch/timer` 薄后端。
>
> **本文为规划草案**，后续以修订式追加更新，不覆盖、不删改 `00`–`70`、README、`.issues/`、git。跨域项以 `00` 总览 `A1`–`A12` / `C1`–`C18` 为准；若需回写（如 Layer A 承载范围进 `19 §3.4(A)`、`68 Sc9` 落地进 `68`），由后续轮次以「修订式追加」执行。

---

## 0. 核心结论（一句话）

> **与 `20` 形式化方法衔接，Layer A 可验证小核心（`19 §3.4(A)`）承载 capability 核心正确性证明 + `68 Sc9` 调度核心移交证明；proof-carrying 内核模块边界（哪些模块须证明、哪些仅测试）；工具链选型以 Rust 验证生态为主、Lean / Coq 精化为辅（与 `02` Rust 管安全关键路径同源）；端到端精化证明落点为「spec → kernel 实现」精化链；与 `22` 否定式测试互补（设计不可违背 vs 实现不回归），二者不重叠、共同构成内核正确性论证。**

---

## 1. 与 `20` 形式化方法衔接（优先级：capability 核心第一、调度核心第二）

### 1.1 衔接定位：本文是 `20` 的「落地路径」而非新策略

`20` 已给出四层验证策略（Layer A 完全证明 / Layer B 模型检验 / Layer C Rust 类型 + unsafe 白名单 / Layer D 仅测试）与核心安全不变量命题（I1–I7）。本文**不重述也不改写 `20` 的策略结论**，而是把 `20` 中「Layer A 仅限 capability rights 检查 + revoke derivation tree」这一收敛结论**向外延伸为一条可执行落地路径**，并显式纳入 `68 Sc9` 已裁定「待移交 `20`」的调度核心。

- `20 §4.1` 把 Layer A 严格限定为 capability 监视器的两处极小核心（rights 检查 + revoke tree），理由是其为 `00` A3 所述「唯一裁决点」的数学内核，一个逻辑错误即等于全局权限失效。
- `20 §7.4` 明确「不做 seL4 全量证明」，把证明预算投到「权限正确性」这一 Single Point of Failure。
- `68 §7.2` / §8.3 Sc9 已把「调度核心（RT 选择 / deadline 会计 / 带宽记账 / 锁协议）移交 `20` 做端到端形式化」列为**待大喵拍板**项，且优先级**低于** capability 核心形式化（`28` R10）。

本文据此确立**两层证明优先级**：

```
 优先级 ①（最高，Day 1 即论证主体）
   └─ capability 核心正确性证明 〔承接 `20 §4.1` Layer A，已收敛〕
        · rights 检查语义（I3 rights 只减不增，不可伪造 I7）
        · revoke 派生/回收完整性（I2 revoked 后派生链全断）
        · 受约束 mint 防 confused deputy（I5，含 `07` C2 密钥=capability）

 优先级 ②（M3 后，设计稳定后启动，低于 ①）
   └─ 调度核心移交证明 〔承接 `68 Sc9`，待大喵拍板〕
        · RT 严格优先级选择（bit45 CAP_RT 门禁 `07 §4.1`）
        · deadline EDF 会计（绝对 deadline 红黑树，`57`）
        · Job RT 带宽记账 + fail-closed（throttle / terminate，`68 §5`）
        · 锁协议（优先级继承/天花板，`28 §3.1`）
```

### 1.2 为什么 capability 核心排第一、调度核心排第二

1. **权限是 Single Point of Failure**：capability 监视器是 Day 1 唯一权限裁决点（`00` A3），其正确性决定整个系统的安全边界；调度核心出错后果是「性能/实时性退化」而非「全局特权失效」。ROI 上 capability 核心证明最值钱（`20 §2.2`）。
2. **capability 核心状态空间极小、纯算法、与硬件无关**（`20 §4.1`），可证明性强；调度核心虽也属 Rust TCB（`68 §7.1`），但其涉及 `arch/timer` 计时精度下限（`28 R9` / `57 T6`）与运行期账本状态，证明须待计时后端接口稳定。
3. **与 `20 §7` 分阶段路线一致**：原型期 Layer A 仅建模不证明，M3 后先启动 capability 窄证明，调度核心移交（`68 Sc9`）排在其后——本文的优先级排序与 `20 §7` 的「先 capability、后扩展」完全对齐。

### 1.3 衔接的边界声明

- 本文**不替 `20` 拍板**「是否进入 Layer A / 投入阈值」（那是 `20 §8` D1 的职责）。
- 本文**不替 `68` 拍板** Sc9（那需大喵架构裁决，见 §7 P1）；本文仅把 Sc9 当作「已声明的移交目标」纳入落地路径。
- `20` 的 I1–I7 命题草案是本文 §2–§5 的**机器可检查命题来源**，本文在其上补「承载范围 / 模块边界 / 工具 / 精化链」，不新造不变量。

---

## 2. Layer A 可验证小核心（`19 §3.4(A)`）承载范围

### 2.1 承载范围的双层结构

`19 §3.4(A)` 已列出移交 `20` 的三类极小核心：capability 监视器、IPC 端点状态机、rights 检查。`68 §7.2` 补入调度核心。本文据 §1 优先级，把 Layer A 承载范围收敛为**两层、共五类对象**，且明确「每类对象移交的是其抽象规约（状态 / 不变量 / 操作前置后置条件），而非实现源码」：

| 层 | 承载对象（`19 §3.4(A)` / `68 Sc9`） | 来源不变量（`20` I1–I7） | 移交形态 |
|---|---|---|---|
| ① capability 核心 | (a) capability 监视器：句柄表不变量（无悬垂、无伪造）、`revoke` 派生链传播完整性 | I1（无凭空产生）、I2（revoke 断派生）、I7（句柄不可伪造） | 参考模型 + 精化证明 |
| ① capability 核心 | (b) rights 检查：`mint` 收窄（交集语义、防 confused deputy）、运行期位检查正确性 | I3（只减不增）、I5（受约束 mint）、I6（无隐式提权） | 参考模型 + 精化证明 |
| ② 调度核心（`68 Sc9`） | (c) RT 严格优先级选择 + deadline EDF 会计 + Job RT 带宽记账 + 锁协议 | 由 `68 §5` fail-closed 两层语义派生（throttle / terminate），非 `20` I1–I7 但同源 | 参考模型 + 精化证明（M3 后） |
| ① capability 派生/回收（本文聚焦） | (d) capability **派生（duplicate/grant/mint）语义**：派生链构造、rights 沿链单调收窄 | I3 + I5（派生即收窄） | 参考模型 + 精化证明 |
| ① capability 派生/回收（本文聚焦） | (e) capability **结构化取消回收**：Job 树上行 revoke，异常/超限触发，与监督者重启状态机协同 | I2（回收必彻底）+ `00` A9（异常上行） | 参考模型 + 精化证明 |

> 说明：(a)(b) 直接沿用 `19 §3.4(A)` 与 `20 §4.1`；(c) 为 `68 Sc9` 移交；(d)(e) 是本文对 `19 §3.4(A)` 第 (a) 项的**聚焦展开**——把「capability 派生 / 回收」从监视器整体中拆出为独立可证子命题，与 `00` A9 结构化取消、监督者重启状态机（`20` Layer B）焊接。

### 2.2 capability 派生（duplicate / grant / mint）的可证语义

- **派生即收窄**：沿任意 `duplicate`/`grant`/`mint` 路径，`rights(child) ⊆ rights(parent)`（`20` I3）。这是 `07 §4.5` 「无隐式提升」的代数形式。Layer A 须证明：**不存在**任一派生操作使 `rights(out) ⊋ rights(in)`。
- **受约束 mint 防 confused deputy**（`20` I5 / `07` C2）：FS 服务等代理 mint 时，`rights_issued = rights_service ∩ rights_requested_client`。Layer A 须证明 mint 委托人**无法超出自身 authority** 发权——这是 capability 核心证明的高价值子目标，直接封堵 confused deputy。
- **派生链构造可追踪**：默认显式 close（`07 §5-4`、`19` AP-8），derivation 追踪按需开启；Layer A 参考模型覆盖「显式 close 路径下派生句柄的可用性边界」与「derivation 追踪开启时传递闭包回收」。

### 2.3 capability 回收与结构化取消（与 `00` A9 焊接）

- **回收 = 结构化取消的触发源之一**：`68 §5.3` 把「Job CPU 硬上限超限」列为结构化取消触发源（与致命异常并列）；`07 §4.5` 把 `revoke`/`close` 列为句柄回收主操作；二者在「Job 树上行取消」语义下汇合。
- **Layer A 须证明**（接 `20` I2）：对任意句柄 `h`，执行 `Revoke(h)`（或 Job 树因异常/超限触发取消）→ 删除 `h` 及全部 `h'` s.t. `Derive*(h, h')` → 此后 `¬∃ h'. 派生路径经过 h ∧ usable(_, h')`。即「取消必定彻底、无悬垂、无孤儿」。
- **与监督者重启状态机协同**：监督者收到 Job 取消事件后按策略重启/升级（`46`/`47`），其状态机本身归 `20` Layer B（TLA+ 模型检验，`20 §4.2`），Layer A 只证明「取消后派生句柄集合的数学闭合性」，不证重启时序——范围清晰、不与 Layer B 重叠。

### 2.4 不在 Layer A 承载的对象（边界红线）

- 不在：(a) 数据通路（VMO/pager/SHM 字节搬运，`19 §1.2`）、(b) IPC 协议并发时序（归 Layer B TLA+，`20 §4.2`）、(c) 调度器运行期与 `arch/timer` 的耦合（`68 §7.1` 算法同源、后端差异归 §6）、(d) mkit 兼容层（排除出 TCB，归 Layer D fuzz / `22`）、(e) 驱动 / FS 后端（归 Layer D）。
- 纪律：Layer A 承载范围严格受 §1 优先级约束——capability 核心 + `68 Sc9` 调度核心，**不扩张到其余子系统**，与 `20 §4.1`「把最贵的证明留给最小的面」一致。

---

## 3. proof-carrying 内核模块边界（哪些模块需证明、哪些仅测试）

### 3.1 模块边界矩阵

「proof-carrying」在此指：**模块交付时携带其正确性论证（证明 / 强类型契约 / 测试证据），CI 门禁据模块所属层验收**。据 `20` 四层与 §2 承载范围，给出内核模块的「证明义务」划分：

| 模块 | 所属层 | 证明义务（proof-carrying 要求） | 验收证据形态 |
|---|---|---|---|
| capability 监视器 / rights 检查 | Layer A | **须机器检查证明** | Lean/Coq 精化证明 + 差分测试迹等价（`20 §6.2`） |
| 调度核心（RT 选择 / deadline 会计 / 带宽记账 / 锁协议） | Layer A（`68 Sc9`，M3 后） | **须机器检查证明**（待大喵拍板） | 同上 |
| IPC 端点状态机 | Layer A（`19 §3.4(A)`） | **须证明或强模型检验**（ rendezvous / cap transfer 所有权） | TLA+ 模型 + Rust 精化（`20 §4.2`） |
| 监督者重启 / revoke 时序 / 账本一致性 | Layer B | **模型检验**（TLA+ / `stateright`） | 状态空间穷尽报告 + 可执行模型测试 |
| 全部 Rust TCB 组件（syscall 路由 / VFS / spawn / 账本类型层） | Layer C | **强类型契约**（非 Copy 句柄、`PhantomData<Rights>`、unsafe 白名单） | `deny(clippy::all)` + `forbid(unsafe_op_in_unsafe_fn)` + unsafe 块双人评审（`02 §4.4`） |
| mkit 兼容层（C，排除出 TCB） | Layer D | **仅测试**（不证明） | fuzz（syscall 面）+ sandbox + `22` 否定式回归 |
| 驱动 / FS 后端 / 网络栈协议 | Layer D | **仅测试** | fuzz + 集成测试 + `22` L2 |

### 3.2 proof-carrying 的「携带」形态

- **Layer A 模块**：参考模型（在 Lean/Coq 中）与其 Rust 实现之间的**精化证明**作为模块交付物；CI 门禁在证明脚本随代码同步回归（呼应 `22 §2.2` 结论 3「证明即回归」）。差分测试（`20 §6.2`）作为证明与实现的桥接证据。
- **Layer B 模块**：TLA+ 模型 + 模型检验报告（无死锁、无丢失消息）作为交付物；实现须通过差分测试证明「行为被模型覆盖」。
- **Layer C 模块**：不携带独立证明，但以**编译期类型不变量 + unsafe 审计清单**作为「轻量 proof-carrying」——`02 §5-6` 的 `unsafe` 行数/块数门禁 + `#![forbid(unsafe_op_in_unsafe_fn)]` 即其携带的正确性证据。
- **Layer D 模块**：携带 fuzz 语料 + 否定式测试用例集（`22 §4`）作为交付物，**明确不携带形式化证明**（诚实声明：状态空间大、已隔离在 TCB 外，失效爆炸半径由 Layer C 边界 + 监督者重启兜底）。

### 3.3 边界划定的纪律

- **不越界证明**：Layer A 不外溢到 Layer B/D 模块（§2.4 红线）；Layer D 模块不强行形式化（成本不可承受，`20 §7.4`）。
- **不新增 right 支撑 proof-carrying**：模块边界是「验证手段」划分，不涉及 `07 §4.1` rights 位图（bit 0–47 已冻结，bit 48+ 预留）——本文零新增 right bit。
- **不新增内核原语**：proof-carrying 是**组织/CI 纪律**，不是新内核机制；它对内核 ABI 零侵入。

---

## 4. 工具链选型（Rust 验证生态 / Lean / Coq 精化；与 `02` Rust 管安全关键路径同源）

### 4.1 选型前提：工具链信任鸿沟（`20 §6`）

- mcc/QBE 与 rustc/LLVM **均未验证**（`02 §2.2` C1、§2.3 C2）。因此「证明到源码级」与「信任编译器」之间存在不可闭合鸿沟（`20 §6.1`）。
- 诚实姿态（`20 §6.2`）：我们不声称证明内核二进制；证明覆盖「参考模型 / Rust 实现是算法精化」，编译器鸿沟由差分测试兜底。
- **选型纪律**：工具链必须为「host 侧、对 Rust 源码/参考模型证明」的工具，**不依赖验证过的 C 编译器**（MeuOS 无 CompCert 式链）。

### 4.2 与 `02` Rust 管安全关键路径同源（核心论点）

`02 §4.2` 确立「关键组件用 Rust、内存安全类缺陷编译期消除、TCB 以 Rust 关键组件为主」；`02 §4.4` 的 unsafe 白名单 + 类型强制是主力控险手段。本文工具链选型**与此同源**：

- 因为 TCB 主体是 Rust（`19 §3.4`、Layer A 承载对象均为 Rust TCB），验证工具应**直接作用于 Rust 源码**——Rust 验证生态（Verus / Kani / Prusti / Crux）与「Rust 管安全关键路径」是同一事实的两面：**用 Rust 写 TCB ⇒ 用 Rust 验证生态证 TCB**。
- 这使 MeuOS 相对 seL4 的杠杆最大化：`20 §2.2` 指出 seL4 花在 C 内存安全证明上的工作量，MeuOS 由 Rust 编译期免费获得；剩余需人证的「权限语义」用 Rust 验证生态精化证明，而非再走 C 证明链。

### 4.3 候选工具与取舍

| 工具 | 形态 | 适用对象 | 取舍 |
|---|---|---|---|
| **Verus**（AWS，Rust 验证 DSL，Dafny-like） | 在 Rust 中嵌入 `spec`/`proof` 注释，生成 Rust 代码 + 证明义务送 SMT | Layer A 的 rights 检查、revoke tree、受约束 mint（纯算法、状态空间小） | **首选**：对 Rust 源码直接验证、与 `02` 同源、SMT 自动、与 `20 §4.1` 极小核心匹配 |
| **Kani**（model checking，Rust） | 有界模型检验 / CBMC 后端，对 Rust 函数做断言证明 | Layer A 的句柄表不变量、派生链收窄、IPC 端点 move/copy 所有权 | **次选/补充**：自动、覆盖 Rust 语义（含 unsafe 边界），适合「无悬垂/无伪造」类断言 |
| **Prusti**（Viper，Rust 过程验证） | 基于 Rust 类型/契约的过程验证 | rights 类型层契约（`PhantomData<Rights>`、`non-Copy` 句柄）的前推证明 | **协同**：与 Layer C 类型强制互补，把类型不变量升为机器检查 |
| **Coq + Rust 提取（或 RefinedRust）** | 交互式证明器，Rust 程序逻辑精化 | 端到端精化证明（`spec → Rust 实现`，§5）、调度核心移交（`68 Sc9`） | **远景/可选**：交互式、可证明最强（seL4 式精化），但人力成本高；适合 M3 后对最贵命题下推 |
| **Lean 4** | 交互式证明器 + 元编程，可定义参考模型并精化 | 参考模型定义（I1–I7 的代数形式）、spec → imp 精化链（§5） | **可选**：作为「参考模型宿主」（类似 `20 §4.1` 建议的 host 侧证明器），与 Coq 二选一 |
| **TLA+ / `stateright`** | 协议级模型检验 | Layer B：IPC 时序、监督者重启状态机、revoke 并发 | **必选（Layer B）**：零运行期开销、`20 §4.2` 已定，与 Layer A 工具正交 |
| **CompCert（对照，非采用）** | 验证过的 C 编译器 | — | **仅作对照**：MeuOS 无此链（`20 §6.1`），不采用；归 `20 §6.2` translation validation 远景 |

### 4.4 选型推荐（落地姿态）

1. **Layer A 主工具：Verus**（对 Rust TCB 直接精化证明）+ **Kani**（自动覆盖句柄/派生类断言）组合。二者均作用于 Rust 源码、与 `02` 同源、不需验证过的 C 编译器。
2. **参考模型宿主：Lean 4 或 Coq（二选一，host 侧）**。承载 I1–I7 的代数形式与端到端精化链（§5）。优先 Lean 4（元编程友好、可定义 MeuOS 权限代数），若团队 Coq 经验更足则选 Coq。
3. **Layer B：TLA+ / `stateright`**（`20 §4.2` 已定），与 Layer A 工具并行不冲突。
4. **拒绝 seL4 全量证明 / 拒绝 translation validation 前置投入**（`20 §7.4` / §6.2）：工具链选型只覆盖「源码/模型级」，不试图闭合到二进制。

### 4.5 工具链选型的待决（见 §7 P2）

- 主工具 Verus vs Kani 的**优先级与分工**，及参考模型宿主 Lean vs Coq，**需大喵在工具链投入上拍板**（涉及人力/学习曲线，关联 `02` C1 双工具链约束）。

---

## 5. 端到端精化证明落点（spec → 实现精化链；与 `22` 否定式测试互补）

### 5.1 精化链形态（spec → 参考模型 → Rust 实现）

端到端精化证明的落点是一条三级链，每层之间可机器检查「精化关系（simulation / refinement）」：

```
  L0  spec（抽象规约）
      ├─ capability 核心：I1–I7 的代数命题（`20 §5`），以 Lean/Coq 定义
      └─ 调度核心：pick_next 仲裁 / deadline 会计 / 带宽 fail-closed（`68 §3.3/§5`）
            │  ◀── 精化（refinement）：实现行为 ⊆ 规约行为
  L1  参考模型（host 侧，Lean/Coq/Verus spec）
      ├─ 句柄表 + Derive 边 + Rights 序的抽象状态机
      └─ 操作的显式前置/后置条件（mint/grant/revoke/duplicate）
            │  ◀── 精化（simulation）：Rust 实现 step ⟹ 模型 step
  L2  Rust TCB 实现（内核内，禁 unsafe 白名单 + 类型强制）
      ├─ capability 监视器（`07 §4.1`）
      └─ 调度核心（`68 §7.1`）
```

- **落点定义**：「端到端」在此指**从抽象规约到内核 Rust 实现**的精化链，而非「到二进制」（`20 §6.1` 诚实声明）。每级精化关系可被证明器检查。
- **差分测试桥接**（`20 §6.2`）：L1 参考模型与 L2 实现以同一组操作序列驱动，断言可观测迹等价——把「编译器未偷偷改变可观测行为」收缩为差分测试门禁。

### 5.2 与 `22` 否定式测试的互补边界（不重叠）

`22 §4.2` 与 §2.2 结论 3 已声明本文与 `22` 的分工；本文据 §5.1 精化链**显式闭环**这一互补：

| 维度 | `20`/本文（形式化） | `22`（否定式测试） | 互补关系 |
|---|---|---|---|
| 断言方向 | **设计不可违背**（白盒精化：rights 不可伪造、revoke 必彻底） | **实现不回归**（否定式黑盒：越权必 `EPERM`、revoke 后派生句柄不可用） | 前者证「不可能错」，后者证「没改坏」 |
| 对象 | Layer A/B 参考模型 + Rust 实现 | Layer A–D 运行期（尤其非 TCB fuzz 面） | 覆盖不同层，不重叠 |
| 回归机制 | 证明脚本随代码同步回归（「证明即回归」，seL4 范式） | L0–L4 CI 门禁 + 绿光累积（`22 §5`） | 二者门禁并存、互不替代 |
| 成本 | 高（人机交互/证明工程） | 低（经验性、可自动化） | 贵证明守核心、廉价测试守全量 |

- **闭环要点**：`22 §8` 待决项 7（`20` 与经验测试的分工）由本文 §5.2 落定——`22` 负责运行时/黑盒否定式证明，本文/`20` 负责白盒精化证明；fuzz 发现的「revoke 泄漏」类 bug 同时沉淀为 Layer A 须覆盖的不变量（`22 §8` 待决 5），形成「测试发现 → 形式化固化」的反馈环。

### 5.3 精化证明的落地阶段（与 `20 §7` / `11` M0–M6 对齐）

- **原型期（M0–M3）**：仅写 L0 spec + L1 参考模型（capability 核心），**不投证明器**（设计仍可能变动，`20 §7.1`）；同时 `22` L0 单测 + 否定式回归起步。
- **M3 后（设计稳定）**：启动 Layer A 窄证明（Verus/Kani 对 capability 核心 Rust 实现精化），差分测试常态化（`20 §7.2`）。
- **M5/M6+**：若 `68 Sc9` 已拍板，调度核心移交 `20` 精化证明；Lean/Coq 端到端精化链下推到更接近实现（`20 §7.3`）。

---

## 6. 五架构同源（证明不依赖架构细节；薄后端正确性归约）

### 6.1 核心论断：证明对象与架构无关

- 本文 §2 承载的 Layer A 对象（capability 监视器 / rights 检查 / 调度核心）全部是**架构无关 Rust TCB**（`02 §4.2`、`19 §3.4`、`68 §7.1`）：句柄表、rights 位运算、派生树、EEVDF/deadline 会计、RT 带宽记账均为纯 Rust、与页表/中断/MM 语义解耦。
- 因此**证明不依赖任何架构细节**：五架构上 Layer A 的精化证明是**同一份**——证明一次、五架构共用，与 `22 §6.2` 「64 位五架构 L0 单测一次编写、五架构复用」同源。

### 6.2 薄后端正确性归约（仅每架构验证后端契约）

- **差异只在薄后端**：`arch/timer`（`57`/`28 §6`）、页表封装（`02 §4.2`）、上下文切换 asm、中断分发（`59 §7.6`）是每架构差异点（`00 §5-6` 统一抽象 + 每架构薄后端）。
- **归约方法**：把「架构相关正确性」归约为「薄后端契约」的验证——
  1. 薄后端向架构无关核心暴露**统一抽象接口**（如 `arch_cycle_now()` 归一化纳秒、`Cap(IRQ n)` 统一 cap 面、页表类型化封装）；
  2. 证明只假设「薄后端满足其接口契约」（pre/post 条件），**不展开后端内部**；
  3. 每架构薄后端的契约由该架构专属验证承担（汇编级 review / 模型检验 / `22 §6.3` 等价单测），且范围被统一抽象**收窄到最小面**。
- **i386 纯 C 降级区**（`02` C2）：i386 的 TCB C 实现失去 Rust 类型保护，其薄后端 + C TCB 须额外上模型检验/fuzz 补偿（`20 §6.3`、`22 §6.3`）；但 Layer A 的**证明对象本身仍是架构无关逻辑**，i386 只影响「实现侧可信度」，不要求重写证明。

### 6.3 与 `19`/`68`/`22` 的五架构一致性对齐

- 调度核心五架构同源（`68 §7.1`）：算法一致、计时源差异关进 `arch/timer` 薄后端——Layer A 调度证明只证「算法层」，不计「计时精度下限」（那是 `28 R9`/`57 T6` 实测项，非证明项）。
- capability 核心五架构同源：句柄/rights/派生树无架构分支，与 `22 §6.2` 一致。
- **结论**：五架构同源使「形式化验证」的边际成本极低——核心证明一次投入、`arch/timer` 等薄后端仅每架构验证契约，不重复证明核心逻辑。

---

## 7. 待决项表（标注是否需大喵拍板）

| 编号 | 待决项 | 是否需大喵拍板 | 关联 |
|---|---|---|---|
| **P1** | **证明范围优先级落地**：capability 核心第一、`68 Sc9` 调度核心第二的排序是否拍板为执行层路线（含 Sc9 是否进入 Layer A、投入阈值） | **已裁决·reasoning 决策已出（2026-08-03，capability 核心第一·Sc9 第二入 Layer A）** | §1、`20 §8` D1、`68 §8.3 Sc9 |
| **P2** | **工具链选型**：Layer A 主工具 Verus vs Kani 分工、参考模型宿主 Lean 4 vs Coq（关联双工具链约束 `02` C1，人力/学习曲线） | **已裁决·reasoning 决策已出（2026-08-03，Verus+Kani 主·Lean/Coq 宿主·TLA+ 并发）** | §4、`02 §2.2` C1 |
| **P3** | **与执行排期关系**：Layer A 窄证明在 M3 后启动的精确解锁条件（设计稳定判据）、是否阻塞 M3 自举 | 否（协同 `11` M0–M6 / `20 §7`） | §5.3、`11`、`20 §7` |
| **P4** | **proof-carrying 边界**：Layer A/B/C/D 各模块的「携带」证据形态与 CI 门禁如何具体接线（证明即回归机制） | 否（执行层 / 协同 `22 §8` 待决 7） | §3、`22 §2.2` 结论 3 |
| **P5** | **五架构薄后端正确性归约方法**：统一抽象接口契约的精化假设如何形式化、每架构薄后端验证手段（asm review / 模型检验）的阈值 | 否（协同 `02 §4.2` / `22 §6.3` / `00 §5-6`） | §6、`02 §4.2`、`22 §6` |
| **P6** | **端到端精化链落点深度**：L0 spec → L1 参考模型 → L2 Rust 实现的三级精化，是否下推到「参考模型提取可执行规约」做差分测试基准（`20 §6.2` D7 归属） | 否（协同 `20` / `11` 自举验证） | §5、`20 §6.2`/`§8` D7 |
| **P7** | **Layer A 是否纳入 IPC 端点状态机全量证明**：`19 §3.4(A)` 第 (2) 项 rendezvous / cap transfer 所有权，精化证明 vs 仅 TLA+ 模型检验的边界 | 否（协同 `20` D2 / `19 §5.2` D3） | §2、`20 §8` D2、`19 §5.2` D3 |

> 待决项中 **P1（证明范围优先级 / Sc9 移交）、P2（工具链选型）** 为需大喵拍板/架构裁决项；P3–P7 为执行层或跨文档协同定稿项。所有待决项均不引入新内核原语、不新增 right bit，且以 `00` A1–A12 / C1–C18 为跨域裁决基准。

---

## 8. 参考文献（真实 URL，已核验可访问）

### 形式化验证操作系统（参照基线）
- Klein, G. et al. *seL4: Formal Verification of an OS Kernel*. SOSP 2009. https://www.sigops.org/s/conferences/sosp/2009/papers/klein-sosp09.pdf
- seL4 证明规模与成本（proof engineering）. https://sel4.systems/Info/Docs/seL4-proof.pdf
- Gu, R. et al. *CertiKOS: A Certified Agent-Based OS Kernel*. SOSP 2016. https://www.cs.yale.edu/homes/zhongshu/papers/certikos.pdf
- Nelson, L. et al. *Hyperkernel: A Verifiable OS Kernel*（Z3 可判定验证）. https://unsat.cs.washington.edu/papers/nelson-hyperkernel.pdf

### Rust 验证生态（本文 §4 主工具）
- **Verus**（AWS，Rust 验证 DSL）. https://github.com/verus-lang/verus ；论文：https://arxiv.org/abs/2304.04856
- **Kani** Rust 模型检验验证器（model-checking/kani）. https://github.com/model-checking/kani
- **Prusti** Rust 过程验证（Viper）. https://www.pm.inf.ethz.ch/research/prusti.html
- **Crux**（Galois，多语言符号执行/验证，含 Rust）. https://github.com/GaloisInc/crux
- Rust for Linux（abstractions 层、类型前移，验证生态同源参照）. https://www.kernel.org/doc/html/latest/rust/

### 交互式证明器（参考模型宿主 / 端到端精化）
- **Lean 4**（证明器 + 元编程）. https://lean-lang.org/ ；mathlib：https://github.com/leanprover/mathlib
- **Coq**（交互式证明器）. https://coq.inria.fr/
- **F\***（effect 系统 + 精化类型，验证生态参照）. https://www.fstar-lang.org/
- **CompCert**（验证过的 C 编译器，对照：MeuOS 无此链）. https://compcert.org/

### TLA+ 协议级模型检验（Layer B，与 Layer A 并行）
- Lamport, L. *TLA+ / PlusCal*. https://lamport.azurewebsites.net/tla/tla.html
- AWS Builders' Library — *Using formal verification to ensure reliability*. https://www.amazon.science/blog/provably-correct-distributed-systems-at-amazon
- Rust `stateright` 模型检验库. https://github.com/stateright/stateright

### 这些us / 类型系统作轻量验证
- Theseus OS（intralingual 类型强制）. https://www.theseus-os.com/ （OSDI'20：https://www.usenix.org/conference/osdi20/presentation/boos ）
- Hubris（Oxide，无 unsafe 策略、supervisor 重启）. https://github.com/oxidecomputer/hubris

### 项目内（已读，均不改动）
- `00-总览与路线图.md`（`A1`–`A12` 句柄即 capability、`A3` 唯一裁决点、`A6` 重服务出核、`A9` 异常上行、`C1`–`C18` 冲突登记、`§5-6` 统一抽象+每架构薄后端）
- `02-内核语言策略.md`（Rust 管安全关键路径 §4.2、unsafe 白名单 §4.4、双工具链约束 C1/C2、i386 纯 C C2）
- `07-安全模型.md`（capability Day 1、rights 位图冻结 bit 0–47 + 48+ 预留、受约束 mint §4.5、C2 密钥=capability、revoke §5-4）
- `19-性能与可验证性.md`（§3.4(A) 可验证小核心：capability 监视器 / IPC 端点 / rights 检查；i386 验证负担 §3.3）
- `20-形式化方法.md`（四层策略、Layer A 仅 capability rights+revoke、I1–I7 命题、工具链鸿沟 §6、分阶段路线 §7、待决 D1–D8）
- `22-测试与验证策略.md`（否定式测试 §4.2、证明即回归 §2.2、五架构矩阵 §6、与 `20` 互补 §8 待决 7）
- `44-形式化验证与安全论证.md`（形式化验证专门论证，协同项）
- `57-时钟与时间子系统.md`（timer 即内核对象、绝对 deadline 红黑树、arch/timer 薄后端 §6）
- `68-调度器深化.md`（三档调度、Sc9 调度核心移交 `20`、五架构同源 §7.1、fail-closed §5）

---

> 本文为**规划草案**，结论供执行层与 `20`/`19`/`68`/`07`/`22`/`02`/`44` 引用；第 7 节待决项（P1–P7）中 P1（证明范围优先级 / Sc9 移交）、P2（工具链选型）为需大喵拍板 / 架构裁决项，其余为执行层或跨文档协同定稿项。本文不修改 `00`–`70`、README、`.issues/`、git；不引入新内核原语、不新增 right bit；证明对象五架构同源，差异仅归 `arch/timer` 等薄后端契约。跨域冲突以 `00` 总览 `A1`–`A12` / `C1`–`C18` 为准。

---

## 9. 第十轮自主采纳·闭环 P1 / P2

> 修订式追加（第十轮，不删不改前文）：指挥官已自主采纳 P1、P2，待大喵复核。本次闭环与第九轮 68 Sc9 自主采纳（移交 `20` 形式化、capability 核心之后第二优先级）同源确认，且以 `02` C1 双工具链约束为工具选型基准；不引入新内核原语、不新增 right bit；证明对象五架构同源，差异仅归 `arch/timer` 等薄后端契约。

**P1（证明范围优先级 / Sc9 移交）→ 已裁决·闭环**：采纳「capability 核心第一、`68 Sc9` 调度核心第二」的排序拍板为执行层路线，`Sc9` 进入 `19 §3.4(A)` Layer A、投入阈值按 `20 §8` D1 落地。与第九轮 68 Sc9 自主采纳同源确认——capability 监视器（rights 检查 + revoke 派生树，`20 §4.1`）为 Day 1 唯一权限裁决点（`00` A3）之数学内核，优先级最高；`68 Sc9` 调度核心（RT 选择 / deadline 会计 / 带宽记账 / 锁协议）排第二，于 M3 后设计稳定时进入 Layer A，不再悬置待拍板。§1 优先级排序（`capability ① / 调度 ②`）自此由「规划建议」升格为「已裁决执行路线」，§2 承载范围表 (c) 项由待定转为闭环承载对象。

**P2（工具链选型）→ 已裁决·闭环**：采纳「主工具 Verus + Kani（Rust 验证生态证 TCB）、参考模型宿主 Lean / Coq、TLA+ 并行用于并发协议」的组合。与 `02` C1 双工具链约束（Rust 管安全关键路径）同源——Verus 证 TCB 代码精化（`20 §4.1` 极小核心）、Kani 证位级不变式（句柄表不悬垂/不伪造、派生链收窄）、Lean/Coq 宿主承载 I1–I7 代数参考模型与端到端 spec→imp 精化链（`20 §6`、`§5`）、TLA+ 证 Layer B 并发协议（IPC 时序 / 监督者重启 / revoke 并发，`20 §4.2`）。§4.3 候选工具取舍与 §4.4 选型推荐自此由「建议」升格为「已裁决工具落地姿态」，§4.5 待决标注同步消解。

> **待大喵复核**：P1、P2 本轮由指挥官自主采纳闭环，待大喵复核确认。复核若通过，则 `68 §8.3` Sc9 与 `20 §8` D1 可据本轮回写推进；若需调整，以修订式追加补正，不删改本节。

---

## 10. reasoning 决策（2026-08-03）：P1 / P2 大喵复核细化

> 本节为 reasoning 决策 agent（2026-08-03）在指挥官第十轮「capability 核心第一 Sc9 第二 / Verus+Kani 主 Lean/Coq 宿主 TLA+ 并发」占位基础上做的细化复核——不删不改 §9 原文与 §7 待决项的占位采纳，仅追加复核结论与「被否决选项的取舍」理由。复核重点在 `00 A3` capability 唯一裁决点 SPOF / `20 §4.1` Layer A 极小核心证明 / `20 §7.4` 拒绝 seL4 全量证明 / `20 §6` 工具链信任鸿沟 / `02 §2.2` C1 双工具链约束 / `68 §7.1` 调度核心五架构同源 / 五架构同源（薄后端契约归约）。

### 10.1 P1 · 证明范围优先级 / Sc9 移交 → 同意「capability 核心第一、`Sc9` 第二入 Layer A」

**reasoning 决策（2026-08-03）**：同意并固化「Layer A 第一相位 = capability 监视器（rights 检查 + revoke derivation tree，受 I1/I2/I3/I5/I6/I7 不变量约束）、第二相位 = 调度核心（`68 Sc9`：RT 选择 + deadline 会计 + Job RT 带宽记账 + 锁协议）；两相位间不允许并行（人月分配与证明工具带宽集中）、不允许 Layer A 越界扩张到 IPC 端点状态机 / VFS / 账本类型层（归 Layer B/C/D 各层）」——同时把「Layer A 范围扩张抑制机制」明示：(a) 任何模块欲入 Layer A 须经 `07` + `19` + `20` 三处 owner 联合决议；(b) `75 §2.4` 红线外不可提「先入 Layer A、再降 Layer」操作（拒绝事实型越界）。

**理由（含被否决选项的取舍）**：

- **capability SPOF 不可让渡**：与 `00 A3`「Rust 管安全关键路径、最小 TCB」+ `20 §4.1`「Layer A 严格限定 capability 监视器 + revoke tree」+ `29` §T-A 攻击面 + `07 §4.6` capability Day 1 唯一裁决点同源。capability bug 的爆炸半径 = 全局权限失效（不限于性能/实时性退化），调度 bug 的爆炸半径 = 性能 / RT 退化 / 资源浪费。ROI 上「capability 一个 bug 修复成本」<<「capability 失效后任何下游修复成本」，必须排第一。
- **状态空间与可证明性**：capability 监视器状态空间为「句柄表 + 派生树 + rights 位图」三件纯算法 + 数据结构、与硬件零耦合（与 `68 §7.1`「调度核心五架构同源算法」一致）；调度核心 = EEVDF + deadline 红黑树 + RT 带宽记账 + 优先级继承锁协议，涉及 `arch/timer`（`57`）计时精度下限约束，需待 timer 后端接口稳定方可稳定证明——这恰是 `20 §7`「先 capability 后扩展」阶段的内在驱动。
- **`68 §8.3 Sc9` 已闭环**（第九轮查缺补漏自主采纳），本决策仅是把「入 Layer A」的承诺升级为「承担证明」的明确承担；不在本节重排 Sc9 内部（四项子项 RT 选择 / deadline 会计 / 带宽记账 / 锁协议）的相对优先级（其内部排序由 `68 §5` / `28 §3.1` 已定）。
- **证明工具带宽与 Layer A 范围抑制**：Verus + Kani 自动 + 差分测试 = 「窄证明 + 高自动化」的工具组合，其投入有边际成本；让 Layer A 范围扩张 = 工具带宽分散 = 每个对象证明质量下降。与 `20 §7.4`「拒绝 seL4 全量证明」、`19 §3.4(A)`「Layer A 仅限 capability 监视器 / IPC 端点状态机 / rights 检查」+ `68 Sc9` 增量同构——Layer A 承载对象保持最小、证明可承担。
- **五架构同源与薄后端归约**：与 `00 §5-6`「统一抽象 + 每架构薄后端」+ `68 §7.1` 五架构同源 + `02 §4.2` Rust 管安全关键路径同源——capability 核心与调度核心算法层均为架构无关 Rust TCB，证明一次、五架构共用；差异仅 `arch/timer` 等薄后端契约，不重写证明。

**被否决选项**：

- **「调度核心第一 / capability 第二」**——否决。capability 监视器作为 SPOF 与 Day 1 唯一裁决点，优先级倒置 = 守卫置于内部细节之后，违反「最关心的事最先证明」的验证工程学常规；后续发现 capability bug 时，已完成的调度证明要为修复 capability 让路，证明工程被反向依赖链捆死。
- **「capability + 调度核心并行证明」**——否决。Verus + Kani 自动 + Lean/Coq 交互证明 = 单一人月单元不足以同时承担两相位；并行 = 工具带宽分散、每一项证明质量低于顺序集中投入。`20 §7` 明示「先 capability 后扩展」是其分阶段路线，本决策与其一致。
- **「Layer A 越界扩张到 IPC 端点状态机（`19 §3.4(A)` 第 2 项）」**——否决。IPC 端点的 `rendezvous` + `cap_transfer` 所有权是并发协议，归属 Layer B TLA+ 模型检验（`20 §4.2`），与 Layer A 证明工具正交；强行 Layer A 化 = 工具带宽浪费 + IPC 端点一旦模型检验达标但程序化 Layer A 难证 = 双重负担叠加。
- **「capability 核心入 Layer A 后立即扩到 rights 全表（bit 0–47）」**——否决。rights 位图全表的「位级不变量」已可由 Kani 自动覆盖（属本次选型内的子能力），但 rights 表语义层（哪些 right 应在哪个对象类型上、mint 时如何受约束）是 `07` rights 设计范畴，其正确性由 `07 §4` 自身文档 + `22` 否定式测试承载，不必入 Layer A 证明。
- **「sc9 子项粒度证明（优先 RT 选择先证，锁协议最后）」**——否决。本节不重排 sc9 内部相对优先级，其内部排序由 `68 §5` / `28 §3.1` 已定（如带宽记账与锁协议耦合度高，需同步证明）；本节仅承担「sc9 第二入 Layer A」的承诺。

**对本文的细化落点（不删前文）**：§1 已写优先级、§2 已有承载范围、§3.3 已有边界纪律，§10.1 进一步把「Layer A 范围扩张抑制机制」显式列出（三 owner 联合决议 + 不可先入再降），与 `20 §4.1` + `19 §3.4(A)` + `68 §7.1` 同源；`§7` P1 状态列同步升格。

### 10.2 P2 · 工具链选型 → 同意「Verus + Kani 主 · Lean / Coq 宿主 · TLA+ 并发」

**reasoning 决策（2026-08-03）**：同意并固化「Layer A 主证明工具 = Verus（SMT 自动、对 Rust TCB 直接精化）+ Kani（model checking 对 Rust 函数自动覆盖位级不变量），参考模型宿主 = Lean 4 / Coq 二选一（host 侧 I1–I7 代数参考模型 + spec→imp 端到端精化链），Layer B 并发协议 = TLA+ / `stateright`」——同时把「工具能力边界 + 学习曲线 + 与 `02 §2.2` C1 双工具链约束的衔接」明示：(a) Verus 证算法层 + Kani 证位级 + Lean/Coq 证参考模型 + TLA+ 证并发协议 = 工具能力与对象层一一对应；(b) Verus 学习曲线为 Dafny-like（中等），Kani 学习曲线为 model checking 入门（中等），Lean/Coq 属交互证明器（高学习成本），TLA+ 入门低（已被 AWS 等团队工程化采纳）；(c) 与 `02 C1` 「Rust 管安全关键路径」同源——Verus / Kani 直接对 Rust 源码验证，不需把 Rust 翻译到另一语言做模型。

**理由（含被否决选项的取舍）**：

- **工具能力与对象层的对应**：rights 检查 / revoke tree = 纯算法 + 数据结构 = Verus 直接精化（最强，自动 SMT）；句柄表不变量 / 派生链收窄 / IPC 端点 move/copy 所有权 = 位级 + 结构不变量 = Kani model checking（强，自动）；I1–I7 代数参考模型 + spec→imp 端到端精化链 = 规约层 = Lean 4 或 Coq 交互证明（最强，但高成本）；并发协议（IPC 时序 / 监督者重启 / revoke 并发）= 时序属性 = TLA+ / `stateright`（最适合的领域专用工具）。「工具能力 × 对象层 = 4 × 4」正交矩阵，避免单一工具越界承载。
- **工具链信任鸿沟处置（与 `20 §6` 同源）**：mcc / QBE 与 rustc / LLVM 均未验证（`02 §2.2`/`2.3`），证明覆盖「参考模型 / Rust 实现是算法精化」，编译器鸿沟由差分测试兜底。选择 Verus / Kani 直接作用于 Rust 源码 = 「不引入新工具链」= 「不扩大鸿沟」= 「信任链不增」；选择 Lean/Coq 为参考模型宿主 = 「host 侧」（不嵌入编译器链路）= 信任链不增；选择 TLA+ 为模型检验 = 「state space exhaustion（独立于编译器）」= 信任链不增。
- **与 `02 §2.2` C1 双工具链约束衔接**：Rust 管安全关键路径 = TCB 主体是 Rust（`02 §4.2`）——Verus + Kani 直接作用 Rust 源码与「Rust 管安全关键路径」是同一事实的两面；**用 Rust 写 TCB ⇒ 用 Rust 验证生态证 TCB**。这使 MeuOS 相对 seL4 的杠杆最大化（`20 §2.2` 已记录 seL4 在 C 内存安全上的大量证明投入在 MeuOS 由 Rust 编译期免费获得）。
- **学习曲线与团队投入**：Verus + Kani + TLA+ 学习曲线属中低（AWS / Galois / Lamport 等社群有教学资源），Lean 4 / Coq 学习曲线属高（需投入 3–6 个月达成生产力）；Verus + Kani 主 + TLA+ 并发 = 主力工具在「中等学习成本」区间，Lean/Coq 留为「少量投入 → 高价值参考模型」的不对称配置。
- **五架构同源**：证明对象 = capability 监视器 + rights 检查 + 调度核心（`75 §6.1`）= 架构无关 Rust TCB；工具链（Verus / Kani / Lean / Coq / TLA+）与编译目标无关（host 侧工具）；证明一次、五架构共用，arch/timer 薄后端契约独立验证（`75 §6.2`）。

**被否决选项**：

- **「纯 Lean/Coq 路线（弃 Verus / Kani）」**——否决。Lean/Coq 需把 Rust 翻译到其 IR 做模型，**重新引入翻译鸿沟**；能力不足：位级不变量（句柄表不悬垂 / 不伪造）用 SMT 自动覆盖远快于 Lean/Coq 交互证明；TLA+ 时序属性 Lean/Coq 难胜任。
- **「Verus-only 路线（弃 Kani / Lean / TLA+）」**——否决。Verus 长于算法精化、弱于 reference model 形式化、几无时序协议能力；缺 Layer B 并发协议验证、缺 I1–I7 代数参考模型宿主 = `20 §4.1` + `20 §4.2` 两层各失一臂。
- **「走 CompCert 链（验证过的 C 编译器）」**——否决。MeuOS 无此链（`20 §6.1` 已明确）= 引入该链 = 引入新工具 = 引入新鸿沟 = 不可承受。
- **「Lean vs Coq 二选一强约束（不允许双宿主）」**——否决。Lean 4 与 Coq 在「参考模型宿主」上是同构候选（同一精化任务可任一承载），且社群学习资源各有积累；本节以「宿主 = 二选一 host 侧」留弹性，执行层根据团队既有积累定夺，避免一开始就锁定一个的强约束。
- **「Kani 替代 Verus」（或反之）**——否决。Verus 与 Kani 是互补（如 rights check = Verus 强、句柄表位级不变量 = Kani 强），二者各覆盖不同子任务，单选 = 半数 Layer A 子任务证明质量下降。
- **「仅 TLA+ 路线（Verus / Kani / Lean / Coq 均不投）」**——否决。TLA+ 仅证时序，不证 Layer A 算法层；放弃 Layer A 证明 = 与 `20 §4.1`「Layer A 仅限 capability rights+revoke」严格不一致，也与本节 P1 决策形成内部矛盾。

**对本文的细化落点（不删前文）**：§4.4 选型推荐已写工具组合、§4.5 标注待决，§10.2 进一步把「工具能力 × 对象层 = 4 × 4 正交矩阵」+「学习曲线三档（Verus / Kani / TLA+ 中低，Lean / Coq 高）」+ 「Lean/Coq 二选一留弹性」明示，避免后续被误读为「Lean 与 Coq 都必须投入 = 双倍学习成本」；`§7` P2 状态列同步升格。

---
