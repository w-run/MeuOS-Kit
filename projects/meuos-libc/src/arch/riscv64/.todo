# RISC-V 64-bit (riscv64) 运行时移植

## 已验证通过的测试
- ✅ hello: "riscv64 MeuOS libc"（qemu-riscv64-static v7.2.0）
- ✅ atomic: exit 0
- ✅ setjmp/longjmp: "setjmp ok"

## mcc 后端状态
- ✅ SExtThr (Initial-Exec TLS): 已实现，`la.tls.ie` + `add tp` 序列可被 riscv64-linux-gnu-as 正确汇编

## 已修复的 bug
1. `atomic.S`: `andi X, Y, 0xffff` → `slli/srli` 零扩展（andi 不支持 16 位立即数）
2. `atomic.S`: `__atomic_compare_exchange_{1,2}` 缺失字内保留
3. `thread_clone.S`: child 入口参数传递错误
4. `crt1.S`: 缺少 gp 初始化，`exit()` 函数中 gp-relative 访问导致 qemu-system（非 qemu-user）下 segfault
   - 修复：`_start` 开头添加 `.option norelax; la gp, __global_pointer$; .option pop`

## 线程测试（qemu-user 7.2 限制）
- ❌ phase2/bare-tls/malloc-threads 在 qemu-riscv64-static v7.2 下崩溃
- 根因：qemu-user 的 `thread_cpu` 是全局变量而非 TLS
  子线程 `_exit()` 时执行 `thread_cpu = NULL`，导致父线程的
  `thread_cpu` 也被置 NULL。后续 `gettid()` 通过 `thread_cpu` 获取
  当�� CPU 状态时返回错误结果。
- 在 qemu 10.1.0 中已修复：`thread_cpu` 改为 `__thread` TLS 变量
- 见 `qemu-thread-tls-fix.patch`：可将该补丁应用到 qemu 7.2 源码

## 解决路径
1. 应用 `qemu-thread-tls-fix.patch` 到 qemu 7.2 源码并重新编译
2. 或使用 qemu >= 8.x 的 qemu-riscv64-static 二进制
3. 或在真实 riscv64 硬件上运行
