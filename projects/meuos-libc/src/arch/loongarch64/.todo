# 待实现：LoongArch 64-bit (loongarch64) 运行时移植

## 背景
mcc 的 loongarch64 后端相对成熟（专项 ABI/VLA/TLS 汇编回归通过），
libc/运行时尚未移植。

## 需要实现的文件
- `src/arch/loongarch64/atomic.S`        - 原子操作（ll/sc 系列指令）
- `src/arch/loongarch64/setjmp.S`        - setjmp/longjmp
- `src/arch/loongarch64/sigreturn.S`     - rt_sigreturn restorer
- `src/arch/loongarch64/thread_clone.S` - clone + 子线程 TLS（$tp 寄存器）
- `src/arch/loongarch64/tls.c`           - TLS 初始化
- `crt/loongarch64/crt1.S`               - _start 入口
- `src/internal/arch/loongarch64/syscall.S` - syscall gate（$a7 传号，$a0-$a5 传参）

## 关联修改
- `src/internal/syscall.h`：loongarch64 syscall 编号。
- `include/sys/syscall.h`：补 loongarch64 `SYS_*` 宏。
- `Makefile`：`ARCH=loongarch64` 构建规则。

## 验收
- `make ARCH=loongarch64` 全量编译通过。
- qemu-loongarch64 下运行 Phase-2 测试程序。
