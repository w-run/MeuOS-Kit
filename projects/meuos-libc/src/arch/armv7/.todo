# armv7 (ARMv7 hard-float) libc runtime — 已完成

## crt1.S
- ARM EABI 进程入口：读取 argc/argv/enp + __meuos_tls_init + set_tls (mcr p15) + 对齐 SP + main → exit。✅

## syscall.S
- __meuos_syscall6: 6 参数 syscall gate (svc #0, r7=syscall number)。✅

## atomic.S
- 32/16/8-bit load/store/exchange/CAS/fetch-op via ldrex/strex + dmb 屏障。✅

## setjmp.S
- setjmp/longjmp + sigsetjmp/siglongjmp。保存 r4-r11, lr, sp, d8-d15。siglongjmp 的 sigprocmask 已修复（r0 用于 how，r12 保存 env）。✅

## sigreturn.S
- __meuos_restore_rt: rt_sigreturn (svc #173)。✅

## thread_clone.S
- __meuos_thread_clone: clone(2) 包装，CLONE_SETTLS to set TP in child。✅

## set_tls.S
- __meuos_set_tls: `mcr p15, 0, r0, c13, c0, 3`。✅

## tls.c
- TLS variant I (GAP=16). PT_TLS 扫描 + mmap2 + memcpy + set_tls。✅

## aeabi.c / aeabi_wrap.S
- ARM EABI 运行时 helpers: __aeabi_uidiv/idiv/uidivmod/idivmod/uldivmod/ldivmod/memcpy/memmove/memset/memclr。
  64-bit 除法使用纯 32-bit 算法（无递归 __aeabi 调用）。✅

## 验证
- 所有 .S 经 arm-linux-gnu-gcc 汇编通过 ✅
- libc 全量以 cross-gcc 编译通过 ✅
- 与 libc 静态链接 + hello world 链接通过（53KB ELF32/ARM）✅
- 与 libc 静态链接 + atomic 测试链接通过（28KB ELF32/ARM）✅
- qemu-arm 运行时验证：待 qemu-arm 就绪（本 worktree 无 qemu-arm 二进制）
- mcc 编译 libc armv7：待 mcc ARMv7 后端完成（see mcc armv7 backend .todo）
