# 待实现：AArch64 (aarch64) 运行时移植

## 背景
TARGETS.md 推荐在 64 位非宿主 target 中优先选择 aarch64 或 riscv64 移植
`crt1`、原子 runtime、syscall gate 与 MeuOS libc，并增加 qemu 运行测试。
mcc 已有 aarch64 后端代码生成基线（整数 ABI + 浮点汇编回归通过），
但 libc/运行时尚未完整移植。

## 当前进度（2026-07-22）

### 已完成（7 个运行时文件已就位，但 tls.c 尚未编译通过）
- `crt/aarch64/crt1.S` — `_start` 入口，解析 argc/argv/envp，调用
  `__meuos_tls_init` + `main`，传退出码给 `exit`
- `src/internal/arch/aarch64/syscall.S` — `__syscall6` 网关
  （x0→x8 传号、x1-x6→x0-x5 传参、`svc #0` 进入内核）
- `src/arch/aarch64/atomic.S` — 完整 40 个 C11 原子符号
  （ldarb/stlrb 获取/释放、ldaxr/stlxr 独占 ll/sc、dmb ish 屏障）
- `src/arch/aarch64/setjmp.S` — `setjmp`/`longjmp`/`sigsetjmp`/`siglongjmp`
  保存 22 个 unsigned long（x19-x30, sp, d8-d15）
- `src/arch/aarch64/sigreturn.S` — `rt_sigreturn` trampoline
  （syscall 139，`brk #0` 陷阱而非 x86 `ud2`）
- `src/arch/aarch64/thread_clone.S` — `clone(flags=0x013d0f00)`
  子线程不需要手动 `arch_prctl`（CLONE_SETTLS 内核直接设 TPIDR_EL0）
- `src/arch/aarch64/set_tls.S` — C 可调用的 `__meuos_set_tls(tp)`
  （`msr tpidr_el0, x0; ret`；因 mcc 不支持内联汇编而拆出独立 .S）
- `src/arch/aarch64/tls.c` — **变体 I TLS 实现**
  （TPIDR_EL0 指向 TLS 块开头；TLS 变量位于非负偏移）
  **状态：已创建但被 mcc aarch64 代码生成 bug 阻塞，尚未编译通过**

### 已验证（汇编级）
- 6 个 `.S` 文件均可用 `aarch64-linux-gnu-gcc -c` 成功汇编
- mcc aarch64 代码生成对简单输入工作正常（生成有效 aarch64 ELF .o）

### 当前阻塞
mcc aarch64 后端存储 64 位值到全局变量时崩溃
（`src/target/aarch64/aarch64_emit.c: dying: invalid class`）。
诊断与修复方向见 `projects/mcc/.todo/aarch64-store-fix.md`。

## 待办任务（按依赖顺序）

### 任务 8：修改公共运行时代码支持 aarch64
- `Makefile`：新增 `ARCH=aarch64` 分支
  - `CC := $(MCC) --target=aarch64` 交叉编译 C 源
  - `AS := aarch64-linux-gnu-gcc` 汇编 .S（mcc 不支持 `msr`/`mrs`/`svc`/`brk` 等系统指令）
  - `ARCH_RT_OBJS` 追加 `tls.o setjmp.o sigreturn.o thread_clone.o set_tls.o`
- `include/setjmp.h`：
  ```c
  #if defined(__aarch64__)
  typedef unsigned long jmp_buf[22];     /* x19-x30, sp, d8-d15 */
  typedef unsigned long sigjmp_buf[24];  /* + saved mask flag + sigset_t */
  #endif
  ```
- `src/signal/signal.c`：aarch64 `k_sigaction` 只有 3 个字段
  （`{handler, flags, mask}`，无 `sa_restorer`，无 `SA_RESTORER` 位）
- `src/internal/syscall.h`：aarch64 使用 asm-generic 系统调用号表
  （与 x86_64 号码不同；read=63, write=64, close=57, mmap=222, munmap=215,
  clone=220, exit=93, futex=98, exit=93, rt_sigreturn=139 等）
- `src/thread/c11_threads.c`：3 处内联 `munmap` 公式替换为
  `__meuos_tls_free(control->tls)`，并提取 `__meuos_tls_free` 到
  `x86_64/tls.c`、`i386/tls.c`、`aarch64/tls.c` 的公共接口
- ~15 个系统调用包装器：aarch64 无 `open`/`stat`/`mkdir`/`link`/`unlink`
  等，全部走 `*at` 变体（`openat=56`, `newfstatat=79`, `mkdirat=34` 等），
  需要为 aarch64 单独实现或在公共接口加 `#ifdef` 分支

### 任务 9：构建 aarch64 sysroot
- 编译产出 `libc-meuos.a`（含 crt1.o + 全 syscall + stdio + 线程 + signal）
- 编译产出 `libatomic-meuos.a`（aarch64 原子操作）
- 安装到 `${MEUOS_SYSROOT}/lib` + `include`

### 任务 10：mcc aarch64 编译 + QEMU VM 运行回归
- `env/bin/qvm boot aarch64` 启动 aarch64 VM（cortex-a72, virt 机器）
- 9p 共享 `env/share/` ↔ guest `/mnt/host/`
- 编译 `test/c11/atomic_concurrent.c`（counter=2000 等价闭环）
- 静态链接，丢入 `env/share/`，在 guest 内运行
- 验证输出 `counter = 2000`

### 任务 11：回归测试套件 + 提交
- 新增 `projects/mcc/test/aarch64/` 目录
  - `runtime_atomic.c` / `runtime_fp.c` / `runtime_tls.c` / `runtime_threads.c`
  - `runtime.sh`（QEMU VM 运行脚本）
  - Makefile `check-aarch64-runtime` target
- 更新 `STATE.md` §2/§4：P2 标记完成
- 提交到 `aarch64-complete` 分支

## 关键 ABI 备忘
- **AAPCS64**：x0-x7 传参、x0 返回值、16 字节栈对齐
- **Linux syscall ABI**：x8 传号、x0-x5 传参、`svc #0`、返回值在 x0
- **TLS 变体 I**：TPIDR_EL0 指向 TLS 块开头（与 x86_64/i386 变体 II 相反）
- **clone 参数顺序**：x0=flags, x1=child_stack, x2=ptid, x3=tls, x4=ctid
- **CLONE_SETTLS**：内核直接设 TPIDR_EL0（子线程无需 `arch_prctl`）
- **k_sigaction**：3 字段（无 `sa_restorer`），无 `SA_RESTORER` 位

## 验收
- `make ARCH=aarch64` 全量编译通过，产出 `libc-meuos.a` /
  `libatomic-meuos.a` / `crt1.o`
- qemu-aarch64 下运行 atomic / threads / TLS / stdio 回归
- counter=2000 等价闭环通过
