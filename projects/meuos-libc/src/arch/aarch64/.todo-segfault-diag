---
status: resolved
resolved_at: 2026-07-24
resolution: 
  - commit 2a3f5c9 "aarch64: complete libc runtime + qemu gate + TLS GAP_ABOVE_TP fix"
  - 2026-07-22 diag-fpdiag2 (MODE 1-4) on qemu-aarch64 v7.2.0 全部通过
  - test/aarch64-bootstrap.sh 全 6 测试（含 bare_tls errno 端到端）全部通过
---

# aarch64 thread + fputc segfault — 已修复，归档参考

## 背景
2026-07-22 发现 thread_create/join 后调用 fputc 导致 SEGFAULT。
根因：TPIDR_EL0 在 clone 过程中被破坏，导致 _Thread_local errno 写入崩。

## 修复
commit 2a3f5c9 的 TLS GAP_ABOVE_TP 修正了 TLS 布局，同步修复了
TPIDR_EL0 的稳定性。所有 aarch64 运行时测试（含 bare_tls errno 端到端）
已在 qemu-aarch64-static v7.2.0 下通过验证。

## 验证
```bash
# bootstrap 全 6 测试
make ARCH=aarch64 && MEUOS_AARCH64_RUN=1 make check-aarch64-bootstrap

# diag-fpdiag2 4 模式
env/qemu/qemu-aarch64-static env/share/diag-fpdiag2-m{1,2,3,4}
```
