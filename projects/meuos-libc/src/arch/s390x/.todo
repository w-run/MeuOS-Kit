# 待实现：IBM s390x (z/Architecture) 64-bit 运行时移植

## 背景
PORTING.md §5 第 7 条：仅在明确用户空间需求或有可用 QEMU/硬件门禁时排期。
mcc 的 s390x 后端目前也是占位（见 `projects/mcc/src/target/s390x/.todo`），
本目录跟随 mcc 节奏，不提前实现。

## 需要实现的文件
- `src/arch/s390x/atomic.S`        - 原子操作（CS / CSG / LAA / LAAG 系列）
- `src/arch/s390x/setjmp.S`        - setjmp/longjmp（保存 r6-r15 + r14 返回地址）
- `src/arch/s390x/sigreturn.S`     - rt_sigreturn restorer
- `src/arch/s390x/thread_clone.S`  - clone + 子线程 TLS（r0 寄存器或 aarch-specific TLS 入口）
- `src/arch/s390x/tls.c`           - TLS 初始化
- `crt/s390x/crt1.S`               - _start 入口
- `src/internal/arch/s390x/syscall.S` - syscall gate（r1 传号，r2-r7 传参，svc 0 指令）

## 关联修改
- `src/internal/syscall.h`：s390x syscall 编号（参考 Linux `arch/s390/include/uapi/asm/unistd.h`）。
- `include/sys/syscall.h`：补 s390x `SYS_*` 宏。
- `Makefile`：`ARCH=s390x` 构建规则。

## 前置依赖
- mcc 的 s390x 后端落地（指令选择 + s390x ELF ABI + 汇编发射）。
- qemu-s390x 运行验证。

## 验收
- `make ARCH=s390x` 全量编译通过。
- 无宿主 libc 的静态程序运行 `write`/`malloc`/stdio/C11 atomic。
- TLS、`thrd_create/thrd_join`、futex、signal/setjmp 回归。
- qemu-s390x 下运行 Phase-2 测试程序。
