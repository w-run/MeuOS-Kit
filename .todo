# MeuOS Kit 五架构完善 — 待办清单
# 架构：x86_64 i386 aarch64 riscv64 loongarch64
# QEMU: 10.1.0（自编译，env/qemu/qemu-{arch}）

## P0 — 测试基础设施（阻塞回归）

[x] 修复 check-ld-x86_64 / check-ld-sysroot：因 sysroot 未安装 libc 头文件而跳过
    -> make -C projects/meuos-libc install DESTDIR=/workspace/MeuOS-Kit/sysroot PREFIX=/usr
    -> ld link.c: PT_TLS header for .tbss-only case was skipped (QEMU workaround)
    -> 改为使用 .tbss group 的正确地址，不再跳过 PT_TLS

[x] make -C projects/meuos-toolchain check 全量通过
    -> 全部 20+ 项 PASS（含 counter=2000 端到端 + readelf/strip/objcopy/objdump）

[x] 修复 meow 因缺失 sys/utsname.h 和 uname() 的编译失败
    -> 新增 include/sys/utsname.h 和 src/syscall/uname.c
    -> 新增 i386/aarch64/riscv64/loongarch64 syscall 号翻译
    -> make -C projects/meow check 全面通过

## P1 — loongarch64 核心补齐

[ ] mt/as loongarch64 编码器扩增
    -> 当前 640 行 / 1 个编码函数，缺伪指令 (move, st.d 别名等)
    -> 参考 aarch64: 971 行 / 2 个编码函数
    -> 需要补充：beqz/bnez/blt/bge 等条件分支的编码

[x] mcc loongarch64 isel + emit 完善
    -> 基线功能完整：all patterns + O0/O1/O2 + float + struct + VLA + ABI
    -> isel=284行 + emit=447行（对比 x86_64: 946+885行）
    -> 持续优化方向：更多模式规则、寄存器分配 hint、条件移动

[ ] libc loongarch64 exit() 后 cleanup SIGSEGV
    -> bare_tls 输出数据正确（main=5 child=9 errno=31/47），exit 时 crash
    -> 地址 0x41ed08 附近，疑似 TLS cleanup 或 thread TSS free 路径
    -> QEMU 7.2→10.1 后数据正确，退出时仍 crash

## P2 — 跨架构完善

[ ] mt/as riscv64 编码器扩展
    -> 当前 802 行 / 1 个函数
    -> 需要补充更多伪指令和重定位类型

[ ] mt/as aarch64 编码器扩展
    -> 当前 971 行 / 2 个函数，相对完整
    -> 补充 NEON/SVE 指令的编码支持（可选）

[ ] mt/as x86_64: 无独立编码器，依赖 host cc
    -> 当前：x86_64 的架构目标直接透传到 host cc
    -> 不是 bug，但自举后需要自己实现

[ ] mcc C23 特性持续完善
    -> check-c23 已通过，但 #embed suffix/prefix/if_empty、
       _BitInt(N)、属性语法 [[]] 等可选项目待补充

## P3 — 自举验证

[ ] Phase 4: 用 sysroot 内的 mcc + meow 重新编译全套工具
    -> mcc 自己编译自己
    -> meuo-libc 用自产 mcc 编译
    -> mt 用自产 as/ld 汇编+链接

[ ] meow 多架构 YAML 配方
    -> 为所有 5 架构提供完整的 meow.yaml
    -> 验证 meow build --arch=loongarch64 dash / bzip2 等

## P4 — 长期

[ ] m++ C++ 前端启动
    -> 参考 reference/cxx-frontend/ 和 reference/aburi/
    -> C++23 标准逐步实现

[ ] meow 取代 make
    -> 本项目的 Makefile → meow.yaml 迁移
    -> 增量构建、依赖 DAG 完善
