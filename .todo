# MeuOS Kit 五架构完善 — 待办清单
# 架构：x86_64 i386 aarch64 riscv64 loongarch64
# QEMU: 10.1.0（自编译，env/qemu/qemu-{arch}）

## P0 — 测试基础设施（阻塞回归）

[x] 修复 check-ld-x86_64 / check-ld-sysroot：因 sysroot 未安装 libc 头文件而跳过
    -> make -C projects/meuos-libc install DESTDIR=/workspace/MeuOS-Kit/sysroot PREFIX=/usr
    -> ld link.c: PT_TLS header for .tbss-only case was skipped (QEMU workaround)
    -> 改为使用 .tbss group 的正确地址，不再跳过 PT_TLS

[x] make -C projects/meuos-toolchain check 全量通过
    -> 全部 20+ 项 PASS（含 counter=2000 端到端 + readelf/strip/objcopy/objdump）

[x] 修复 meow 因缺失 sys/utsname.h 和 uname() 的编译失败
    -> 新增 include/sys/utsname.h 和 src/syscall/uname.c
    -> 新增 i386/aarch64/riscv64/loongarch64 syscall 号翻译
    -> make -C projects/meow check 全面通过

## P1 — loongarch64 核心补齐

[x] mt/as loongarch64 编码器扩增 + bug 修复
    -> 新增 jirl/syscall/move/nop/ret/jr/conditional branch 等编码
    -> 修复 parse_operands 把立即数当符号的错误（缺 strtol 判断）
    -> 修复 ALU 2RI12 块条件过于宽泛截胡 jirl/beq/syscall 的 bug
    -> 全链路验证：mcc --target=loongarch64 → mt/as → GNU ld → qemu (exit=42)

[x] mcc loongarch64 isel + emit 完善
    -> 基线功能完整：all patterns + O0/O1/O2 + float + struct + VLA + ABI
    -> isel=284行 + emit=447行（对比 x86_64: 946+885行）
    -> 持续优化方向：更多模式规则、寄存器分配 hint、条件移动

[x] libc loongarch64 exit() 后 cleanup SIGSEGV
    -> 阻塞解除：mt/ld 已完整实现 TLS LE reloc + B16/B26/PCALA 支持
    -> mt/as 修复：2RI12 ALU 支持 reloc 表达式, lu32i.d/lu52i.d 支持 %le64_lo20/le64_hi12,
       bstrins.d/bstrpick.d/break/dbar/ibcl 编码, float 3R/unary 指令,
       unconditional b/bl 不被 dbar/break 截胡, resolve_fixups 排除 LA64 reloc
    -> mt/ld 修复：R_LARCH_B16 (64) 应用支持
    -> 全部 6 个测试 ELF 编译检查通过（mt/as + mt/ld 全链路）
    -> hello (getpid+write) qemu-user 运行时验证：exit=0 正确输出
    -> 原子测试 qemu-user 失败（exit=5），疑似 QEMU LL/SC 问题，P2 中跟进
    -> 安装 libc-meuos.a/libatomic-meuos.a/crt1.o 到 sysroot-loongarch64

## P2 — 跨架构完善

[x] mt/as riscv64 编码器扩展 + 链接器分发
    -> 新增 beqz/bnez/blez/bgez/bltz/bgtz 分支伪指令 + tail 远跳转伪指令
    -> 修复 link.c 缺少 riscv64 reloc 分发的 bug（riscv64_apply_reloc 从未被调用）
    -> make -C projects/meuos-toolchain check 全量 20+ 项 PASS
    -> 当前 900+ 行，支持 RV64I + A + F/D + 全部常用伪指令

[x] mt/as aarch64 编码器扩展
    -> 新增 20+ 编码指令（stp/ldp/cmn/msub/csinc/brk/hint/ldrsb/ldrsh/atomics/dmb/msr-tpidr）
    -> 修复逗号分割嵌套方括号 bug（[sp, #-16]! 不再拆分）
    -> make check 全量 20+ 项 PASS，encode.c 从 972 → 1248 行

[x] mt/as x86_64: 独立编码器提取
    -> x86_64 编码从 assemble.c 内联 (~500 行) 提取到独立 src/target/x86_64/encode.c
    -> target.c 中 x86_64 的 encode_insn 从 NULL 改为指向 x86_64_encode_insn
    -> 所有 5 架构统一通过 encode_insn 函数指针调度
    -> make check 全 20+ 项 PASS

[x] mcc C23 特性完善
    -> check-c23 全 20 测试 PASS（embed 参数/BitInt/属性全部通过并验证）
    -> .todo 已过时：所有 AGENTS.md §2.2 列出的 C23 特性均已完整实现

## P3 — 自举验证

[x] Phase 4: 用 sysroot 内的 mcc + meow 重新编译全套工具
    -> check-sysroot-static 全部通过：82 个 .c 编译 + libmcc.a + mcc 链接
    -> 自产 mcc 运行 --version 验证通过，hello world 编译+运行验证通过
    -> 需修复：type.c: mktype 加 qual=QUALNONE, typecompatible 加 TYPESTRUCT/UNION/ENUM
       locale.h/time.h 新增, time_stubs.c (strftime/localtime/setlocale/mkdtemp)
       ir.h/ir_util.c const char* 签名, emitf 函数 const char* 签名
    -> meow 已可通过 mcc + sysroot 构建（check 中的 --bootstrap 步骤）

[x] meow 多架构 YAML 配方
    -> meow 新增 HOST= autoconf triplet 导出（arch -> triplet 自动推导）
    -> bzip2: 纯 meow YAML 构建，零 make/autoconf 依赖（模式规则 + 依赖 DAG）
       mcc 编译 + ar 归档，压缩/解压验证通过
    -> dash/binutils: 当前仍用 ./configure/make，需要预生成 config.h 后迁移

## P4 — 长期

[ ] m++ C++ 前端启动
    -> 参考 reference/cxx-frontend/ 和 reference/aburi/
    -> C++23 标准逐步实现

[ ] meow 取代 make
    -> 本项目的 Makefile → meow.yaml 迁移
    -> 增量构建、依赖 DAG 完善
