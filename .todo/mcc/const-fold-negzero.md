# mcc 常量折叠丢失负零符号（-0.0 折成正零）

> 状态：🔄 开放（2026-08-04 由 exec-integration-lite 排查根因，从 libc fp_fmt 维度迁移到 mcc）
> 关联 commit：无（非聚合引入，mcc 既有缺陷）

## 现象

`printf("%f", -0.0)` 且**同 TU 含 +0.0 常量**时，输出 `0.000000`/`0`（应为 `-0.000000`/`-0`）。

## 根因（2026-08-04 exec-integration-lite 精核实测）

- **非 libc**：libc `fp_fmt.c` / `src/stdio/fp_fmt.c` 对真正负零是正确的（用变量传 `-0.0` 时输出正确）——root cause 在 **mcc 编译器**；
- mcc 常量折叠层对 `0x0p+0` 与 `-0x0p+0` **误归并**：IEEE `0.0 == -0.0` 但位模式不同（0 vs 0x8000000000000000），折叠时因判相等而合并，丢失负零符号位；
- 同 TU 同时含 +0.0/-0.0 时负零被折成正零；只出现负零（无正零）时符号保留。

## 最小复现

```c
printf("%f\n", 0x0p+0);   /* 正零 */
printf("%f\n", -0x0p+0);  /* 负零 */
```
- **mcc**：输出两行 `0.000000`（第二行错，期望 `-0.000000`）；
- **gcc**：输出 `0.000000 / -0.000000` 正确。

## 范围

- `projects/mcc` 的**字面量求值 / IR 常量折叠层**（保留 `-0.0` 的 IEEE 符号位）；
- libc `src/stdio/fp_fmt.c` **无需改动**。

## 验收

- 复现 case 第二行输出 `-0.000000`；
- `make -C projects/mcc check` 全 PASS；
- 不引入其它架构/门禁回归。

## 范围约束

- 由 exec-mcc-lite 修复 mcc 折叠层；doc-pm 只登记与追踪；
- 修复后经验沉淀到 `.agents/knowledge/`。
